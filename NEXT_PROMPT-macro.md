# 다음 세션 프롬프트 — 매크로 모멘텀 레인 (2026-09-17 마감)

> 덧붙이지 말고 **한 블록으로 다시 쓴다**. 이 파일은 09-15 에 세 번 덧붙여져 400줄이 됐고
> 그래서 그날 갈아엎었다. 09-16·09-17 도 갈아엎어서 이 판이다. 다음 세션도 그렇게 한다.

---

## 0. 한 문장

**슬리브는 09-15 에 동결됐고, 09-17 에 「채점을 할 수 있는 상태」가 됐다.**
09-16 까지는 코드가 계열을 09-08 에서 잘라 **채점 봉이 원리상 한 개도 안 쌓이고 있었다.**
그 벽을 갈랐고, 크기 상수를 못 박았다. **남은 것은 오너 결정이고 09-17 에 「일단 냅둔다」로 보류됐다.**

---

## 1. ★★★북극성 — [OWNER 2026-09-17]

> **Optimal Trading System = (Optimal Short-term Portfolio + Optimal Trading Strategy)**

이 한 줄이 이 레인의 물음을 바꾼다. 지금까지 우리는 **전략만** 최적화해 왔고, 포트폴리오
쪽은 레인마다 따로 굴러가는 배분기 **둘**로 갈라져 있다.

    위험 배분기    총위험 고정 · w 0.25 → k_mr 0.954 · k_tr 0.318      (슬리브 등록서 W1·W4)
    증거금 배분기  효율가중 w_i = eff_i/mean(eff) · 100억 · 로트 50억    (krw-crs `PREREG_03` §3.3)

같은 장부를 두고 하나는 **0.954**, 하나는 **1.000** 이라 하고 **중재자가 없다.** 09-16 의 ⑨ 는
둘을 이은 배선이었지 중재가 아니었고, 그래서 「k_mr 을 증거금 여력의 분모에 건다」는 봉합이 남았다.
**§4-1 의 충돌은 Optimal Short-term Portfolio 가 없어서 생긴 병이다.**

★그 눈으로 보면 §4-1 의 두 갈래가 **둘 다 모양이 틀렸다.**

    진짜로 줄인다   위험 상수 하나를 남의 레인 로트에 손으로 박는다 — 배분이 아니라 **협상**
    안 줄인다      평균회귀가 먼저 와 있다는 이유로 100억을 다 갖는다 — 배분이 아니라 **선착순**

증거금은 그날 **가장 값을 많이 사는 곳**으로 가야 한다.

★★그리고 09-17 에 잰 수가 방향을 말한다.

    ρ(평균회귀, 슬리브) = **−0.018**      1/s_mix = **1.2717**
    → 같은 총위험으로 총 노출이 1.000 → 1.2717. **무상관 다리 하나가 공짜로 +27.2%**
    같은 자리에서 우리가 다투던 것은 **+4.4%** 였다 (§4-1). **자릿수가 다르다.**

이 데스크의 기록과도 맞는다 — 브레드스 N=2 라 Sharpe 상한이 0.6대이고(AQR 1.1 은 N=30),
절대수익 매크로의 Sharpe 는 「폭과 결합」의 산물이지 배분 기교의 산물이 아니다.
**병목은 배분이 아니라 원천 수다.**

---

## 2. 09-17 에 한 것 — 동결 벽을 두 등록이 공유하고 있었다

정본 `docs/RESULT_freeze_split_2026-09-17.md` · 커밋 `89c6828e`(09-17) · `6c53d68c`(09-16)

### ① 09-16 인계문 §1 의 진단이 **틀렸다** — 자료 지연이 아니었다

    real_pv01()          2016-01-04 ~ **2026-09-16**  2,646행   라이브 SQL · 안 잘림
    load_irs_series()    2017-01-06 ~ **2026-09-08**  2,389행   ← 그 「2,389봉」

자르던 것은 적재가 아니라 `app/momentum.py:72 FREEZE = "2026-09-08"` 한 줄이었다.
그것은 **09-08 매크로 등록의 벽**인데 IRS 슬리브의 집행 경로가 같은 함수를 지났다. 그래서
`sleeve_daily` 의 `scored = d > FREEZE_DATE(09-15)` 가 **영원히 거짓**이었다 —
**자료를 아무리 다시 적재해도 안 풀리는 자리였다.**

### ② 수리 — 벽을 안 옮기고 갈래를 냈다 (유도는 정본 §2)

    load_irs_series(start=START, end=mo.FREEZE)
        end=mo.FREEZE   기본 — 09-08 등록의 창. **호출부를 하나도 안 고쳤다**
        end=None        라이브 — 슬리브 집행 경로만
    ★캐시 키에 `end` 를 넣었다 (§8 `_RUNS` 와 같은 결함 자리)

`sleeve_dv01_path()` 안에서 **두 창이 갈린다** — 신호·DV01 은 라이브, 크기(vol·배수)는
`blend` 컷으로 동결 창 그대로. ⚠ **`mo.FREEZE` 를 옮기면 안 된다.** 09-08 북의 채점 구간
손익이 샌다. ★비트 동일 재현 `tests/test_freeze_split.py` 다섯이 못 박는다 [§10-9].

### ③ 크기 상수를 못 박았다 — 등록 표가 재현이 안 되고 있었다

옆 레인 적재가 복구되자 **같은 코드가 다른 수를 냈다** — 배수 11.889 → 11.92, 집행표
**2,318행 전부 +0.279%**, pv01 열은 **불변**(커브가 아니라 배수가 움직였다는 진단).
재료가 `crs_evaluate.net_returns` 라 **옆 레인 적재가 이 레인 동결 표를 바꾸고 있었다.**

    sleeve_monitor.py   MULT_REGISTERED = 11.889136764356689   ← ledger run 2026-09-15
                        K_MR_REGISTERED = 0.9537491439525946   ← monitor_state log[0]
                        K_TR_REGISTERED = 0.3179163813175315

★값은 **동결일에 실제로 기록된 것**이다(등록서의 「11.89·0.954·0.318」은 그 반올림이고
시험이 양쪽을 다 맞춘다). ★재계산판은 `mult_live`·`k_mr_live` 로 같이 다닌다.

### ④ 기준일을 두 표가 같은 함수로 지나게 했다 [§10-10]

매크로 신호가 IRS 종가보다 하루 늦게 끝나는 날로 찍으면 거시 북 넷이 엔진에서 조용히
0 이 된다(`ext.get(day, 0.0)`). `sleeve_execution` 은 막고 있었고 **`sleeve_daily` 는 안 막고
있었다** — 둘 다 09-08 에 묶여 있어 안 보이던 자리다. `asof_for()` 하나로 모았다.

### ⑤ 적재 쪽 — 이건 진짜 자료 이야기이고, 거의 복구됐다

`imx_data.timeseries` 의 08-24 정지가 **09-11 까지 구멍 없이 backfill** 됐다. 남은 지연은
3영업일이고 대상은 국고채커브 6항목(3월·6월·9월·1.5년·2.5년·15년) + 통안채커브 4항목
(3월·6월·9월·2.5년)이다. → `fetch_legs.py` 재실행으로 **BSS 6M·9M·1.5Y 가 08-24 → 09-11**
(나머지 여섯 09-16). 1.5Y 는 9다리 중 Sharpe 1등이던 다리다.
옛 판 `krw-crs/work/_bak_2026-09-17/`(그 레인은 git 아님).

---

## 3. 지금 서 있는 것 — 건드리지 말 것

### 동결 둘. 이름이 같아서 헷갈린다. **09-17 까지 상수 하나를 공유하고 있었다.**

| | **IRS 슬리브** (09-15 동결) | **매크로 신호** (09-08 동결) |
|---|---|---|
| 정본 | `docs/PREREG_sleeve_5050_2026-09-15.md` | `data/krw-macro-vintage/PREREG.md` |
| 계기 | IRS 3Y·10Y | 국채선물 |
| 거시 다리 | **테마별 부호 북 넷** 등가중 | 네 테마 부호 **평균** 하나 |
| 위험선호 | **확장 평균 대비** | 0 대비 |
| 채점 | **2026-09-16~** · 첫 판정 2027-09 무렵 | 2026-09-09~ · 750봉 또는 전환 12회 |
| 창의 끝 | `load_irs_series(end=None)` | `mo.FREEZE` = 2026-09-08 |
| 화면 | 없음(집행 스크립트만) | `/momentum` 이 이것을 비춘다 |

⚠ **두 등록 다 「판정일까지 들여다보지 말 것」이다.** 09-08 쪽은 코드도 고치지 않는다.

### 등록 수 (슬리브 · 재동결판) — 안 움직였다

    판정문  Momentum-{trend,macro,blend}-IRS-books-paper
            50/50  SR 1.000 · DSR 0.9955 · PBO 0.1207 · 위약 0.0105/0.0105 · MinTRL 3.75년
            거시   SR 0.925 · DSR 0.9942 · PBO 0.1713 · 위약 0.0105/0.0105 · MinTRL 4.03년
            추세   SR 0.847 · DSR 0.9828 · PBO 0.2117 · 위약 0.0105/0.0158 · MinTRL 5.73년
    결합    w 0.25 · ΔCalmar +0.318 [+0.019, +0.719] **P 0.048** · ΔMDD −0.261 P 0.005
            창 = IRS 달력 ∩ MR 달력 **1,646봉** · k_mr 0.954 · k_tr 0.318 · 총위험 배수 11.89
    ⚠ 결합 기준에서 추세 단독(+0.307 P 0.048)과 **같은 자리**다. 이긴다고 적지 않는다.

### 벽을 내린 뒤 첫 표 — 2026-09-16 (첫 채점일)

    ── 슬리브 주문표 · 기준일 2026-09-16 **(채점 창)** ──
      배수 11.89 · W4 배율 1.000 · 직전 기록 2026-09-08
      3Y   순 DV01 -130.0만 · 액면 46.2억 페이 · 주문 **페이 18.7억**
      10Y  순 DV01 -122.6만 · 액면 15.1억 페이 · 주문 **페이  4.2억**
      합 61.3억 · 회전 22.9억 · 증거금 3.07억
      평균회귀 95.37억 · 여력 4.63억
      ★★평균회귀를 **실제로 안 줄이면** 여력 **0.00억** · 배율 **0.000**

창이 2,389 → **2,395봉**, MR 겹침 1,647 → **1,653봉**. 시험 **1,267 passed · 7 skipped · 1 xfailed**.

⚠ **원장에 안 적었다**(`--dry` 로만 돌렸다). 09-16·09-17 이 채점일로 지나갔고 행이 없다.
신호는 이력에서 결정적이라 **뒤에 소급해 만들 수 있다** — 잃은 것은 없다. 적을지는 §4-2 와 한 몸.

---

## 4. ★[OWNER] 열려 있는 결정 — **09-17 「일단 냅두고」로 보류**

다음 세션은 이걸 **다시 묻지 말고** 오너가 먼저 꺼낼 때까지 둔다. 아래는 그때 쓸 재료다.

### 1. k_mr 축소를 MR 레인에 지시할 것인가 — **이건 사실 배분 층 물음이다(§1)**

왜 줄이는가: 「총위험 고정」은 슬리브를 위에 **얹는** 게 아니라 같은 예산을 75/25 로 **다시
나누는** 것이라, `k_mr = 0.75/s_mix = 0.9538` 이 따라 나온다. ρ = −0.018 이라 분산효과
1/s_mix = 1.2717 이 대부분을 돌려줘 **25% 가 아니라 4.6% 를 뗀다.** 축소가 사 오는 것은
합산 연변동성 4.365억 유지 하나뿐이다(안 줄이면 4.557억 · **+4.4%**).

★**진짜 문제는 위험이 아니라 회계다.** `여력 = 100억 − 증거금 × k_mr` 에서 `k_mr` 은 **위험**
규약인데 **증거금** 분모에 걸려 있다. 로트를 실제로 안 줄였으면 묶인 돈은 100억 그대로고,
09-16 의 여력 4.63억 = 100억 × (1−0.9537) 은 **축소분 그 자체 · 장부에만 있는 여력**이다 [§10-13].

    갈래            해야 할 일                        온전히 서는 날   못 서는 날   평균 배율
    진짜로 줄인다    MR 로트 50 → **47.7억** (그 레인 사전등록 사안)  1,630일      0일      0.998
    안 줄인다       **없음** — W4 가 이미 그날 비례 축소한다         1,397일    **248일**   0.848
                                                                   / 1,653일  (15.0%)

평균회귀가 상한을 다 쓰는 날이 248일(15.0%)이고 **최근 20영업일 중 10일**이다. 09-16 도 그중 하나.

### 2. 슬리브를 실제로 켤 것인가 · 첫 채점 행을 원장에 적을 것인가

배관은 다 섰고 벽도 내려갔다. 09-15 부터 열려 있는 물음이다.

### 3. 성장 축 새 사전등록을 열 것인가

자료는 손에 있고 비용은 0 이다(§6). ⚠ 여는 순간 표본내 측정이 시작된다 — 열기 전에 §6 의
자유도 소비 목록을 등록서에 옮긴다.

### 4. 배분 층을 별도 레인으로 열 것인가 (§1 에서 올라온 것 · **새로 생김**)

    ① 목적함수 하나     지금은 레인마다 다르다(SR · Calmar · ΔCalmar · 낙폭당 배수)
    ② 공통 위험 단위    **있다** — unit-vol 정규화가 이미 레인을 가로지른다
    ③ 공통 제약        **있다** — 증거금 100억 · 조달선 · 국고 현물 공매도 금지
    ★죽는 자리 = **기대수익.** 평가층이 아홉 중 셋만 위약을 통과시켰다. 그 추정치를 평균-분산에
      먹이면 잡음을 최적화한다. N=2~3 에서 정직한 «최적»은 **위험 패리티 + 증거금 제약**이고,
      거기서 얻는 것은 배분이 아니라 ①의 통일과 ③의 정직한 집행이다.
    ⚠ 두 동결 등록은 그 층의 «입력»이 된다. 층이 서면 **새 등록**으로 그 아래로 옮긴다 —
      동결한 규칙을 지금 바꾸면 판정이 무의미해진다.

---

## 5. 집행 배관 — `docs/EXEC_sleeve_2026-09-15.md` 가 상태판 (①~⑨ 완료)

    아침    python -m scripts.sleeve_daily        주문표(목표 아닌 **어제와의 차이**)  `--dry` 면 기록 안 남김
    분기    python -m scripts.sleeve_monitor --quarterly   관문 셋 + 상관 경보
    연 1회  python -m scripts.sleeve_monitor --annual      W2 상향 조건
    대조    python -m scripts.sleeve_monitor --daily --margin-source resim   09-15 까지 쓰던 판

    sleeve_execution.py   실측 pv01 · 집행표 · `asof_for()`   sleeve_prereg_5050.py --structure books
    momentum_irs_books.py 판정문 셋                          sleeve_allocation[_heatmap].py  배분 그림
    margin_headroom.py    (krw-crs) 평균회귀가 오늘 묶은 증거금

★주문표는 **여력 기준일과 주문표 기준일이 어긋나면 경고**하고, **평균회귀를 실제로 안 줄였을
때의 여력**을 같이 찍는다. 지금은 둘 다 09-16 이라 앞의 경고는 안 나온다.

---

## 6. 표본내에서 본 것 — «본 뒤에 안 켠 것»으로 기록

새 등록을 열 때 이 목록을 자유도 소비로 같이 적는다.

    docs/RESULT_theme_books_2026-09-15.md   구조 여섯 (합성/북넷 × 부호/연속 + 논문 정의판 둘)
    docs/RESULT_bok_books_2026-09-15.md     한은 입력 일곱 (금년/내년/평균 축)
    docs/RESULT_cycle_bok_2026-09-14.md     경기순환 한은 교체 · §7 왜 나쁜가 · §8 안 잰 둘

★ 한은 입력은 **논문 구조에서도** OECD 를 못 넘는다(0.923 대 0.932). 한은 GDP 전망 북은 금년
축에서 −0.04 로 죽어 있고 물가 북만 산다(0.877).
★ 국제교역 북은 어느 판에서도 음(−0.27~−0.40)이다. 논문과 정의가 **정확히 같은 유일한 테마**가
유일하게 음수다 — 그 테마의 경제 논리가 원화에서 안 선다는 뜻일 수 있다.
★ 성장 축 대체 정의 자료는 **적재만 해 뒀다**(논문 부록 B 의 「OECD 실현 GDP·CPI 의 YoY 1년
변화, 한 분기 지연」 · `B1GQ_Q` 197개 · `pull_oecd_gdp.py` → `out/gdp_pit_quarterly.csv`).
⚠ 자료지연 중앙 2분기 · 「한 분기 더 지연」은 이중 계산이라 `dg_1y`·`dg_1y_lag1q` 둘 다 냈다.
★ 안 켠 것: 한은 전망치 · 내년 축 · 성장/물가 북 분리 · 확장 평균의 장기 이력 · 두 원천 혼합 ·
  **그리고 GDP 빈티지 — 09-16·09-17 다 안 쟀다. 이 상태를 지킬 것.**
★ 전망치 조달은 견적 문제다 — Consensus Economics(이력 가격 비공개) · Bloomberg ECFC(**데스크
  보유 여부 미확인**) · KCIF 는 `robots.txt` 전면 금지. 정본 `RESULT_growth_axis_sources_2026-09-16.md`.

---

## 7. 계보 — 「왜 과거 결과와 다른가」

정본 `docs/LINEAGE_momentum_numbers_2026-09-15.md`. 표제수 변화 17건을 출처와 함께 귀속했고
**«결과를 보고 고른 것»은 없다**. 교훈은 「관문이 문턱에 붙어 있어서(위약 0.0211 대 0.05 ·
결합 P 0.048 대 0.05) **장부 정리 수준의 변경이 판정을 뒤집는다**」이다.

09-17 에 **한 건이 더 붙는다** — 배수 11.889 → 11.92(옆 레인 적재 복구). 이건 귀속이 아니라
**막았다**(§2-③). 계보에 「재계산되던 상수를 못 박은 날」로 적을 것.

**미확인 하나만 남았다** — 선물 9,852/771 대 10,039/958. 순손익 9,081 이 같고 전·비용이
정확히 187만원씩 갈려 한쪽이 롤 비용을 뺀 것으로 보이나 확인 전이다.

---

## 8. 코드 결함 — 같은 자리를 다시 밟지 말 것

    metrics.py 사유줄     비용전 판이 깨져도 비용후 p 만 찍었다 → 깨진 쪽을 적는다
    _RUNS 캐시 키         `external is not None` 만 봐서 원천 다른 북이 재사용됐다 → 내용 해시
    app.dv01.pv01        **애뉴이티(연)** 다. ₩/bp 로 쓰려면 ×명목×1e-4
    tests/conftest.py     시험이 raw CSV 를 다시 썼다 → 세션 전체 `BIGFOOT_OFFLINE=1`
    발행 캘린더 시험      굴러가는 창 안의 날짜를 못 박아 낡았다 → 자료에서 사례를 찾는다
    margin_budget_book    `.fillna(0)` 이 「자료가 안 왔다」를 「포지션이 없다」로 읽는다 (09-16)
    pull_oecd            `.M.` 만 물어서 분기 계열(GDP)이 통째로 안 보였다 (09-16)
    ★load_irs_series     `mo.FREEZE` 하나로 **두 등록의 창을 같이** 잘랐다 (09-17 · §2)
    ★sleeve_daily        기준일을 `ix[-1]` 로 골라 거시 북 넷이 0 인 날을 목표로 낼 수 있었다 (09-17)
    ★크기 상수           매 실행 재계산 → 옆 레인 적재가 이 레인 동결 표를 움직였다 (09-17)

**안 고치고 기록만 하는 것 셋** — 고치면 동결된 표본이 바뀐다.

    mkt_irs_close 의 정체 행 30일(국내 휴장) · 손익 0 · 변동성 0.6% 차
    margin_budget_book 의 `.fillna(0)` — 고치면 슬리브의 MR 다리 수익률이 바뀐다
    sleeve_execution 의 「상한 초과일 28일」 — 등록서 §4.1 이 인용한 수라 재현되게 둔다
      (배분기 기준 24일을 **같은 함수로** 옆에 같이 찍는다)

★★09-17 에 그 셋째에 딸린 것이 하나 나왔다 — **등록서 §4.1 의 「28일」은 k_mr 축소를 이미
가정하고 센 수다.** 그 가정을 빼면 같은 셈이 **256일**(못 서는 248 + 일부 축소 8)이다.
W4 를 「거의 안 무는 부수 규칙」으로 적었는데 축소를 실제로 안 하면 **열 배 무는 규칙**이다.
**[OWNER 09-17] 일단 냅둔다** — 부기 정정은 §4-1 이 정해진 뒤에 같이 한다.

---

## 9. 화면 쪽

### ★★★★ Strategy/Momentum 이 IRS 북으로 갈아탔다 (2026-09-21 오후) [OWNER]

> "원래있던 KTB 모멘텀을 대체하는거니까 거기서 제공하던 정보들도 들어가야해.
>  슬리브라는 표현보다는 그냥 Strategy/Momentum 이 더 잘 맞겠다."

**세입자가 넷에서 셋으로 줄었다.** 09-08 등록(선물·합성)을 비추던 거울 면은 **완전히
은퇴**했고(오너 지시), 그 자리를 09-15 동결·채점 중인 IRS 50/50 이 받는다. 한 화면에
모멘텀이 둘이면 「어느 모멘텀이 우리 것인가」를 매일 되묻게 된다 — 실제로 그 혼동이 났다.

    src/sleeve/{api.ts,SleevePage.tsx}  →  src/momentum/{api.ts,MomentumPage.tsx}
    ?g=strategy&s=momentum-sleeve       →  ?g=strategy&s=momentum
    src/ui/nav.ts::RETIRED_STRATEGY     은퇴한 주소를 새 면으로 넘긴다(링크가 안 죽는다)

- **KTB 면이 주던 것을 그대로 들고 왔다** — ①룩백 다섯 열의 신호 격자(테너 × 20/40/60/
  120/250일 + 합성·속도합의·유지일) ②테마 넷 부호 북 ③표본내 성적 표. 신호 격자와 성적
  표는 **카드 밖 · 페이지 폭**에 세운다(카드에 넣으면 룩백 다섯 열이 접혀 격자가 격자로
  안 읽힌다 — MR 계획면에서 스트립을 페이지 폭으로 내린 것과 같은 판단).
- ★**원화 열은 짝이 있어야 읽힌다.** 낙폭이 그랬듯 **Ulcer 도 「위험맞춤 Ulcer」 열을
  옆에 세운다**(`sleeve.py::build_perf` → `ulcerVolMatched`). 실측이 그 이유다 —
  거시 다리는 원화 Ulcer 584만으로 추세 860만보다 «나아» 보이는데, 위험을 맞추면
  1,303만으로 뒤집힌다. 마우스를 올려야 보이는 값은 없는 값이라 뜻풀이가 아니라 열이다.
- ★★**페이로드 모양이 바뀌었으니 `cache.SCHEMA_VERSION` 을 19→20 으로 올렸다.** 안 올리면
  v19 캐시가 그 칸 없는 표를 계속 내주고 화면은 **에러도 없이 빈 칸**을 그린다(v13·v16·
  v19 에서 이미 세 번 밟은 함정). 가드가 이 수를 잰다(`guards/mr-book.test.ts`).
- ★★**브라우저 실측이 잡은 레이아웃 결함** — 1,920×855 에서 **주문표 칸이 높이 0 으로
  접혀 「오늘 칠 것」이 통째로 안 보였다.** 바닥 띠는 `fixed` 가 아니라 기둥의 마지막
  칸이라(`ui/BottomStrip.tsx` 머리), 내용이 기둥보다 길면 늘어난 만큼 **자라려는 칸이
  0 으로 눌린다**. 조건 바·히어로는 위에 남기고 **그 아래를 구르는 칸**으로 감쌌다
  (`overflowY:'auto'` + `minHeight={360}`). 스크린샷만 봤으면 못 봤다 — 접힌 칸은
  «없는 것»처럼 보이지 결함으로 안 보인다.
- ★산문은 **재는 폭**이 따로 있다 — 각주가 1,920px 한 줄이면 눈이 다음 줄 머리를 못
  찾는다. 이 면의 주문표 카드와 같은 `maxWidth={760}` 으로 접는다(말줄임은 안 한다).
- **화면에서 「슬리브」라는 낱말을 내렸다** — 다만 `registry.book` 의 「IRS 50/50 슬리브」는
  **그대로 둔다**. 그건 동결된 판정문(`docs/PREREG_sleeve_5050_2026-09-15.md`)이 스스로를
  부르는 이름이고, 화면이 다른 낱말을 쓰면 등록서와 화면이 갈린다.
- 가드 아홉이 빨개졌고 **명제는 살리고 주어만 옮겼다**(`momentum-canon` ×5 ·
  `momentum-prereg-lock` ×2 · `critique-repairs` ×1 · `nav-strategy-tenants` ×1).
  잠금 명제는 `book.lock.note` 에서 **`perf.window`** 로 다시 가리킨다 — 서버가
  `mo.FREEZE` 에서 끊은 창을 화면이 그대로 적는 자리다.

### ★★★★ 화면에서 «배분» 을 걷었다 (2026-09-22) [OWNER]

> "이 문장은 상관없는게 MR이랑 별개로 돌릴거고 자본배분은 나중에 포트폴리오
>  탭에서의 역할이야"

09-21 판은 조건 바 밑에 **「평균회귀를 안 줄이면 여력이 0억이라 오늘 이 북은 못
서요」**를 붉게 세우고 있었다(§5-1). 내렸다. 그리고 같은 전제를 지고 있던 것들을
전부 같이 걷었다 — 오너 판정은 **「화면에서 전부 걷는다」**였다:

    히어로       · 배율 1.000          → 내림
    주문표       「축소 후」 열         → 내림 (「목표 액면」은 그냥 「액면」)
    산출 근거    「단독 기준」 칸       → 「크기」로 합침
    산출 근거    「배율」 칸           → 내림 (W4 배율·평균회귀 증거금·여력·상한 넘은 날)
    조건 바      연동 실패 안내 문장    → 내림
    산출 근거    여력 기준일·적재 지연 각주 둘 → 내림

**근거는 취향이 아니라 역할이다.** 저 넷은 「이 북이 얼마인가」가 아니라 「이 북에
자본을 얼마나 줄 것인가」의 수였고, 그 물음의 주인은 Portfolio 다. 한 화면에 겹쳐
두니 이 면이 매일 「오늘 못 선다」를 1순위로 외쳤는데 그건 이 북의 사실이 아니었다.

- ★**서버는 안 고쳤다.** `build_sheet` 가 이미 `standalone`(배율 1.0 판)을 **다리까지**
  내고 있어서(`_legs(..., 1.0, prev)`), 화면이 읽는 자리를 `sheet.legs` →
  `sheet.standalone.legs` 로 옮기는 것으로 끝났다. 「주문」도 그래서 축소 **전** 액면의
  차이가 된다. `scale`·`headroom`·`mrMargin` 은 페이로드에 그대로 있다 — **쓰는 화면이
  따로 생길 것**이라 지우지 않았다.
- ⚠ 오늘 배율이 1.000 이라 **화면의 수는 안 변했다**(포지션 규모 67억 · 회전 29억).
  갈리는 것은 배율이 1 이 아닌 날이고, 그날 이 면은 축소 **전** 수를 적는다.

### ★★ 얼라인 — 페이지 폭 블록도 카드다 (2026-09-22, 브라우저 실측)

[OWNER — "지금 디자인 얼라인이 안 맞아서"]. 1,920×855 에서 DOM 을 재니 결함은 하나였다:

    제목의 왼쪽    Momentum 24  주문표 41  산출 근거 817  추세 신호 **24**  거시 24  성과 24

카드 안 제목은 카드가 진 `paddingX={2}` 만큼 들어가는데(41), 페이지 폭 블록 셋만
맨몸이라 기둥에 그대로 붙어서(24) **내려 읽으면 제목이 24 → 41 → 24 로 튀었다.**

기준은 옆 세입자다 — MR 계획면의 청산·손절 패널이 **페이지 폭인데 `.sr-card`** 이고
제목이 41 에 선다. 그래서 규칙은 이렇게 읽힌다:

    제목이 있는 페이지 폭 블록  →  `.sr-card`  (제목 +17 · 표 +1)
    제목이 없는 사실 스트립     →  `.sr-stats` (제 헤어라인이 경계를 준다)

`PanelCard` 를 이 파일 안에 두고(`ui/` 로 안 올렸다 — 쓰는 곳이 하나다) ExitPanel 의
골격을 그대로 옮겼다. 고친 뒤 제목 전부 41. ★`h3` 였던 것도 `h2` 로 고쳤다 — 이
블록들은 위 두 카드의 **하위가 아니라 형제**인데 `h3` 가 종속을 말하고 있었다.

- ⚠ **표가 넓게 퍼지는 것은 결함이 아니다.** 추세 신호 표의 자연폭은 492px 인데 카드는
  1,857 이다. MR 의 페이지 폭 카드도 746 대 1,855 라 **그게 이 리포의 캐논**이다.
  재 보기 전에는 이것도 고칠 것으로 보였다.
- ⚠ 남은 것: 거시 신호 카드는 내용 자연폭이 315px 이라 1,857 카드에서 **17%만 찬다**.
  레인 정본이 페이지 폭을 요구한 것은 ①신호 격자와 ③성적 표뿐이라, ②테마 부호 북은
  옆에 붙여도 된다. **안 옮겼다** — 오너 판정 없이 정보 배치를 바꾸지 않는다.

### 슬리브가 화면에 올라왔다 (2026-09-21 오전) [OWNER — "모멘텀도 좀 고쳐두자"]

**굴리는 쪽에 화면이 없었다.** 아침 주문표도 배율·여력도 전부 스크립트와 마크다운뿐이라
매일 손으로 돌려야 보였다. Strategy 세입자로 올렸다(그날 오후에 위 항목으로 자리와 이름이 바뀐다).

    backend/app/sleeve.py          레인 함수를 **불러 모으기만** 한다(§10-8·§10-10)
    /api/momentum/sleeve           배경 빌드 + 디스크 캐시 + 화면 폴링 (한 번 38~44초)
    src/momentum/{api.ts,MomentumPage.tsx}   (그날 오후 이 자리로 옮겼다)
    backend/tests/test_sleeve.py   25건

- **세입자를 가른 이유** = 수를 섞으면 어느 등록의 성적인지 못 읽는다. 조건 바 첫 칸이
  어느 등록인지 적고, 각주가 옆 면과 다른 북임을 적는다.
- **화면이 제일 크게 말하는 것은 §5-1 이다** — 「평균회귀를 실제로 안 줄이면 여력 0억,
  오늘 슬리브는 못 선다」가 조건 바 밑에 붉게 선다. 그 아래 액면·배율은 전부 k_mr 축소를
  **가정**한 수라고 같은 줄이 적는다.
- **원장은 안 건드렸다** — 채점 창이 열렸는데(09-16~) 채점 행이 0 인 것은 결함이 아니라
  §4-2(슬리브를 켤 것인가) 대기다. 화면이 「슬리브를 켤지 정한 뒤에 적어요」로 적는다.
- ⚠ 자료는 있다 — `mkt_irs_close` 09-18 까지 2,648행(2026-09-21 SQL 확인). 원장이 빈 것은
  자료 공백이 아니라 **아무도 안 돌린 것**이다. `sleeve_daily` 에는 `--date` 가 없어서
  소급하려면 그 손잡이부터 필요하다.
- ★★**실측으로 잡은 배관 결함 둘**(시험이 없어서 브라우저에서 드러났다):
  ① `cached(name, hash, …)` 의 **첫 인자가 그대로 파일명**인데 거기에 키를 넣어
  윈도에서 `[Errno 22]`(`:`·`|`)로 죽었다 — 그런데 라우트는 200 이라 화면은 영원히
  「굽는 중」이었다. ② 그 실패가 어디에도 안 드러났다 → `_sleeve_error` 를 응답에 싣는다.
  **조용한 실패는 이 리포가 반복해서 밟는 그 결함이다.**
- ★캐시 키에 **읽는 파일의 자취**(집행표 CSV·원장 mtime)를 넣었다 — 자료 키만 보면
  스크립트를 다시 돌려도 화면이 옛 표를 낸다.

⚠ **화면 감사에서 믿을 것은 스크린샷이 아니라 DOM 이다**(눈으로 올린 다섯 중 셋이 오탐).
⚠ `.env.development.local` 이 `:8299` 를 가리키는데 배포 백엔드는 `:8200` 이다. **개발 서버는
뜰 때 한 번만 그 파일을 읽는다** — 파일을 고친 뒤 재기동을 안 하면 화면이 계속 `:8200` 을
두드리고 새 라우트에서 「FAILED TO FETCH」가 난다(2026-09-21 실측). 기동 로그의
`Environments: .env.development.local` 줄이 그 확인이다.
⚠ 시험 전체는 `--ignore=tests/test_static_agreement.py` 로 돌린다(포트 8200 PID 쪽지에서
수집 단계에 터진다 · 레인과 무관). 그렇게 돌리면 **1,267 passed · 7 skipped · 1 xfailed** (10분 55초).
⚠ 백엔드는 **뜰 때 한 번만 SQL** 을 읽는다(07:00 `SauronV2Refresh`). `fetch_legs.py` 가 그
백엔드를 두드리므로, 방금 들어온 봉이 필요하면 그쪽 asof 를 먼저 확인한다.

---

## 9½. 테너 조합 탐색 — **고르지 말라**가 답이다 (2026-09-21) [OWNER]

> "꼭 3Y 10Y 뿐만아니라, 최적 조합을 1~10Y 사이에서 찾을 수 있으면 찾아볼래?
>  스왑으로 모멘텀 플레이를 건너오면서 이런 방식이 가능해졌어."

정본 `backend/output/MOMENTUM_TENOR_SEARCH_2026-09-21.md` · 스크립트
`backend/scripts/momentum_tenor_search.py`. **등록은 안 건드렸다**(09-15 동결·
09-16 채점 중). 231조합 × 1~3다리 + 전 테너 등가중 대조군, 위험은 북 변동성
목표로 **구조가 맞춘다**.

### 답 — 폭이 이기되 「AQR 의 폭」이 아니다

| | 다리 | N_eff | SR | CDaR 비 | 순위 |
|---|---|---|---|---|---|
| 등록 3Y+10Y | 2 | 1.04 | 1.000 | 1.140 | 88/231 |
| 최고 칸 **3Y** | 1 | 1.00 | 1.118 | **1.332** | 1 |
| **전 테너 등가중** | 11 | 1.07 | 1.060 | 1.210 | **31/232** |

1. **고르지 않는 규칙이 등록 칸을 이긴다.** 대조군이 고른 231칸 중 201개를
   이기면서 **자유도를 하나도 안 쓴다**.
2. **다리를 늘리면 중앙값이 오르고 천장은 안 오른다**(중앙 0.903→0.950→1.115,
   최고 1.332→1.312→1.309). 분산 투자의 전형적 자국이다.
3. **그 폭은 독립 베팅이 아니다** — ρ̄ 0.933, 가장 먼 쌍조차 1Y–10Y **0.749**,
   N_eff 가 둘에서 열하나로 가도 1.04 → 1.07. §10-2 의 「N=2」는 IRS 로 넘어와도
   **그대로 사는데, 이유가 「상품이 둘」이 아니라 「인자가 하나」**였다. 좋아지는
   폭도 작고(1.140→1.210) 꼬리사건 7·8건 위라 **그 차이만으로는 유의하지 않다**.
4. **고르는 것은 안 선다** — 1등 3Y(1.332)이지만 상위 열 칸이 1.271~1.332 에
   몰려 있고, 그 한 칸은 231칸에서 표본내로 고른 것이다. 신호를 지는 것은 **짧은
   쪽**이다(상위 20 중 3Y 16회·2Y 11회·18M 7회). ⚠그 분포가 **순위자와 함께
   뒤집혔다** — 고장난 순위자에선 1Y 가 19회였고 지금은 **1회**다. 「어느 테너가
   신호를 진다」도 순위자에 딸린 진술이라는 뜻이다.

### ★★★★ 이 측정의 제일 큰 교훈 — **순위자가 답을 정했다**

처음 판은 레인 기본 순위자(`vol_normalize` 위의 CDaR 비)로 쟀고 **1Y 단독이
1위·등록 칸이 132위**였다. 그 순위를 **버렸다**. 무너지는 방식이 조용하다:

- `vol_normalize` 는 확장창(`min_obs=60`)이라 배수가 **첫해에만 튄다** — 이 북은
  이미 북수준 변동성 목표를 걸어 2018 이후 배수가 1.01~1.07 로 평평한데 **2017 만
  11~15배**다. 그 한 해가 모든 칸의 낙폭 꼬리(최악 5% = 116봉)를 차지한다.
- 그래서 분모가 칸마다 같아지고 비율이 **분자만 따라간다**:
  **Spearman(정규화 CDaR 비, SR) = +0.964.** 낙폭 순위자인 줄 알았던 것이
  **샤프의 다른 이름**이었다.
- 등록 칸이 **첫해를 버리는 것만으로 0.194 → 1.246(6.4배)** 움직인다. 버림 길이가
  또 자유도라 그 길로는 못 닫는다.
- 그 순위자가 1위로 올린 1Y 단독은 **원계열 MDD/연변동성이 3.24**(등록 칸 1.03)다
  — 진짜 낙폭 순위자였다면 1위가 될 수 없는 칸이다.

**정규화를 뺀 근거는 이 레인에만 있다**: 칸마다 북 변동성이 `TARGET_VOL` 로 이미
맞춰져 있고(실측 연변동성 11.0~11.7M) CDaR 비는 `r → c·r` 에 불변이다.
⚠ **다른 레인에 그대로 옮기지 말 것** — 거긴 북이 안 맞춰져 있을 수 있다.
⚠ `evaluation/metrics.py` 는 **안 고쳤다**(§10-8 — 남의 레인 코드다).

### 못 하는 말

    ✘ 「이 조합이 최고다」 — 순위자를 바꾸면 1등이 바뀐다(Calmar 1등과 SR 1등이
      Spearman +0.091 로 무관하다). 표본내이고 **위약(순환이동)도 안 쟀다**.
    ✘ 「등록 칸이 몇 위다」 단독 인용 — 순위는 순위자에 딸려 있다.
    ✘ 등록 변경 — 09-15 동결·09-16 채점 중이다.

### 움직이려면 — 0번이 새로 생겼다

**0. 순위자를 먼저 못 박는다.** 무엇으로 재는지를 안 정하면 표를 보고 순위자를
고르게 되고, 그건 조합을 고르는 것보다 자유도가 크다. 그 다음이 조합인데, 가장
방어하기 쉬운 문안은 **「전 테너 등가중」**이다 — 자유도 0 인데 31/232 위이고,
**고를 것이 없다는 것 자체가 사전등록의 내용**이 된다. 나머지는 위약·시행수 N
재계산·집행 배관(`sleeve_execution.LEG_T` 가 3Y·10Y 로 박혀 있다)·비용 가정
(편도 0.5bp 는 3Y·10Y 의 값이다).


## 10. 이 레인의 상시 규율

1. 국고 현물 공매도를 하지 않는다[오너]. 선물·IRS 가 양방향이 되는 자리다.
2. 브레드스 N=2. 방향성 상품이 둘뿐이라 **Sharpe 상한이 0.6대**다(AQR 의 1.1 은 N=30 이다).
   ★★★★**2026-09-21 에 이 규율의 뜻이 바뀌었다** — IRS 열한 테너로 직접 재 보니
   규율은 살았는데 **이유가 달랐다**(§9½). 상한을 만든 것은 「상품이 둘뿐」이 아니라
   **인자가 하나뿐**이다: ρ̄ 0.933, 가장 먼 쌍조차 1Y–10Y **0.749**, 열한 다리를 다
   담아도 N_eff **1.07**. 그래서 **폭에 두 종류가 있고 스왑은 싼 쪽만 사 줬다** —
   한 인자 위의 점들을 평균해 잡음을 깎는 폭(공짜다·**중앙값**을 올린다)은 얻었지만
   독립적인 베팅은 안 얻었다(**천장**은 그대로 닫혀 있다).
   ⇒ **따름정리**: 아웃라이트 테너를 더 늘리는 어떤 설계도 이 상한을 못 연다.
   여는 것은 **다른 인자**뿐이다(커브·플라이·타 자산군 — 여기서 안 쟀다).
3. 비용: 선물 편도 0.5틱 + 롤 왕복 1틱 · IRS 편도 0.5bp[오너]. 낮추지 않는다.
4. 위험을 맞춘 뒤에 비교한다(Man 2016 Figure 7 규약).
5. 룩백·격자를 매년 고르지 않는다 — 전진 선택은 IRS 9년 −1,562만 · ktb-tsmom 7년 −3,230만.
6. 판정문 id 가 겹치면 **조용히 덮어쓴다**. 태그를 붙인다(`-books-paper` 등).
7. **푸시는 오너 지시가 있어야 한다.**
8. **남의 레인 코드를 여기서 고치지 않는다** — 그쪽 모듈을 불러 쓰고, 필요하면 그쪽에 짓는다.
9. **옮긴 것과 새 규칙은 다르다.** 옮겼다면 **비트 동일 재현**을 시험으로 못 박는다. 재현이
   안 되면 그건 새 규칙이고 시행수를 하나 더 쓴다.
10. **같은 축은 두 표에서 같은 함수를 지난다.** 두 스크립트가 같은 양을 다르게 세면 결함이다.
11. ★**물려받은 진단을 그대로 쓰지 않는다.** 「자료가 멈췄다」는 09-16 인계문의 문장이었고
    원천을 직접 두드려 보니 틀렸다. 남이 적어 둔 원인은 **재현하고 나서** 믿는다.
12. ★**재계산되는 수는 등록 상수가 아니다.** 등록했으면 값을 못 박고, 재계산판은 **대조로**
    옆에 찍는다. 갈리는 순간이 곧 옆 레인이 이 레인을 움직인 순간이다.
13. ★**순위자를 먼저 못 박는다.** 무엇으로 재는지를 안 정하고 표를 보면, 표를 보고
    순위자를 고르게 된다 — 조합을 고르는 것보다 **자유도가 크다**(§9½: 같은 231칸
    위에서 정규화 CDaR 1등은 1Y, Calmar 1등은 다른 칸, 둘의 Spearman +0.091).
    그리고 **지표가 무너지는 방식은 조용하다** — 확장창 정규화가 이 북 위에서
    샤프로 축퇴했고(Spearman +0.964) 아무 오류도 안 났다. 지표를 쓰기 전에 그
    지표가 **무엇과 상관되는지** 재 본다.
14. ★**위험 규약을 증거금 회계에 쓰지 않는다.** k_mr 은 위험 축의 수인데 증거금 여력의
    분모에 걸려 **장부에만 있는 여력**을 만들어 냈다. 두 축은 단위가 다르다.

---

## 11. 다음에 열 만한 것

1. **배분 층 설계**(§4-4). §1 의 북극성이 가리키는 자리. 코드보다 **목적함수 하나**가 먼저다.
2. **k_mr 갈래를 닫는다**(§4-1). 오너가 꺼낼 때까지 먼저 묻지 않는다.
3. **성장 축 새 사전등록**(§4-3) — 성장 축을 수출에서 실질 GDP 빈티지로 바꾸는 **한 칸만** 묻는다.
   지연 2분기라 「구할 수 있다」이지 「같다」가 아니라는 것도 적는다.
4. **무상관 원천 하나 더**(§1). +27.2% 가 나온 자리다. 4.4% 를 다투는 것보다 크다.
5. Consensus Economics 이력 견적 — **3 의 판정이 난 뒤에** 뜻이 있다. 둘은 랜덤워크 가정 아래
   같은 양이라, 대체 정의로 안 서면 전망치로도 안 설 가능성이 크다.
6. Bloomberg 보유 여부 확인 — 있으면 ECFC 이력이 견적 없이 열린다.
7. 선물 187만원 차이 확인(롤 비용 가설).
8. 09-08 등록의 표본밖 판정이 나면 W6 연동 — H1 NO-GO 또는 G4 실패 시 슬리브 등록서를 다시 연다.

---

## 12. 상태

    커밋      `6c53d68c`(09-16 작업) · `89c6828e`(09-17 작업) · 그 위에 이 인계문 · 작업트리 깨끗
    미푸시    **49커밋** · 푸시는 오너 지시 대기 [§10-7]
    시험      1,267 passed · 7 skipped · 1 xfailed
    원장      마지막 행 2026-09-08(연습). **09-16·09-17 채점일에 행 없음** — 소급 가능(§3)
    자료      IRS 09-16 · BSS 여섯 09-16 · BSS 6M·9M·1.5Y 09-11(imx 3영업일 지연)
