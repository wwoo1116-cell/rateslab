# 다음 세션 프롬프트 — 매크로 모멘텀 레인 (2026-09-16 마감)

> 덧붙이지 말고 **한 블록으로 다시 쓴다**. 이 파일은 09-15 에 세 번 덧붙여져 400줄이 됐고
> 그래서 그날 갈아엎었다. 09-16 도 갈아엎어서 이 판이다. 다음 세션도 그렇게 한다.

---

## 0. 한 문장

**슬리브는 09-15 에 동결됐고 채점 창이 09-16 에 열렸다. 집행 배관은 ①~⑨ 가 다 섰다.**
09-16 에 둘을 했다 — W4 여력의 분모를 그 레인의 배분기에 걸었고(⑨), 성장 축의 「못 고치는
것」을 철회했다. **남은 것은 오너 결정 둘과 자료 재적재 하나다.**

---

## 1. ★먼저 볼 것 — 자료가 09-08 에서 멈춰 있다

    IRS 계열      2,389봉 · 마지막 **2026-09-08**      (`mkt_irs_close` · 적재 지연 잦음)
    MR 다리 파일  `krw-crs/work/leg_*.json` · **2026-09-07**
    그중 셋       6M · 9M · 1.5Y 는 **2026-08-24** 에 얼어 있다 (그 레인 `PREREG_03` §4-14)

**채점 창은 열렸는데 채점할 봉이 없다.** 아침 한 줄(`sleeve_daily`)을 돌리기 전에 이것부터
확인한다. 지금 돌리면 09-08 기준으로 「연습」이 한 줄 더 쌓일 뿐이다.

★ 주문표는 이제 **여력의 기준일이 주문표의 기준일과 다르면 경고를 찍는다**(09-16 추가).
지금이 그 경우다 — 여력은 09-07, 주문표는 09-08 이다.

---

## 2. 지금 서 있는 것 — 건드리지 말 것

### 동결 둘. 이름이 같아서 헷갈린다.

| | **IRS 슬리브** (09-15 동결) | **매크로 신호** (09-08 동결) |
|---|---|---|
| 정본 | `docs/PREREG_sleeve_5050_2026-09-15.md` | `data/krw-macro-vintage/PREREG.md` |
| 계기 | IRS 3Y·10Y | 국채선물 |
| 거시 다리 | **테마별 부호 북 넷** 등가중 | 네 테마 부호 **평균** 하나 |
| 위험선호 | **확장 평균 대비** | 0 대비 |
| 채점 | **2026-09-16~** · 첫 판정 2027-09 무렵 | 2026-09-09~ · 750봉 또는 전환 12회 |
| 화면 | 없음(집행 스크립트만) | `/momentum` 이 이것을 비춘다 |

⚠ **두 등록 다 「판정일까지 들여다보지 말 것」이다.** 09-08 쪽은 코드도 고치지 않는다.

### 등록 수 (슬리브 · 재동결판) — 안 움직였다

    판정문  Momentum-{trend,macro,blend}-IRS-books-paper
            50/50  SR 1.000 · DSR 0.9955 · PBO 0.1207 · 위약 0.0105/0.0105 · MinTRL 3.75년
            거시   SR 0.925 · DSR 0.9942 · PBO 0.1713 · 위약 0.0105/0.0105 · MinTRL 4.03년
            추세   SR 0.847 · DSR 0.9828 · PBO 0.2117 · 위약 0.0105/0.0158 · MinTRL 5.73년
    결합    w 0.25 · ΔCalmar +0.318 [+0.019, +0.719] **P 0.048** · ΔMDD −0.261 P 0.005
            창 = IRS 달력 ∩ MR 달력 **1,646봉** · k_mr 0.954 · k_tr 0.318 · 총위험 배수 11.89
    ⚠ 결합 기준에서 추세 단독(+0.307 P 0.048)과 **같은 자리**다. 이긴다고 적지 않는다.

---

## 3. 09-16 에 한 것 둘

### ① ⑨ W4 여력의 분모를 배분기에 걸었다 — `docs/RESULT_margin_source_2026-09-16.md`

W4 의 **규칙은 안 고쳤다.** 고친 것은 「평균회귀가 그날 쓴 증거금」이 **가리키는 자리**다.
09-15 까지 슬리브는 그 수를 자기 쪽에서 다시 시뮬레이션했고, 그 재시뮬은

    · 규칙 **A**(균등 로트)였다 — 그 레인이 등록한 것은 규칙 **B**(효율가중 · `PREREG_03` §3.3)
    · `.fillna(0)` 이 **적재 멈춘 다리의 증거금을 조용히 풀고 있었다**(9M 이 지금도 18.64억을 묶고 있다)

★ **§0.1 의 `ext.get(day, 0.0)` 와 같은 결함이 다른 레인에 한 벌 더 있었다.**

    같은 날(2026-09-07)        묶임      여력      배율   원한(1.75억) 대비
    재시뮬 · 규칙 A            50.00억   52.31억   1.000      29.9배
    배분기 · 등록 규약(B)      81.36억   22.40억   1.000      12.8배
    배분기 · 라이브 규약(B) ←   100.00억    4.63억   1.000       2.6배
    〃 · k_mr 축소를 안 하면   100.00억    0.00억  **0.000**    0배

경로 전체로는 거의 안 움직인다 — **발동일 28일 → 24일**(1,646봉), 최저 배율 0.775 로 같다.
**바뀐 것은 성과가 아니라 오늘의 방석이다.** 원한 최대가 10.08억이라 그런 날엔 배율 0.46 이 된다.

    data/krw-crs/src/margin_headroom.py     신설 — 그 레인이 내는 「오늘 묶인 증거금」
    scripts/sleeve_monitor.py               `mr_margin_path()` · `--margin-source {allocator,resim}`
    scripts/sleeve_daily.py                 주문표에 출처·기준일 · 기준일 어긋나면 경고
    scripts/sleeve_execution.py             같은 축을 두 표가 **같은 함수로** 지나게 한 줄 추가
    tests/test_margin_source.py             시험 여섯

★ **재현으로 못 박았다** — 새 배분기를 「등록 규약 · 아홉 고정 정규화」로 돌리면 `alloc_grid`
규칙 B 와 **비트 동일**이다(최대 차이 0.00원 · 다른 날 0일). 옮긴 것이지 새 규칙이 아니다.
`margin_budget_book`·`alloc_grid` 는 **안 고쳤다**(동결 등록서가 물고 있다).

### ② 성장 축 — 「못 고치는 것」을 철회했다 — `docs/RESULT_growth_axis_sources_2026-09-16.md`

논문 부록 B 의 **다음 문장**이 전망치 없을 때의 대체 정의를 준다.

> Prior to 1990, I use one-year changes in **realized** YoY real GDP growth and CPI inflation,
> **lagged one quarter** (equivalent to changes in forecasts assuming random walks). Both from the **OECD**.

그 계열이 이 레인이 이미 두드리는 OECD 빈티지 흐름 안에 있다 — **`B1GQ_Q` 한국 에디션
197개(201005~202609) · 분기 2010-Q1~2026-Q2**. **09-08 적재가 `.M.` 만 물어서 안 보였다**
(GDP 는 분기라 `.Q.`). 빠뜨린 것이지 없던 것이 아니다.

    krw-macro-vintage/src/pull_oecd_gdp.py  신설
      → out/oecd_gdp_vintage_long.csv · out/gdp_pit_quarterly.csv (170행 × B1GQ_Q·P6_Q·B1GQ_D)
      동결 파일(`oecd_vintage_long.csv`·`macro_pit_monthly.csv`)은 **안 건드렸다**

⚠ **성과는 하나도 안 쟀다.** 재는 순간 새 등록의 자유도를 태운다.
⚠ 자료지연 **중앙 2분기**(지금 서 있는 EX 는 1~2개월) — 논문이 1990 이후 전망치로 갈아탄 이유다.
⚠ 「한 분기 더 지연」은 진짜 빈티지와 **이중 계산**이라 `dg_1y` 와 `dg_1y_lag1q` 를 둘 다 냈다.
  고르는 것은 새 사전등록이 **이유와 함께** 할 일이다.

★ 전망치 조달은 여전히 견적 문제다 — Consensus Economics(논문의 그 출처 · 현행호 US$629/년 ·
**이력은 가격 비공개**) · Bloomberg ECFC(**데스크 보유 여부 미확인**) · KCIF 월별 IB 8곳 평균은
모양이 가장 가까운데 **`robots.txt` 가 `User-Agent: *` 에 `Disallow: /`**(kasp.krx 와 같은 등급).
09-04 카탈로그 §10-5 「컨센서스 없다(확정)」은 **이벤트 서프라이즈** 물음이라 다른 물음이었다.

---

## 4. 집행 배관 — `docs/EXEC_sleeve_2026-09-15.md` 가 상태판 (①~⑨ 완료)

    아침    python -m scripts.sleeve_daily        주문표(목표 아닌 **어제와의 차이**)
    분기    python -m scripts.sleeve_monitor --quarterly   관문 셋 + 상관 경보
    연 1회  python -m scripts.sleeve_monitor --annual      W2 상향 조건
    대조    python -m scripts.sleeve_monitor --daily --margin-source resim   09-15 까지 쓰던 판

    sleeve_execution.py   실측 pv01 · 집행표      sleeve_prereg_5050.py --structure books  등록 수
    momentum_irs_books.py 판정문 셋               sleeve_allocation[_heatmap].py           배분 그림
    margin_headroom.py    (krw-crs) 평균회귀가 오늘 묶은 증거금

09-08 기준 집행값: 3Y 페이 27.5억 · 10Y 페이 10.9억 · 원한 증거금 1.92억 · 배율 1.000 ·
관문 셋 통과 · 상관 +0.089(최장 초과 0봉) · W2 는 ①② 미충족으로 w 0.25 유지.

---

## 5. ★[OWNER] 지금 걸려 있는 결정 셋

1. **k_mr 축소를 MR 레인에 지시할 것인가** (⑨ 에서 올라온 것 · 이게 제일 크다).
   총위험 고정은 평균회귀 장부를 **0.954배로 줄여 세운다**는 뜻인데 그 지시를 한 적이 없다.
   등록서의 「평균회귀 쪽은 안 건드린다」와 k_mr 을 여력에 적용하는 것이 **같은 문장에서 충돌한다**.
   · 줄인다   → 그 레인 로트 50억 → 47.7억. **그 레인 사전등록 사안이다.**
   · 안 줄인다 → 오늘 여력 0.00억. **슬리브가 오늘 못 선다.** W4 가 그렇게 말한다.
2. **슬리브를 실제로 켤 것인가.** 09-15 부터 계속 열려 있는 물음이다. 배관은 다 섰다.
3. **성장 축 새 사전등록을 열 것인가.** 자료는 손에 있고 비용은 0 이다(§3-②).
   ⚠ 여는 순간 **표본내 측정이 시작된다** — 열기 전에 §6 의 자유도 소비 목록을 등록서에 옮긴다.

부수: 09-16 변경은 **커밋 안 했다**(작업트리 9파일). 미푸시 45커밋. 푸시는 오너 지시 대기.

---

## 6. 표본내에서 본 것 — «본 뒤에 안 켠 것»으로 기록

새 등록을 열 때 이 목록을 자유도 소비로 같이 적어야 한다.

    docs/RESULT_theme_books_2026-09-15.md   구조 여섯 (합성/북넷 × 부호/연속 + 논문 정의판 둘)
    docs/RESULT_bok_books_2026-09-15.md     한은 입력 일곱 (금년/내년/평균 축)
    docs/RESULT_cycle_bok_2026-09-14.md     경기순환 한은 교체 · §7 왜 나쁜가 · §8 안 잰 둘

★ 한은 입력은 **논문 구조에서도** OECD 를 못 넘는다(0.923 대 0.932). 한은 GDP 전망 북은
금년 축에서 −0.04 로 죽어 있고 물가 북만 산다(0.877).
★ 국제교역 북은 어느 판에서도 음(−0.27~−0.40)이다. 논문과 정의가 **정확히 같은 유일한 테마**가
유일하게 음수다 — 그 테마의 경제 논리가 원화에서 안 선다는 뜻일 수 있다.
★ 안 켠 것: 한은 전망치 · 내년 축 · 성장/물가 북 분리 · 확장 평균의 장기 이력 · 두 원천 혼합 ·
  **그리고 09-16 의 GDP 빈티지(안 쟀으므로 아직 자유도를 안 썼다 — 이 상태를 지킬 것).**

---

## 7. 계보 — 「왜 과거 결과와 다른가」

정본 `docs/LINEAGE_momentum_numbers_2026-09-15.md`. 표제수 변화 17건을 출처와 함께 귀속했고
**«결과를 보고 고른 것»은 없다**. 교훈은 「관문이 문턱에 붙어 있어서(위약 0.0211 대 0.05 ·
결합 P 0.048 대 0.05) **장부 정리 수준의 변경이 판정을 뒤집는다**」이다.

**미확인 하나만 남았다** — 선물 9,852/771 대 10,039/958. 순손익 9,081 이 같고 전·비용이
정확히 187만원씩 갈려 한쪽이 롤 비용을 뺀 것으로 보이나 확인 전이다.
(09-11 IRS 손익 두 판은 **닫혔다**: 창 하나다.)

---

## 8. 코드 결함 — 같은 자리를 다시 밟지 말 것

    metrics.py 사유줄     비용전 판이 깨져도 비용후 p 만 찍었다 → 깨진 쪽을 적는다
    _RUNS 캐시 키         `external is not None` 만 봐서 원천 다른 북이 재사용됐다 → 내용 해시
    app.dv01.pv01         **애뉴이티(연)** 다. ₩/bp 로 쓰려면 ×명목×1e-4
    tests/conftest.py     시험이 raw CSV 열넷을 다시 썼다 → 세션 전체 `BIGFOOT_OFFLINE=1`
    발행 캘린더 시험       굴러가는 창 안의 날짜를 못 박아 낡았다 → 자료에서 사례를 찾는다
    ★margin_budget_book   `.fillna(0)` 이 「자료가 안 왔다」를 「포지션이 없다」로 읽는다 (09-16)
    ★pull_oecd            `.M.` 만 물어서 분기 계열(GDP)이 통째로 안 보였다 (09-16)

**안 고치고 기록만 하는 것 셋** — 고치면 동결된 표본이 바뀐다.

    mkt_irs_close 의 정체 행 30일(국내 휴장) · 손익 0 · 변동성 0.6% 차
    margin_budget_book 의 `.fillna(0)` — 고치면 슬리브의 MR 다리 수익률이 바뀐다
    sleeve_execution 의 「상한 초과일 28일」 — 등록서 §4.1 이 인용한 수라 재현되게 둔다
      (배분기 기준 24일을 **같은 함수로** 옆에 같이 찍는다)

---

## 9. 화면 쪽

가드 108파일 1,768건 · 빌드 · 린트 0건. `/momentum` 면은 **09-08 등록**(선물·합성)을 비추는
거울이고 슬리브와 **다른 북**이다 — 서버가 `registry` 로 이름을 낸다. 수치를 새 정의로 갈면
비추던 대상이 사라진다.

⚠ **화면 감사에서 믿을 것은 스크린샷이 아니라 DOM 이다**(눈으로 올린 다섯 중 셋이 오탐).
⚠ `.env.development.local` 이 `:8299` 를 가리키는데 백엔드는 `:8200` 이다 — 그대로 띄우면 전
화면이 오류 상태다. 돌고 있는 `:8200` 은 다른 세션 것이라 새 라우트를 안 물 수 있다.
⚠ 시험 전체를 돌리면 `tests/test_static_agreement.py` 가 **포트 8200 PID 쪽지**에서 수집 단계에
터진다(레인과 무관). `--ignore` 로 빼고 돌리면 1,262 passed · 7 skipped · 1 xfailed.

---

## 10. 이 레인의 상시 규율

1. 국고 현물 공매도를 하지 않는다[오너]. 선물·IRS 가 양방향이 되는 자리다.
2. 브레드스 N=2. 방향성 상품이 둘뿐이라 **Sharpe 상한이 0.6대**다(AQR 의 1.1 은 N=30 이다).
3. 비용: 선물 편도 0.5틱 + 롤 왕복 1틱 · IRS 편도 0.5bp[오너]. 낮추지 않는다.
4. 위험을 맞춘 뒤에 비교한다(Man 2016 Figure 7 규약).
5. 룩백·격자를 매년 고르지 않는다 — 전진 선택은 IRS 9년 −1,562만 · ktb-tsmom 7년 −3,230만.
6. 판정문 id 가 겹치면 **조용히 덮어쓴다**. 태그를 붙인다(`-books-paper` 등).
7. **푸시는 오너 지시가 있어야 한다.**
8. ★**남의 레인 코드를 여기서 고치지 않는다** — 그쪽 모듈을 불러 쓰고, 필요하면 그쪽에 짓는다.
9. ★**옮긴 것과 새 규칙은 다르다.** 옮겼다면 **비트 동일 재현**을 시험으로 못 박는다. 재현이
   안 되면 그건 새 규칙이고 시행수를 하나 더 쓴다.
10. ★**같은 축은 두 표에서 같은 함수를 지난다.** 두 스크립트가 같은 양을 다르게 세면 결함이다.

---

## 11. 다음에 열 만한 것

1. **성장 축 새 사전등록** — 자료는 §3-② 로 손에 있다. 성장 축을 수출에서 실질 GDP 빈티지로
   바꾸는 **한 칸만** 묻는다. 지연 2분기라 「구할 수 있다」이지 「같다」가 아니라는 것도 적는다.
2. **k_mr 갈래를 닫는다**(§5-1). 이게 닫혀야 슬리브를 켤 수 있다.
3. Consensus Economics 이력 견적 — **1 의 판정이 난 뒤에** 뜻이 있다. 둘은 랜덤워크 가정
   아래 같은 양이라, 대체 정의로 안 서면 전망치로도 안 설 가능성이 크다.
4. Bloomberg 보유 여부 확인 — 있으면 ECFC 이력이 견적 없이 열린다.
5. 선물 187만원 차이 확인(롤 비용 가설).
6. 09-08 등록의 표본밖 판정이 나면 W6 연동 — H1 NO-GO 또는 G4 실패 시 슬리브 등록서를 다시 연다.
