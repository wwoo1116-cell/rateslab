# 다음 세션 프롬프트 — 2026-09-10 인계 (전략 중립 평가층 · 계기 판정)

아래 블록을 통째로 붙여 넣으면 이어받을 수 있다.
세부는 `docs\EVAL_LANE_STATE.md`(§0~§15) 한 장에 다 있다 — 그 문서가 정본이다.

---

## 붙여 넣을 블록

```
2026-09-10 세션을 이어받는다. 원화 금리 데스크, sauron-v2 리포다.
09-09 에 아홉 커밋(~d2029628), 09-10 에 계기 판정 두 커밋이 더 얹혔다.
★09-10 분은 **로컬에만 있다** — 푸시는 오너 지시가 있어야 한다(푸시가 곧 배포다).

【오너가 닫은 결정 — 다시 묻지 말 것】
· 평가 «단위» = **다리 하나**. 통합 장부로 재면 고르는 행위가 없어져 전부 통과한다.
· 자본 기준은 레인마다 다르고 그게 맞다 — MR 은 **액면(원금)**, 모멘텀은 **분모를
  안 만든다**(무차원 비율로 비교), 증거금 상한판은 **100억**(가정이 아니라 제약).
· 손절σ 는 **탐색한 축**이다(N ×3). 화면 기본값은 2.5.
· ★**순위 기준 = 변동성 정규화 CDaR 비.** 2026-09-09 「ㄱ 으로 간다」로 닫혔다.
  **Lo 보정 Sharpe 가 3~5배 좁다는 것을 알고도** 그렇게 정한 것이다 — 다음 세션이
  「좁은 쪽이 낫잖아」로 갈아끼우면 개선이 아니라 **결정 위반**이다.
  `test_the_ranking_basis_is_the_cdar_ratio_and_that_is_a_decision` 이 지킨다.
  화면 자동채택도 같은 `cdarRatio` 이고 `guards/mr-optimize.test.ts` 가 그쪽을 잰다 —
  **둘은 한 자여야 한다.** 다시 여는 조건 = 낙폭 사건이 문헌 눈금(15개)에 가까워질 때.
  그때는 시험을 고치지 말고 **오너에게 다시 물을 것.**
· 게이트에 걸린 것은 **사양을 안 바꾸고 «적기만»** 한다(실패 유형 · 사건 수 · 구간).
· **섞기는 판단 보류** — 반쪽마다 답이 갈리고 구간까지 겹친다.

【이 세션에서 지은 것 — 전략 중립 평가층】
  backend/evaluation/metrics.py    DSR · MinTRL · PBO(CSCV) · 변동성정규화 · CDaR ·
                                   Lo(2002) · 실패유형 · 낙폭 사건수 · 블록 부트스트랩
  backend/evaluation/report.py     한 장짜리 판정문(게이트가 맨 위 · 시각 안 적음)
  backend/evaluation/reports/*.md  판정문 열넷 — MR 아홉 · Momentum 셋 · CRS 둘
  backend/scripts/mr_evaluate.py         MR 다리를 태우는 자리
  backend/scripts/momentum_evaluate.py   모멘텀 세 다리 — 같은 자리
  backend/scripts/crs_evaluate.py        **증거금 상한판** — 세 번째 소비자
  backend/scripts/lane_costs.py          두 레인의 비용·회전율을 한 자로
  backend/tests/test_{evaluation,momentum_evaluate,crs_evaluate}.py
  docs/EVAL_LANE_STATE.md          ← **정본**

    python -m scripts.mr_evaluate --all              MR 아홉
    python -m scripts.momentum_evaluate              모멘텀 세 다리
    python -m scripts.momentum_evaluate --trials-sweep   N 민감도
    python -m scripts.crs_evaluate                   증거금 상한판
    python -m scripts.crs_evaluate --vs              ★두 레인 «수준» 비교
    python -m scripts.crs_evaluate --mix             섞는 비율·창
    python -m scripts.lane_costs                     비용·회전율

MR 화면도 같이 갔다 — 히어로·트리거 문장·IRS 커브 여덟/플라이 넷·자동 최적화
(노브 다섯 제거)·통합 장부 회계·CDaR 채택. Momentum 세입자는 **상륙시켰다**
(그 레인이 지은 것을 오너 지시로 내가 올렸다) + 히어로 한 벌.

【★★★ 이 세션의 최대 발견 — 절반이 «내 주장이 뒤집힌 것»이다】
1. ★★★**순위 지표가 사건 둘 위에 서 있다.** CDaR 꼬리 관측 80점이 나온 낙폭
   사건이 MR·모멘텀 모두 **둘**(상한판 5). 극단지수 θ ≈ 0.025 라 종속을 무시하면
   **표준오차를 40배 과소평가**한다(Ferro & Segers 2003). POT 실무 눈금은 독립
   초과 30~50, 15 미만 불안정 — 우리는 2~5개다.
2. ★★★**그래서 이 표본에서는 셋을 못 가른다.** 순환 블록 부트스트랩(63봉·씨앗
   고정) 90% 구간: CDaR 비 MR 1.68 [0.33, 4.11] · 모멘텀 0.44 [0.06, 1.15] ·
   상한판 3.26 [0.56, 3.32]. **MR 의 구간이 나머지 둘의 점추정을 통째로 담는다.**
   내가 적었던 「MR 이 CDaR 비 4.9배 크다」는 **구간을 안 보고 한 말**이었다.
   Lo 보정 SR 은 3~5배 좁다(2.1~8.3배) — Van Hemert 외(2020)의 「낙폭 통계는
   관측을 낭비한다」가 이 장부에서 폭 배수로 나온 것이다.
3. ★★**증거금 상한이 MR 의 절반을 먹는다.** AR 보정 Sharpe 1.42 → **1.006**.
   「SR 1.74」로 견주던 것은 **없는 판의 수**였다. 상한판은 DSR 0.9978 통과 ·
   PBO 0.5455 미통과인데, 그 PBO 는 「칸이 닮아 못 고른다」이지 과적합이 아니다
   (칸 상관 0.90 · 제일 나쁜 칸도 SR 1.07).
4. ★★**「CDaR 비에 자기상관 보정이 없다」고 적었는데 정반대였다.** 같은 일별 SR 로
   φ 만 흔들면 CDaR 비가 0.601 → 0.224(φ 0→0.45), 같은 구간 Lo SR 은 0.961 →
   0.591 — **CDaR 비가 더 세게 벌한다**(실현 경로에서 재니까).
5. ★★**「이 층은 스케일 불변」도 거짓이었다.** `vol_normalize` 워밍업이
   `fillna(1.0)` 이라 원(₩) 계열에서 CDaR 비의 **부호까지** 갈렸다(+7.27 대 −0.15).
   워밍업은 「안 건드린 구간」이 아니라 **「못 잰 구간」(NaN)** 이다.
6. ★★**오너 규율의 전제가 뒤집혔다.** 「MR 회전이 추세의 5~20배」 → 실제로는
   **CTA 가 4.9배 더 돌고**(27.86 대 5.66회/년) MR 이 단가 3~8배 비싸다.
   ⚠ 처음 낸 30.98회는 **등록된 북이 아니었다**(엔진 기본 인자).
7. ★**급히 세운 3칸 격자가 PBO 를 낮춰 보이게 했다** — 그 레인의 진짜 축으로
   세우니 0.51~0.66 → 0.60~0.71 로 **올라갔다**.
8. ★**BSS-1Y 의 PBO 0.8691 을 「가장 나쁜 과적합」으로 읽었는데 아니었다** — 칸
   상관 0.89 에 모든 칸 SR 0.94~1.03. 진짜 과적합 서명은 6M·10Y 둘뿐이다.

판정(2026-09-09): 열셋 중 **BSS-2Y 하나만 통과**(DSR 0.9966·PBO 0.1603·CDaR 1.683).
**편도 0.5 → 1.0bp 면 그것도 죽는다.** 모멘텀 셋은 막는 것이 PBO 가 아니라 **DSR**
이고, ★**섞으면 과적합이 준다**(추세 PBO 0.68 → 50/50 0.39 · MinTRL 15.0 → 10.7년,
표본 9.40년).

【규약·함정 — 다시 밟지 말 것】
· ★**SCHEMA_VERSION 을 올릴 것**(오늘 17→18→19). **화면이 안 쓰는 칸일수록 위험**
  하다 — 화면 칸이면 줄이 사라져 눈에 보이는데, 남의 레인이 읽는 칸은 그쪽이
  **조용히 못 재는** 상태로만 남는다. `guards/mr-book.test.ts` 가 승급을 잰다.
· ★**`barCost` 부호 규약이 두 엔진에서 반대다**(MR 음수 · CTA 양수). 교차 비교에서
  조용히 틀린다 — 한 번 10.5%를 13.3%로 냈다. `/api/mr/book` 봉의 `cost` 는
  **지불액(양수)** 으로 실어 그 함정을 없앴다.
· ★**표본을 라우트와 같게 자를 것**(`_mr_reconcilable`). 안 자르면 화면이 안 보여
  주는 6년이 섞여 SR 연 −0.03 대 +0.50 으로 판정이 통째로 갈린다.
· ★**남의 레인 북을 다시 세우면 카드 등식을 시험으로 못 박을 것** — 그쪽이 구조를
  바꾸면 조용히 딴 것을 재는 대신 시험이 깨진다(`test_momentum_evaluate`).
  동결일 뒤를 자르는 것도 그 자리의 책임이다(엔진은 오늘까지 그린다).
· 게이트에 떨어지면 **CDaR 비를 아예 안 낸다**(사양). 표의 «—» 는 결함이 아니다.
· **JSX 주석 자리** — 삼항식 `{x ? ( 여기 } · 속성 사이 둘 다 문법 오류다.
  오늘 그걸로 빌드를 **두 번** 깼다. 주석은 요소 **위**(자식 자리)에 둔다.
· **`next build` 가 `.next` 를 덮어** 개발 서버가 CSS 없이 뜬다 — `rm -rf .next`
  후 재기동. 개발 백엔드는 `.env.development.local` 이 가리키는 **:8299** 다.
· **curl 로 배포를 폴링하지 말 것** — 오늘 20초×14분으로 Vercel 봇 보호(403
  Security Checkpoint)를 걸었다. 그리고 **지연 로드 세입자는 루트 HTML 에 문자열이
  없다** — 확인은 청크를 grep 하거나 브라우저로 연다.
· UI 는 `cds-code` 를 **먼저** 부른다. 해요체 · 말줄임 금지 · 32px 등고 ·
  낱말 중간 줄바꿈 금지 · 캐논에 있으면 임포트(새로 만들지 말 것).

【2026-09-10 에 더한 것 — 계기를 게이트에 태웠다(정본 §15)】
  backend/scripts/momentum_irs_evaluate.py    IRS·선물 추세 다리를 같은 자리에
  backend/tests/test_momentum_irs_evaluate.py 10 통과 (레인 범위 258 통과)
  evaluation/reports/Momentum-trend-{IRS,FUT-on-IRS-window,IRS-full}.md

    python -m scripts.momentum_irs_evaluate              판정 둘 + 짝지은 차이
    python -m scripts.momentum_irs_evaluate --diff-only  차이만
    python -m scripts.momentum_irs_evaluate --costs      비용 민감도
    python -m scripts.momentum_irs_evaluate --full       IRS 가 가진 한 해를 더

· **계기가 축이 되면 N 이 두 배다**(36 → 72). 그 값이 실제로 문다 — 같은 선물 북의
  DSR 이 0.9034 → **0.8954** 로 내려간다. 세지 않으면 계기를 갈아타는 것만으로
  게이트가 헐거워진다.
· 판정(공통 창 2,370봉) — **둘 다 미통과, 걸리는 자리가 다르다.**
      IRS  편도 0.5bp[O]  DSR 0.9830 통과 · PBO 0.2335 미통과 · MinTRL  5.66년
      선물 편도 0.5틱      DSR 0.8954 미통과 · PBO 0.6786 미통과 · MinTRL 16.13년
  선물 PBO 는 폐기 눈금(0.50)을 넘고, IRS 는 문턱까지 **0.0335** 남았다.
· ★★★**주변 구간 둘을 겹쳐 보는 것은 «차이의 검정»이 아니다.** 두 북 상관 +0.892 —
  각자의 폭에 든 「어떤 해였나」는 차이에서 지워진다. 블록을 **한 번만** 뽑아 둘에
  같이 먹이면(`paired_ci`): Lo 보정 SR 차이 **+0.312 [+0.006, +0.660]** P 0.048 로
  **방향이 식별된다.** CDaR 비는 점추정 −0.302 인데 **뽑기 중앙이 +0.251** 로 부호가
  넘어간다(P 0.095) — 못 가른다.
· ★★그 부호 뒤집힘의 뿌리: IRS 의 순위값이 **낙폭 사건 «하나»** 위에 서 있다
  (1/28 · 선물 2/49). §13 의 「사건 둘」보다 얇다. **그래도 순위 기준은 안 바꾼다** —
  다시 여는 자리는 오너다.
· ★비용을 올리면 **두 게이트가 반대로 움직인다**(IRS 0.25→1.0bp: DSR 0.9925→0.8915 ·
  PBO 0.3239→**0.0688**). 비용이 칸을 «가르기» 때문이다. 어느 줄에서도 둘이 같이
  통과하지 않는다. ⚠ 통과하는 비용을 찾으러 격자를 더 돌지 **않았다** — 비용은
  고르는 축이 아니라 정해진 값이다[OWNER].
· ★표본을 한 해 더 줘도(2,640봉) PBO 는 **오히려 오른다**(0.2335 → 0.2587). 「칸이
  닮아 못 고른다」는 표본 길이가 아니라 **구조**다 — 아래 미결 2 가 이 다리엔 안 듣는다.
· ★★**인계문이 적었던 「Sharpe 0.63 → 0.94」는 그 표에 없는 수였다.** 0.63 은 IRS 의
  **Calmar** 이고 0.94 에 해당하는 줄이 없다. 실제로는 **0.53 → 0.85**(Lo 보정
  0.600 → 0.912). 카드에서 수를 옮길 때 «어느 줄인지»를 안 적으면 이렇게 된다.
· ★★IRS 는 **비용을 더 물고도** 이긴다 — 회전은 더 적은데(0.1002 대 0.1125) 문
  비용이 3.6배(18.9% 대 9.5%)다. 이기는 이유는 **비용 전 손익이 1.82배**인 것이고,
  그래서 원인이 비용이 아니다. 「고시가 뭉갠 것 아니냐」도 **닫혔다**(IRS 일 Δ AR(1)
  −0.006/−0.041 · Δ=0 인 날 4.8%/3.7%). 남은 후보는 롤 없음 · CTD 안 바뀜 둘이다.

【미결】
1. **왜 IRS 의 비용 전 손익이 1.8배인가** ← 다음에 열면 첫 번째. 후보 둘(롤 없음 ·
   CTD 가 안 바뀜)을 가르는 자리는 선물 쪽이다(조정가·CTD 는 그 레인의 것).
   그 전에는 「계기를 갈아타자」고 말할 수 없다.
2. **표본이 1.3년 더 쌓이면 모멘텀 50/50 재측정**(MinTRL 10.74 대 9.40).
   ⚠ IRS 추세 다리에는 안 듣는다(위 참조).
   지금 미통과의 상당 부분은 「표본이 짧다」이지 「전략이 나쁘다」가 아니다.
3. **`krw-crs` 가 `PREREG_03` 을 동결하면** 그 규약(배분 규칙 다섯까지 칸)으로
   상한판을 한 번 더. 지금 칸은 로트 여섯뿐이다.
4. **아침 굽기에 CSV 복사를 물릴 것** —
   `data\krw-macro-vintage\out\macro_themes_daily.csv` → `backend\data\`.
   (모멘텀 레인 미결 6. 아직 안 됐다.)
5. 섞기 판단 보류 · CTA 레인이 자기 N 을 확정하면 재측정.

【상시 제약】
· **`git add -A` 금지. 경로 지정 스테이징만.** 다른 세션이 같은 리포를 만진다.
  `backend/data/raw/bigfoot_*.csv` 는 남의 것이다. `*.premove.bak` 도 안 건드린다.
· 푸시는 `git push origin HEAD:main`(로컬 master, 상류 main). **오너 지시 없이
  푸시하지 말 것** — 푸시가 곧 Vercel 배포다.
· **:8200 은 Funnel 로 공개된 라이브다.** 새 필드를 올리려면 재기동해야 하는데
  스케줄 작업 Stop/Start 로는 안 죽는다 — 포트 8200 PID 를 `Stop-Process` 한 뒤
  `Start-ScheduledTask SauronV2Backend`.
· **브라우저는 계산하지 않는다**(§16). 1순위·순위·집계는 서버가 낸다.
· 게이트는 범위에 맞출 것 — 백엔드 전체 스위트는 DB·네트워크 의존 시험 때문에
  10분 넘게 안 끝난다. 이 레인 범위는
  `pytest tests/ -k "eval or momentum or crs or mr" --ignore=tests/test_static_agreement.py`
  (258 통과). ⚠ `--ignore` 가 필요하다 — `test_static_agreement` 가 **수집 단계에서**
  터져(쪽지 PID 대 :8200 불일치) 스위트 전체가 안 돈다.
```

---

## 오늘 나간 여덟 커밋

```
48cf1a5d  Momentum 에도 히어로 — 「지금 무엇을 볼까」의 답을 표 앞에
caa6bff9  Momentum 세입자를 올린다 — 그리고 눈이 잡은 캐논 결함 다섯
f0d48875  순위 기준은 CDaR 비다 — 알고도 그렇게 정했다
bdc71c3c  문헌에 숫자가 있었다 — 그리고 그 숫자가 우리 순위 기준을 겨눈다
3f8670c6  문헌이 겨눈 약점은 내가 짚은 데가 아니라 우리 순위 지표의 두께였다
67839631  「미통과」 한 낱말이 서로 다른 세 사정을 덮고 있었다
47f98c2c  증거금 상한판이 서면서 «누가 크냐» 를 처음으로 물을 수 있다
815a4499  모멘텀도 같은 자에 올린다 — 그리고 CTA 는 기다릴 필요가 없었다
```

## 참고문헌 — 이 층이 인용하는 것

Bailey & López de Prado (2014) DSR · Bailey 외(2017) PBO/CSCV ·
Chekhlov 외(2005) CDaR · Lo(2002) 자기상관 보정 연환산 ·
Bailey & López de Prado (2015) 자기상관이 하방을 깊게 한다(φ=0 가정 시 67~71%
과소평가) · Van Hemert·Ganz·Harvey·Rattray 외(2020) 낙폭 통계는 관측을 낭비한다 ·
Ferro & Segers (2003) 극단지수·디클러스터링 · Politis & Romano (1992) 순환 블록
부트스트랩 · Burghardt 외(2003) 최대낙폭은 트랙 길이에 좌우된다
