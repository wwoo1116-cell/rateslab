# 전략 중립 평가층 레인 — 인계 (2026-09-09)

## ★ 이 문서는 **Phase 0 시점**의 기록이다 — 그 뒤로 Phase 1~3 이 집행됐다

오너가 2026-09-09 오후에 열린 질문 둘을 닫고 이 레인을 넘겼다:

  · **평가 단위 = 다리 하나** [OWNER] (§6-① 닫힘)
  · **자본 기준 = 액면(원금) · 거래마다 진입일 커브** [OWNER] (§7-1 의 미결)
  · **손절σ 는 탐색한 축** [OWNER] (§6-② 닫힘 → N 이 3배)
  · 화면 기본 손절도 **2.5** 로 옮겼다(별건 · 커밋 `e0b20c36`)

집행 결과는 `docs/EVAL_LANE_STATE.md` 가 진다 — **거기가 지금 상태이고, 이 문서는
그 앞의 진단 기록**이다. 아래 판정표·실측(§3)은 그대로 유효하다.

관련: `docs/EVAL_LANE_STATE.md` · `backend/evaluation/` · `backend/scripts/mr_evaluate.py`
· `docs/MR_LANE_STATE.md` · `backend/app/mrmetrics.py`

---

# 프롬프트 (여기부터 복사)

## 0. 이 레인이 답하는 물음

지금 성과 카드(Sortino·Calmar·Martin·Ulcer·GPR·Omega·PF)는 **평균회귀에 유리하게
기울어 있다**. 승률이 높고 왜도가 음수인 전략을 구조적으로 위로 올린다. 나중에
모멘텀·복합 장부를 **같은 자**로 재려면 그 자가 먼저 있어야 한다.

오너 원 지시: 「전략 무관 평가층을 만들어라. 단 **진단 먼저, 수리는 그 다음**이다.」

## 1. 첫 3분 — 환경

- 리포 `Projects\apps\sauron-v2`. 프런트 `pnpm dev` :3200 · 내 백엔드 :8299.
- **`:8200` 은 Funnel 로 공개된 라이브다 — 안 건드린다.** 읽기 전용 조회는 된다:
  `curl "http://127.0.0.1:8200/api/mr/strategy?id=BSS-10Y"`
- ⚠ **재기동**: `Stop-ScheduledTask` 로는 python 이 안 죽는다.
  `Get-NetTCPConnection -LocalPort 8200` → PID → `Stop-Process -Force` → `Start-ScheduledTask`.
- ⚠ **디스크 캐시가 보드를 잡는다**: 라우트를 고쳐도 `backend/app/cache.py::SCHEMA_VERSION`
  을 안 올리면 새 필드가 안 나온다(현재 v17).
- ⚠ `.env.development.local`(gitignore)이 `127.0.0.1:8299` 를 가리킨다. 실제 백엔드가
  8200 이면 dev 화면이 FAILED TO FETCH 다.

## 2. 오너가 못 박은 규율 — 원 지시 보존

- **진단 먼저, 수리는 그 다음.** Phase 0 을 보고하고 **오너가 답하기 전에는 지표
  코드를 한 줄도 안 쓴다.**
- **찾은 것을 조용히 고치지 않는다.** 보고하고 멈추고 기다린다.
- 필요한 입력이 없으면 **없다고 말한다.** 대체값·기본값을 쓸 거면 **ASSUMPTIONS 절**에
  가정으로 적는다.
- **numpy/pandas/scipy 외 새 의존성 금지** (쓰려면 먼저 묻는다).
- **Profit Factor·Omega 는 새 층에서 아예 안 낸다.** 둘 다 승률의 준-단조변환이라
  구조적으로 MR 을 편든다.
- **정규화 안 한 수익률로 전략을 비교하지 않는다.**
- **전략마다 다른 비용 가정을 쓰지 않는다.** MR 회전율이 추세추종의 5~20배라
  2bp 대 5bp 가정 하나가 순위를 뒤집는다.
- **사전(ex-ante) 스케일링에 전체표본 통계 금지.**

## 3. Phase 0 실측 — 완료 (2026-09-09)

### 3-1. 대상 식별

오너가 준 수상한 숫자 일곱은 **`/api/mr/strategy?id=BSS-10Y` · span `all` · 라우트
기본 노브**다. 25계열을 전수 대조해 찾았고 일곱이 모두 정확히 재현된다.

```
노브   lookback 60 · entryZ 2.0 · exitZ 0.5 · stopZ 3.5 · costBp 0.5(편도)
       notional 1,000,000 ₩/bp · carry on · entryMode level · regime none
       timeStop 0 · costModel flat · reverseExit false · countOpen false
표본   2020-01-02 ~ 2026-09-08 · 1,647봉 · 6.54년

Sortino 1.668 · Calmar 0.623 · Martin 3.886 · Ulcer 1,855,866
GPR 3.402 · Omega 1.561 · PF 10.480
totalPnl 47,139,025 · MDD 11,572,384 · winRate 0.8333 · Sharpe 0.904 · 18거래
```

**다음 세션은 이 일곱을 먼저 재현해 «같은 것을 보고 있음» 을 확인할 것.**

### 3-2. 판정표

| # | 항목 | 결과 | 판정 |
|---|---|---|---|
| D1a | 손절·강제청산 | z 발산 손절뿐(`mrbacktest.py:353`). 우선순위 stop>exit>reverse>time(`:361`,`:395`). 마진콜·강제청산·자본제약 없음 | DEFECT |
| D1b | 청산 사유 분포 | 목표청산 15/18 = **83.3%** · 손절 3/18 = **16.7%**. 목표청산 15승0패 avg +3.79bp 11.9봉 / 손절 0승3패 avg −1.81bp **1.0봉** | DEFECT |
| D1c | 표본 끝 미청산 | **1건.** 2026-08-14 진입 z=2.06 → 09-08 현재 z=2.28 · 16봉 · **평가손 −4,341,217** | DEFECT |
| D1d | 보유기간 상한 | `time_stop` 있으나 **기본 0 = 꺼짐**. 무한 보유. 최장 33봉·중앙 6봉. 증거금·레포 헤어컷 미계상 = **무한 자본 가정** | DEFECT |
| D2 | Ulcer 단위 | 백엔드는 **음수가 안 나온다**(`mrmetrics.py:223`). 마이너스는 화면이 붙인다(`src/mr/parts.tsx:139` `fmtKrw(-perf.ulcer)`). 단위가 **%가 아니라 원(₩)** — Ulcer Index 가 아니고 Martin(`:224`)이 그대로 물려받는다 | DEFECT ×2 |
| D3 | 시행 횟수 | **탐색을 했고 화면 기능이다**(`/api/mr/optimize` 162칸·순위 표시). 아래 §3-4 | DEFECT |
| D4 | 수익 계열 | **수익률이 아니라 원화 손익.** 일별 1,647봉. 비용 반영됨(편도 0.5bp·왕복 1.0bp). AR(1) **0.223** | DEFECT(원인 다름) |
| D5 | 꼬리 사건 | 낙폭 사건 19개 중 **MaxDD 50% 초과가 3개**(40% 초과 4 · 30% 초과 6) | OK, 단서 있음 |

### 3-3. D1 — PF 10.48 이 부풀려진 세 자리

1. **미청산 다리가 거래 통계에서 빠진다**(`main.py:2089` `countOpen=false`).
   총손익·MDD·일별 계열에는 이미 들어 있고 **승률·거래수·PF 에만 없다**.
   그 −434만원을 손실로 세면 **PF 10.48 → 5.82 · 승률 83.3% → 78.9%.**
2. **★진입에 |z| 상한이 없어 「손절선을 이미 넘긴 채로」 들어갈 수 있다.**
   손절 3건 중 둘이 **z=4.37 · 4.36 에서 진입**했다(stopZ 3.5). 손절 규칙이 진입을
   막지 못하고 **한 봉 뒤에 내보낼 뿐**이라, 손절 3건이 전부 정확히 1.0봉이다.
3. **★손실 3건은 사실 사건 하나다.** 2024-08-28 / 08-30 / 09-03 연속 재진입
   (−2,184,692 / −2,698,803 / −547,099). 거래로 세면 「18건 중 3패」인데 경로로
   보면 **2024-08-28 ~ 2025-02-07 의 −675만원 낙폭 하나**다.

### 3-4. D3 — 정직한 N

```
lookback {20,60,120} × entryZ {1.5,2.0,2.5} × exitZ {0,0.5,1.0}
  × stopZ {2.5,3.0,3.5} × entryMode {level,touch}  =  162칸
계열 25 (+통합 장부) · 채점 구간 4 · 실전노브 96 · 자료출처 2
⚠ stopZ 프리셋이 2026-09-08 에 {3.0,3.5,4.0} → {2.5,3.0,3.5} 로 바뀌었다(합집합 4)
```

**★반대 증거 둘 (공정하게 같이 적는다)**

- 보고된 칸은 **자기 격자의 1등이 아니다**: Calmar 26등 · Sortino 19등 ·
  Martin 25등 · totalPnl 17등 / 162.
- 예전에 이미 CSCV PBO 1~11%, DSR 0.877(N=27)·0.523(N=118)·0.098(N=1062)을 쟀다.
  **⚠ 단 9/8 프리셋 교체로 격자 «구성» 이 바뀌어 그 수치는 옛 격자의 기록이다.**

### 3-5. D4 — 재는 자리의 성질

- **원화 손익이라 AUM 이 없다.** 단위 포지션 액면 1,221,298,458원(pv01 8.188).
  연 손익 721만 = 전체 액면 대비 0.59%/년. **변동성 정규화의 분모를 정해야 한다.**
- **비용**: 편도 0.5bp × 명목 = 집행 1회당 500,000원, 진입·청산 각각. 37회 집행 ·
  총 18,500,000원 = **비용 전 손익의 28.2%**. 손익분기 편도 **1.774bp**(가정의 3.5배).
  ★비용이 **집행 «횟수» 의 함수**이지 체결 규모·호가폭의 함수가 아니다.
- **회전율**: 연 2.75회 왕복 = 편도 5.66다리/년 = 단위 액면의 5.66배/년.
  **노출은 전 봉의 12.0%(197/1,647)뿐** → 실제 물린 자본 대비로는 **연 47.3배**.
- **AR(1) 0.223 은 문턱(0.2)을 넘지만 «가격 뭉갬» 이 원인이 아니다**:

```
일별 손익 전체        0.223
MTM 성분만            0.216
캐리+롤다운+조달      0.564
★원본 스프레드 변화   0.085   ← 출처는 깨끗하다
스프레드 변화(보유일)  0.162
```

  원인은 **무포지션 88%의 뭉침** + **보유 중 캐리의 결정론적 적립**이다.
  일별 지표가 부푼 것은 맞으나 **처방이 다르다** — 출처를 의심할 게 아니라
  보유일만 재거나 거래 단위로 재야 한다.

### 3-6. D5 — 낙폭 사건

```
1  11,572,384원 · 142일 · 2020-04-10 ~ 2020-11-06
2   7,325,352원 ·  56일 · 2026-06-19 ~ ★표본 끝까지 진행 중(미청산 다리가 여기 있다)
3   6,751,825원 · 106일 · 2024-08-28 ~ 2025-02-07
4   5,151,951원 ·  27일 · 2025-04-25 ~ 2025-06-09
```

3개면 단일관측은 면했으나 여전히 얇고, 1등은 코로나 창이며 2등은 미결이다.
Calmar 의 표준오차는 무한대는 아니지만 **매우 넓다** → CDaR 로 가는 판단은 선다.

### 3-7. 동반 진단 (참고)

왜도 **+3.506** · 초과첨도 **+40.67** · 꼬리비 1.770(전 봉)/1.327(보유일) ·
81개월 중 음수 7 · 평균이익 3,794,056 / 평균손실 1,810,198(페이오프 2.10) ·
최장 물속 142일.

## 4. 오너 답 (2026-09-09) — N 을 가르는 사실

> 「60일/2σ/0.5σ 는 딱히 데이터 보고 정한 건 아님」

→ **손잡이 3축이 N 에서 빠진다.** `MR_LANE_STATE.md` §③ 의 미결이 닫혔다.
   N 추정이 4,212 → **3~25** 로 내려간다.

## 5. ★★★ DSR 미리보기 — 매듭이 옮겨갔다

⚠ **고장난 지표 위에서 돈 참고용 계산이다**(Phase 1 수리 전 · 스크래치패드).
   판정으로 쓰지 말 것. 다만 **평가 «단위» 결정을 가르므로** 적어 둔다.

BSS-10Y 연 SR 0.904 · T 1,647 · 왜도 3.506 · 초과첨도 40.67

```
후보군               N      SR0(연)    DSR      판정
아무것도 안 고름     1      0.000     0.9943   PASS
BSS 9만기(상관감안)  3.3    0.333     0.9451   FAIL(아슬)
BSS 9만기            9      0.549     0.8398   FAIL
보드 25계열          25     1.100     0.2924   FAIL
MinTRL(N=1) 2.77년 대 보유 6.54년
```

**★손잡이 탐색은 더 이상 문제가 아니다. 문제는 «어느 만기를 보고 있나» 로 옮겨갔다.**

**★그런데 BSS-10Y 는 9만기 중 5등이다**(1.5Y 1.67 · 2Y 1.48 · 1Y 1.16 · 6M 0.97 ·
**10Y 0.90** · 3Y 0.85 · 5Y 0.75 · 9M 0.65 · 7Y 0.49). **제일 좋은 걸 고른 게 아니다.**

**★★★통합 장부로 재면 전부 통과한다:**

```
통합 장부(BSS 9만기 전부) 연 SR 1.760 · 왜도 4.43 · 초과첨도 68.5
  DSR  N=1 1.0000 · N=3.3 1.0000 · N=9 0.9999 · N=25 0.9991   전부 PASS
  MinTRL 0.64년 대 보유 6.54년 (10배 여유)
  totalPnl 595,201,441 · MDD 51,075,350 · 145거래 · 승률 0.8483
  ⚠ 미청산 3건 −9,615,012 이 승률·PF 에서 빠져 있다(한 다리의 그 문제가 여기도)
```

**해석**: 아홉을 다 가져가면 «고르는» 행위가 없어져 깎을 것이 없고, 합치면 위험이
상쇄돼 SR 0.90 → 1.76 이 된다. **한 다리만 떼어 채점한 것이 애초에 단위 오류일 수 있다.**

## 6. ★열린 질문 둘 — 다음 세션이 먼저 물을 것

**① 평가 단위가 「만기 한 다리」인가 「통합 장부」인가.**
   답에 따라 결론이 정반대다. 통합 장부면 지금도 이미 게이트를 통과하고, 한 다리면
   「왜 10Y 였나」가 N 을 정한다. **Phase 2 를 짜기 전에 반드시 닫아야 한다.**

**② 손절 3.5σ 의 출처.**
   오너 답은 60일/2σ/0.5σ 셋에 대한 것이었다. `mr_live_report.py::LEGACY` 에도
   **그 셋뿐이고 손절이 없다.** 손절 프리셋은 9/8 에 바뀐 기록도 있다.
   3.5 가 PMS 원본이면 N 에 안 들어가고, 아니면 3배가 된다.

## 7. Phase 1 — 승인 대기 중인 수리 셋

오너가 승인한 것만, **각각 따로 검토 가능한 변경으로.** 나머지 동작은 보존.

1. **Ulcer 를 「전고점 대비 백분율 낙폭의 RMS」로 재정의.** 무단위·비음수.
   ⚠ **자본 기준(분모)을 오너가 정해야 한다** — AUM 이 없다(§3-5).
2. **비용 포함 계열 위에서 전 지표를 다시 잰다.** 비용 모델은 **회전율의 함수**로
   명시·파라미터화(평평한 haircut 금지).
3. **`strategy_id` · `cost_bp_roundtrip` 인자 추가** — MR·모멘텀·복합 장부에
   **같은 비용 모델**이 똑같이 걸리게. ★소비자가 이미 둘이다(§11-3 CTA 레인).

## 8. Phase 2 — 평가층 사양 (원 지시)

모듈 `evaluation/` · 진입점 하나:

```python
evaluate(returns: pd.Series,          # 주기별, 비용 차감 후
         trials: int,                 # D3 의 정직한 N
         is_oos_splits: int = 16,     # CSCV 의 S
         target_vol: float = 0.05,    # 사전(ex-ante) 연환산
         freq: str = "D") -> dict
```

### 지표 1 — Deflated Sharpe (게이트)

Bailey & López de Prado. **왜도·첨도·표본길이·시행수를 동시에** 보정하는 유일한
후보라 MR 과 모멘텀이 비교 가능해진다.

- 관측 SR·왜도·초과첨도·T → SR0(가우시안 최대값 근사 + 오일러-마스케로니 항) → DSR.
- **MinTRL** 도 같이 낸다. 실제 T 와 견주고 부족분을 적는다.
- **PASS: DSR > 0.95.**

### 지표 2 — PBO via CSCV (게이트)

- 수익 행렬(T × n_configs)을 **S=16 개 연속 블록**으로. C(S, S/2) 분할마다 IS 최적
  칸을 뽑아 OOS 순위의 로짓을 잰다.
- PBO = IS 1등이 OOS 중앙값 아래로 가는 분할의 비율.
- IS-OOS 성능 열화 기울기 · OOS 손실확률도 같이 낸다.
- **PASS: PBO < 0.20. PBO ≥ 0.50 이면 전략 폐기.**
- **대체 규칙**: 파라미터 탐색이 없었으면 **꼬리비(|95분위|/|5분위|)** 로 갈아 끼우고
  지표 3 을 2번 자리로 올린다. 꼬리비는 **서술이지 게이트가 아니다.**
  → **이 레인에서는 탐색이 있었으므로 CSCV 를 쓴다.**

### 지표 3 — 변동성 정규화 CDaR 비 (순위)

- **사전 변동성 추정**(확장창 또는 롤링 — 어느 쪽인지 명시)으로 `target_vol` 에 맞춘다.
  **전표본 실현변동성 금지(룩어헤드).**
- CDaR = 낙폭 관측치 **최악 5%의 평균**. 비율 = 연환산 수익 / CDaR.
- MaxDD 가 단일관측 추정량이라 Calmar 를 대신한다. **기준선 ≥ 1.0.**

### 게이트 논리

지표 1·2 는 **AND 게이트**. 하나라도 떨어지면 지표 3 을 **순위값으로 내지 않는다** —
`null` + 실패 사유. **게이트를 통과한 전략만 순위가 매겨진다.**

### 동반 진단 (내되 순위에 안 쓴다)

왜도 · 초과첨도 · 꼬리비(95/5) · 최장 낙폭 지속일 · 승률 · 평균이익/평균손실 ·
연환산 회전율. **셋과 뚜렷이 구분해 배치.** MR 과 모멘텀의 DSR 이 비슷하게 나올 때
서로 다른 실패 방식이 보이라고 두는 것이다.

## 9. Phase 3 — 출력 계약

```
{
  "gate": {"dsr": float, "dsr_pass": bool,
           "min_trl_years": float, "actual_years": float,
           "pbo": float, "pbo_pass": bool,
           "overall_pass": bool},
  "ranking": {"cdar_ratio": float|None, "cdar": float,
              "ann_return_normalized": float, "reason_if_null": str|None},
  "diagnostics": {...},
  "inputs": {"trials": int, "cost_bp": float, "target_vol": float,
             "vol_estimator": str, "n_obs": int, "ar1": float},
  "assumptions": [str]
}
```

전략마다 한 장짜리 마크다운을 `evaluation/reports/{strategy_id}.md` 에 쓴다.
**게이트 판정이 맨 위.**

## 10. 시험

- DSR: IID 정규 · 알려진 SR · N=1 → 고전적 SR t-검정 p값을 근사 복원.
- DSR: 수익 고정하고 `trials` 를 늘리면 **단조 감소**.
- PBO: 일부러 과적합시킨 합성(순수 잡음 200칸) → **0.5 에 근접**.
- CDaR: 큰 낙폭 하나 대 같은 MaxDD 의 중간 낙폭 다섯 → **다섯 쪽이 더 나쁜(큰) CDaR**.
- 변동성 정규화: 실현 변동성이 `target_vol` 허용오차 안.

## 11. ★함정 — 이번에 찾은 것

1. **★★★격자와 카드가 다른 자다.** 162칸은 **엔진 근사** 회계(`real:false`)로,
   상단 카드는 **실가격** 회계(`real:true`)로 채점한다. 같은 설정이 격자에서
   Calmar 0.576 · 카드에서 0.623 이다. **PBO 는 설정 행렬을 먹으므로 이걸 먼저
   정리해야 한다** — 안 그러면 순위 매긴 자와 보고한 자가 다르다.
2. **★★예전 PBO·DSR 수치는 죽었다.** 9/8 손절 프리셋 교체로 격자 구성이 바뀌었다.
   다시 재려면 `scripts/mr_grid_oos.py`·`scripts/mr_pbo.py`(둘 다 프리셋을 읽는다).
   ⚠ `scripts/mr_live_wfo.py` 의 `STOP_Z = 3.5` 는 프리셋이 아니라 그 스크립트 상수다.
3. **★★CTA 레인이 이미 같은 지표를 쓴다** — `backend/scripts/cta_macro.py:39` 가
   `mrmetrics` 를 임포트해 `score()` 로 채점한다(`:123`, `:274`, 원 단위 Ulcer 포함).
   **오너가 예고한 편향이 미래가 아니라 지금 두 레인에서 살아 있다.** 새 층의 두 번째
   소비자가 이미 있다는 뜻이고, §7-3 의 인자 설계는 이쪽을 같이 보고 정해야 한다.
   ⚠ CTA 레인의 자체 비용 가정·회전율은 **안 쟀다** — 오너가 지목한 「2bp 대 5bp 가
   순위를 뒤집는다」가 정확히 그 자리다. 가정하지 말고 **재고** 들어갈 것.
4. **미청산 다리는 두 곳 다에 있다** — 한 다리 −4,341,217 · 통합 장부 3건 −9,615,012.
   총손익·MDD 에는 있고 승률·PF 에는 없다.
5. **`Ulcer`·`Martin`·`Calmar` 는 전부 원/원이다.** 문헌값과 크기 비교 금지
   (`mrmetrics.py` 머리의 그 규율). 새 층은 이 제약을 깨야 하는데, 그러려면 §7-1 의
   자본 기준이 먼저 정해져야 한다.

## 12. 재현 명령

```bash
curl -s "http://127.0.0.1:8200/api/mr/strategy?id=BSS-10Y" > s.json   # 일곱 숫자
curl -s "http://127.0.0.1:8200/api/mr/book"                > b.json   # 통합 장부
curl -s "http://127.0.0.1:8200/api/mr/optimize?id=BSS-10Y" > o.json   # 162칸 격자
```

`spans` 에서 `span=="all"` 인 칸이 이 문서의 수다. 격자는 `cells[].current==true`.

# (여기까지 복사)
