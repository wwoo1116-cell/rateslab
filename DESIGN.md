# sauron-v2 — 디자인 규칙

이 문서는 **취향이 아니라 실측을 적는다.** 각 규칙 뒤에는 그 규칙을 만든 측정이 붙어
있고, 측정이 없는 항목은 규칙이 아니라 미정이다. 되돌리려면 새 측정을 가져올 것.

측정 환경은 이 데스크(Windows 11, DPR 1)와 이 리포의 실행 중인 앱이다.

## 출처 표시 규칙

**측정은 누가 했는지가 값의 일부다.** 이 문서의 모든 수치에는 아래 표시가 붙는다.

| 표시 | 뜻 |
|---|---|
| `[검증됨]` | 이 리포 / 이 패키지 / 실행 중인 앱에서 직접 재고 재현 가능하다 |
| `[미검증·전달]` | 앞 세션 인계 문서에서 온 값이고, **이 문서를 쓴 시점에 재확인하지 않았다** |

`[미검증·전달]` 은 틀렸다는 뜻이 아니라 **아직 아무도 다시 재지 않았다**는 뜻이다.
규칙의 근거로 인용하기 전에 재보는 편이 낫고, 특히 외부 제품 값은 제품이 바뀌면
조용히 낡는다.

---

## 1. 활자

### 1.1 굵기는 두 단계 — **400 본문 / 600 강조** [OWNER 2026-08-14 확정]

> **2026-08-14 확정.** 오너가 **400** 으로 결정했다. 즉 CDS(=Coinbase) 값 그대로이고,
> 예외는 13px `legal` 하나뿐이다(아래 근거 3). 아래 본문은 500 을 고르던 시점의
> 논의 기록이고, 결론만 이 문단이 대체한다. **다시 묻지 말 것.**
>
> ```
> body 16 / label2 14 = 400 (CDS 기본)   ·   legal 13 = 500   ·   강조는 600
> ```
> 코드는 이미 그 상태다 — `sauronTheme.fontWeight` 에 `legal: '500'` 한 줄뿐이다.

세 단계는 쓰지 않는다.

**근거 1 (외부 제품) — `[검증됨]`, 2026-08-13 재측정.**

방법: 각 사이트를 열고 `document.fonts.ready` 이후 `TreeWalker` 로 **보이는 텍스트
노드 전부**를 훑어 `(font-size, font-weight)` 조합을 세었다. 폰트는 이름이 아니라
`@font-face` 의 `src` 로 자체호스팅 여부를 확인했다. 행 높이는 실제 행 컨테이너의
`getBoundingClientRect()` 와 연속 행의 top 간격(pitch)을 같이 쟀다.

| | 폰트 | 자체호스팅 | 본문 | 표본 |
|---|---|---|---|---|
| Coinbase `/explore` | CoinbaseSans + CoinbaseText + CoinbaseDisplay | ✓ `url(./a_*.woff2)` | 16px | 306 노드 |
| 토스증권 | Toss Product Sans (단일 face, 100%) | ✓ | **13px** | 1,007 노드 |
| Robinhood `/stocks/AAPL` | Capsule Sans Text + Display | ✓ `cdn.robinhood.com` | 13 · 15px | 365 노드 |

지배 조합(상위 4개, 실측 개수):

```
Coinbase     16/400 (151)   16/600 (49)    14/400 (31)   12/400 (30)
토스증권      13/600 (514)   12/500 (242)   12/600 (152)  14/600 (44)
Robinhood    32/500 (106)   13/400 (87)    13/700 (55)   15/700 (50)
```

**세 제품 모두 본문에 세 단계를 쓰지 않는다는 결론은 유지된다.** 다만 인계 문서에서
받은 값과 다른 점이 세 가지 있고, 아래 §1.1.1 에 적었다.

이 근거 하나만으로 규칙을 세우지 말 것. 아래 근거 2 가 독립적으로 같은 결론에
도달하므로 "두 단계"라는 결론 자체는 이 표에 의존하지 않는다.

#### 1.1.1 재측정에서 인계값과 달랐던 것

**(가) Coinbase 는 표 헤더에 700 을 쓴다.** 인계 문서는 "400·600 2종"이라고 했는데,
`/explore` 의 자산 표 헤더는 **16px/700** 이다(`rgb(91,97,110)`). 본문 셀은 400/600 이
맞다. 즉 "본문 두 단계"는 맞고 "제품 전체가 두 단계"는 틀렸다.

**(나) 토스증권의 표는 500 본문이 아니라 600 본문이다.** 이게 이 재측정에서 제일
중요하다. 실시간 차트 표의 한 행을 뜯어보면 **1차 값이 전부 13px/600** 이다.

```
순위 1          13px/600   rgba(26,31,41,.89)
종목명 삼성전자   13px/600   rgba(26,31,41,.89)
현재가 266,500원 13px/600   rgba(26,31,41,.89)
등락률 +4.30%    13px/600   rgb(222,43,57)
거래대금 126억원  13px/600   rgba(26,31,41,.89)
시가총액        13px/600   rgba(26,31,41,.89)
거래비율 82 / 18  12px/500   파랑 / 빨강        ← 500 은 여기, 더 작은 보조 숫자
산업 칩          12px/600
```

그래서 토스의 실제 패턴은 **"500 본문 / 600 강조"가 아니라 "주 크기(13px)는 600,
보조 크기(12px)는 500"** 이다. 크기가 내려갈 때 굵기도 같이 내려간다.

**(다) Robinhood 의 500 은 본문이 아니다.** 32px/500(106) · 24px/500(18) 로 표제
전용이고, 본문은 **13/400 과 13/700** 이다. 인계 문서의 "400·500·700" 은 크기를
빼고 보면 맞지만 본문 굵기로 읽으면 틀리다. (측정 대상은 로그인하지 않은 공개
시세 페이지이고, 로그인한 트레이딩 앱은 다를 수 있다.)

**근거 2 (CDS 자체) — `[검증됨]`, 2026-08-13.** `@coinbase/cds-web@9.15.0` 의
`esm/themes/defaultTheme.js` 를 직접 읽었다. 14개 타이포 토큰은 **400 아니면 600
뿐이다.** 다른 값이 없다.

```
display1-3 400 · title1 600 · title2 400 · title3 600 · title4 400
headline 600 · body 400 · label1 600 · label2 400 · caption 600 · legal 400
```

즉 "두 단계"는 우리 판단이 아니라 CDS 와 실측 제품이 이미 합의한 것이다.

**근거 3 (한글).** 한글은 12~13px 에서 400 이면 ㅇ/ㅎ 의 속공간과 ㄹ/ㅂ 의 이음이
메워진다. 같은 크기의 라틴 문자에는 없는 현상이다. 그래서 낮은 단계만 400 → 500 으로
올린다.

> **주의 — 이 근거는 "400 을 피한다"까지만 지지한다.** 원래 여기에 "토스증권의 500
> 본문이 한국 관례다"라고 적혀 있었는데, §1.1.1(나)의 재측정이 그걸 뒤집었다. 토스의
> 표 본문은 **600** 이고 500 은 12px 보조 숫자 자리다. 한국 제품이 13px 한글에서
> 400 을 쓰지 않는다는 점은 확인됐지만, 그 대안이 500 이라는 근거는 없어졌다.
>
> ~~현재 v2 의 숫자 셀(`label2`)은 500 이고…~~ **닫혔다 [OWNER 2026-08-14]: 400.**
> 토스가 그 자리에 600 을 쓴다는 재측정에도 불구하고 Coinbase 스케일을 따른다 —
> 「나머지는 Coinbase」 지시와 같은 방향이다. 근거 3 의 13px 보정만 남는다.

**적용 범위 — 작은 토큰만.** 근거 3 은 작은 크기의 측정이고 28~64px 표제에 대해서는
아무 말도 하지 않는다. 그래서 `title*` / `display*` 는 CDS 값 그대로 두고, ≤16px 중
400 이던 것만 올린다.

```
(2026-08-13 안)  body 16px 400→500 · label2 14px 400→500 · legal 13px 400→500
(2026-08-14 확정) body·label2 는 400 그대로 · legal 13px 400→500 만 적용
headline / label1 / caption 은 이미 600 이라 그대로
```

구현은 `src/theme/sauronTheme.ts` 의 `fontWeight` 한 곳. ThemeProvider 가
`--fontWeight-*` 로 내보내고 모든 CDS 텍스트 컴포넌트가 그걸 읽으므로 컴포넌트마다
덮어쓸 필요가 없다.

### 1.2 면(face)은 자체 호스팅한 Pretendard 하나 — `local()` 금지

**이 데스크에 Pretendard 는 설치돼 있지 않다** — `[검증됨]` 2026-08-14 재확인
(`HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Fonts`, 총 391개):

```
Pretendard    (없음)
Noto Sans KR  Noto Sans KR (TrueType)
Malgun        Malgun Gothic · Bold · SemiLight
```

`local()` 만 걸면 조용히 Malgun 으로 떨어지고, **Malgun 은 굵기가 3종이라 위 §1.1 의
500/600 이 Regular/Bold 로 붕괴한다.** 디자인 결정이 오타에 의해 뒤집히는 셈이다.

`local()` 을 보태지 않고 **완전히 뺀 이유**는 따로 있다. 다른 버전의 Pretendard 가
설치된 기계는 메트릭이 달라서, 포맷 최대값으로 계산한 열 폭이 기계마다 달라진다.
파일 하나, 메트릭 하나.

- `pretendard@1.3.9`, SIL OFL-1.1, `public/fonts/`
- 서브셋 2,009KB → 73.8KB (96% 감소), `wght` 가변축 45~930 보존
- `font-display: block` (swap 아님) — swap 은 face 교체 순간 모든 열 폭을 점프시킨다

**패밀리 이름은 `Pretendard SR`** 이고, 이 이름이 두 곳에 **모두** 들어가야 한다.

1. `src/theme/type.css` 의 `@font-face`
2. `src/theme/sauronTheme.ts` 의 `SR_FONT_STACK` — **CDS 컴포넌트는 이쪽만 읽는다**

> **이 실패는 두 번 났다.** 처음엔 `local()` 만 걸어서, 두 번째는 self-host 후
> `--sr-font-sans` 에만 이름을 넣고 테마 스택은 `'Pretendard Variable'` 로 남겨둬서.
> 둘 다 렌더는 깨지지 않고 그냥 Malgun 으로 나온다. `guards/font-face.test.ts` 가
> 이제 두 스택이 같은 이름으로 시작하는지 본다.

실측(2026-08-13, 고정 문자열 `금리 0123456789` 의 advance):

```
없는 face(대조군)   120.00      ← 순수 폴백 기준선
Pretendard SR       124.95      ← 지금 쓰는 것
Malgun Gothic       125.75      ← 고치기 전에 나오던 것
Noto Sans KR        121.82
```

가변축이 살아 있는지도 확인해야 한다. `Pretendard SR` 에서 400/500/600/700 이
99.16 / 99.69 / 100.23 / 100.77 로 **네 개 다른 폭**이다. Malgun 이었다면 두 개로
붕괴한다. 폰트 검출은 이름을 믿지 말고 canvas `measureText` 로 대조군과 비교할 것 —
`document.fonts.check()` 는 폴백에도 `true` 를 준다.

---

## 2. 밀도

### 2.1 행 높이 60px, 이름 두 줄 [OWNER 2026-08-13]

손으로 고른 수가 아니다. CDS 의 권장 밀도인 `spacingVariant="condensed"`
(위아래 `--space-1`, min-height 없음)가 두 줄 스택에 대해 내놓는 값이다.

```
CDS 스케일(space[1]=8)로 계산:  title lh 24 + description lh 20 = 44 + 8 + 8 = 60
```

실제 렌더에서도 같은 60 이 나온다. 다만 경로가 다르다 — **CDS `TableCell` 은 패딩을
두 겹 중첩한다.** 인라인 height 를 제거하고 잰 값:

```
이름 스택            36   (label1 lh 20 + legal lh 16)
CDS 안쪽 셀 패딩     12   (--space-1 ×2)
CDS 바깥 셀 패딩     12   (--space-1 ×2)
                    ───
자연 콘텐츠 박스     60   ← ROW_H 와 정확히 일치
```

v2 는 `space['1']=6` 으로 낮춰뒀기 때문에 이 값이 나온다(CDS 기본 8이면 68).

`normal`(80) 과 `compact`(40) 은 CDS 9.15 에서 **둘 다 deprecated** 이고 두 줄
행에는 맞지 않는다.

**외부 제품 행 높이 — `[검증됨]`, 2026-08-13 재측정:**

| | 행 높이 | 줄 수 | 비고 |
|---|---|---|---|
| Coinbase `/explore` 자산표 | **76.0px** (10행 전부 동일) | 2줄 | CDS `normal`(80) 급 |
| 토스증권 실시간 차트 | **40px** (58행) / 44px (39행) | 1줄 | 인계값 "~36px"보다 큼 |
| sauron-v2 | **60px** | 2줄 | |

인계 문서의 토스 "~36px" 는 실측 40px 과 다르다. 그리고 **토스의 40px 는 한 줄 행**
이므로 두 줄인 우리 60px 과 직접 비교할 값이 아니다. 비교 가능한 건 Coinbase 의
76px(두 줄)이고, 우리는 그보다 16px 조밀하다.

Coinbase 의 두 줄 이름칸은 **16px/600 위에 14px/400 muted**(`rgb(91,97,110)`)로,
크기 2단계 + 굵기 1단계 + 색 1단계를 동시에 떨어뜨린다. v2 는 14/600 위에 13/500
muted 로 같은 구조를 한 단계 조밀하게 쓴다.

**알려진 잔차(기존부터 있던 것).** 행의 rect 는 60 이 아니라 60.5~61 이다.
`Table variant="ruled"` 가 `border-collapse: collapse` 아래 1px 괘선을 긋고 그 절반이
양쪽 박스에 들어간다. 가상화는 60 으로 추정하고 실측하지 않으므로 77행에서 약 38px
누적된다 — 스크롤바가 조금 낙관적이 되지만 깨지지는 않는다. ROW_H 48 시절에도
48.5 였다. 고치려면 분수 ROW_H(첫/끝 행은 몫이 달라 균일하지도 않다)나 괘선 포기라서
조용히 바꾸지 않는다.

---

## 3. 색과 면

### 3.1 방향색은 둘, 그리고 CDS 것이 아니다

상승 = 적, 하락 = 청. CDS 의 `fgPositive`/`fgNegative` 는 초록-좋음/빨강-나쁨을
싣고 있어 KRW 금리 데스크에서 뒤집힌다 — 금리가 오르는 건 좋은 일이 아니라 **뉴스**다.
v2 소스에서 그 두 토큰의 참조는 0건이다.

값은 `src/theme/direction.css` 에만 있다(`guards/color-source.test.ts` 가 리포 전체에
hex 리터럴이 거기 말고 없는지 본다). 라이트/다크 두 쌍 모두 동결이고 재조정 금지 —
`#d92d3c` 는 `#f04452` 가 3.71:1 로 떨어져서 나온 값이다.

**색각이상 분리 — `[검증됨]` 2026-08-14. 인계값 27.3 은 폐기한다.**

Viénot–Brettel–Mollon 시뮬레이션 + CIEDE2000 으로 직접 계산했다(스크립트는 세션
기록에 있고 값은 재현 가능하다):

```
                       정상시각   protan   deutan
라이트 #de2b39/#2171eb   44.75     50.90    65.37
다크   #e86c76/#5794f0   40.58     34.34    50.14
CDS 녹/적 #098551/#cf202f 67.36    16.83    12.43   ← 참고
```

두 가지가 나온다. 첫째, **적/청 쌍은 색각이상에서 오히려 더 벌어진다**(protan 50.9) —
적록 축이 아니라 명도·청황 축으로 갈리기 때문이다. 27.3 은 어떤 쌍의 값이었는지 알 수
없고 지금 쌍의 값이 아니다. 둘째, **CDS 의 녹/적은 protan 16.8 / deutan 12.4 로 사실상
구분되지 않는다.** v2 가 그 두 토큰을 안 쓰는 이유는 한국 관례가 첫 번째지만, 접근성이
두 번째 이유로 붙는다 — 관례가 없었어도 저 쌍은 쓰면 안 됐다.

**방향 관례 — `[검증됨]`, 2026-08-13.** 적/청이 한국 관례라는 전제를 실제 제품에서
확인했다.

```
토스증권    상승 rgb(222, 43, 57) 빨강   하락 rgb(34, 114, 235) 파랑   (98개 표본)
Coinbase   상승 rgb(9, 133, 81) 초록    하락 rgb(207, 32, 47) 빨강
```

한국 제품은 적/청, 서구 제품은 녹/적이다. CDS 의 `fgPositive`(초록)/`fgNegative`(빨강)를
그대로 쓰면 **한국 독자에게는 하락이 초록으로 보인다.** v2 가 그 두 토큰을 참조하지
않는 이유가 여기 있다.

### 3.2 방향색을 올릴 수 있는 면은 셋뿐

`bg` · `bgElevation1` · `bgElevation2`. 실측:

```
up   #d92d3c on rgb(238,240,243) = 4.19:1   ← 미달
down #0064ff on rgb(238,240,243) = 4.31:1   ← 미달
```

`bgAlternate` 와 `bgSecondary` 는 CDS 라이트에서 **둘 다** rgb(238,240,243) 이고
방향색이 4.5:1 을 못 넘는다. 그래서 **v2 는 표 행을 zebra-stripe 하지 않는다.**
다크에서는 세 면 모두 통과 — 실패는 라이트 전용이다.

### 3.3 회색 본문 단계는 둘이 최대

흰 바탕에서 4.5:1 을 넘는 회색은 `#767676` 언저리가 한계라, 세 번째 단계를 만들면
두 번째와 사실상 같은 색이 된다.

**`--faint` — `[검증됨]` 2026-08-14, 값만 정정.** 목업 앞 판의 `#979da8` 은 흰 바탕에서
**2.73:1** 이다(인계값 2.50 은 살짝 틀렸다). 결론은 그대로 — 기준의 61% 이고 본문에
쓸 수 없다. 다만 이건 **목업(`design/mockup/desk.html`)의 토큰**이지 v2 의 것이 아니다.
v2 가 실제로 쓰는 CDS `fgMuted` 는 측정값으로

```
라이트 rgb(91,97,110)   on bg rgb(255,255,255)      6.21:1
다크   rgb(138,145,158) on bgElevation1 rgb(20,21,25) 5.76:1
```

라 두 스킴 모두 통과한다. 즉 이 항목은 v2 에 남은 문제가 아니라 목업이 왜 폐기됐는지의
근거다.

참고로 토스증권은 본문 잉크를 **`rgba(26,31,41,0.89)`** 로 쓴다 `[검증됨]`. 불투명
회색이 아니라 검정에 알파를 먹인 값이라, 배경이 바뀌어도 대비 관계가 같이 따라간다.
v2 는 CDS 토큰(`fgMuted`)을 쓰므로 이 방식을 채택하지는 않지만, 회색 단계를 늘리는
대신 알파를 쓰는 선택지가 있다는 건 기록해둔다.

**보조는 명도가 아니라 크기와 굵기로 구분한다.**

### 3.4 면은 3단 — `[검증됨]` 적용 완료

바탕 → 카드 → 채운 컨트롤. 컨트롤은 테두리가 아니라 **채운 회색**이다.
헤어라인만으로 구분한 평면 화면은 제품이 아니라 와이어프레임으로 읽힌다.

**카드는 색조 + 라운드 + 헤어라인 셋 다여야 한다.** 하나만 있으면 실수로 읽힌다 —
색조만은 선택 상태, 라운드만은 잘린 이미지, 헤어라인만은 이걸 대체한 그 와이어프레임.

#### 스킴마다 토큰이 뒤집힌다 `[검증됨]`

CDS 는 이걸 한 쌍으로 주지 않는다. **라이트에서 `bgElevation1`·`bgElevation2` 는
`bg` 와 똑같은 흰색**이고(L=1.000 셋 다), 라이트의 elevation 은 색이 아니라 그림자로
표현된다. 다크에서만 실제로 밝아진다.

```
             라이트                      다크
--sr-page    bgAlternate  238,240,243   bg            10,11,13
--sr-card    bg           255,255,255   bgElevation1  20,21,25
--sr-control bgAlternate  238,240,243   bgSecondary   40,43,49
```

#### 부호 있는 숫자는 카드 위에만

```
라이트 bgAlternate 위   상승 4.08  하락 3.98   ← 미달
라이트 bg          위   상승 4.66  하락 4.54   ← 통과
다크   bgElevation1 위  상승 5.94  하락 5.99   ← 통과
```

`--sr-page` 는 크롬만 이고 숫자를 싣지 않는다.

#### 함정: `:root` 에 선언하면 조용히 죽는다 `[검증됨]`

**CDS 는 `--color-*` 를 `:root` 가 아니라 자기 ThemeProvider 래퍼에 인라인으로**
심는다. 커스텀 속성 값은 **선언된 자리에서** 치환되므로 `:root { --sr-card:
var(--color-bg) }` 는 무효값이 된다. 실측:

```
:root      --color-bg = (없음)
카드 요소   --color-bg = rgb(255,255,255)   ← 여기엔 있다
카드 요소   --sr-card  = (빈 값)            ← :root 에서 이미 죽었다
```

**아무것도 깨져 보이지 않는다.** 무효값이면 배경이 투명으로 떨어지고, 카드는
테두리와 라운드가 남아서 "흰 바탕 위 흰 카드"로 보인다. DOM 에서
`backgroundColor` 를 읽어봐야 잡힌다.

**이 리포는 같은 계열을 세 번 밟았다** — 폰트 패밀리(`body { --fontFamily-body }`),
테마 폰트 스택(§1.2), 그리고 이것. 모양은 늘 같다: **자기가 참조하는 값을 소유한
스코프보다 위에서 변수를 정의한 것.** `guards/surface-tokens.test.ts` 가 감시한다.

---

## 4. 행의 구성

### 4.1 이름 셀은 두 레지스터, 세 번째는 없다

이름이 그 행에서 숫자가 아닌 유일한 강조(label1/600)이고, 둘째 줄은 세 번째 굵기가
아니라 **크기와 색**으로 갈라진다(legal/500 + `fgMuted`).

둘째 줄은 `1년 평균 {값}{단위}` 이고, 오늘 값을 행 밖으로 나가지 않고 견줄 기준을
준다. 값은 현재 열과 **같은 포매터**(`rangeText` → `fmtLevel`)를 지나므로 같은 양이
두 자리수로 보일 수 없다.

`호가` 는 붙지만 `보간` 은 붙지 않는다. `Row.quoted` 가 `s.quoted ?? undefined` 로
만들어져서 API 의 `null`(보간)과 필드 없음이 같은 `undefined` 로 뭉개지기 때문이다.
없는 표시는 "호가로 확인되지 않음"이지 "보간"이 아니다.

### 4.2 커브 위치 앵커 칩 — 만들어서 재보고 **뺐다. 재유도 금지.**

규칙 자체는 실측이다: Coinbase `/explore` 는 모든 행을 코인 로고로 앵커하고, 눈이
걸릴 곳이 없는 목록은 확실히 읽기 어렵다. 하지만 **v2 의 라벨은 이미 만기로 시작한다.**
칩은 모든 탭에서 이름의 부분 문자열을 반복했다(2026-08-13, 실행 중인 앱):

```
아웃라이트   [3M]  3M                  ← 완전히 동일
스프레드     [6M]  6M/9M               ← 접두
버터플라이   [6M]  6M/9M/1Y            ← 접두
포워드       [3M]  3Mx6M               ← 접두
변동성       [1Y]  1Y σ                ← 접두
국고         [6M]  국고 6M              ← 포함
국채선물     [3Y]  3년 국채선물 가격      ← 포함 ("3년"이 곧 3Y)
크레딧       [6M]  통안 6M / 산금 6M / … ← 포함, 그리고 해롭다
```

크레딧 탭이 결론을 냈다. 그 행들은 **발행체**로 갈리고 만기를 공유하는데, 칩은 공통인
쪽을 반복하면서 정작 구분해 주는 값은 옆에서 강조 없이 앉아 있게 만든다. 구분값을
가리는 앵커는 앵커가 없는 것보다 나쁘다.

목업이 칩을 필요로 했던 건 그쪽 라벨이 산문이었기 때문이다("IRS 3M", "Call (1D)").
v2 의 `buildRows` 는 시리즈 id 를 쓰고 그게 곧 만기다. **이 표의 앵커는 이름 자체다.**

---

## 5. 폭

### 5.1 열 폭은 포맷 최대값 × 실측 advance — 오늘 데이터가 아니라

`tabular-nums` 아래서는 모든 숫자의 advance 가 같으므로 가장 넓은 렌더링은 고정
템플릿 문자열이고, 그래서 탭을 바꾸든 정렬하든 필터하든 격자가 움직이지 않는다.

폭은 `<colgroup>` 이 px 로 선언한다. CSS 그리드 문자열을 헤더와 본문에 각각 주는
방식은 이미 한 번 깨졌다 — `ch` 는 **그 요소 자신의 폰트**에서 "0"의 advance라
`font-medium` 헤더와 `font-normal` 본문이 같은 `44ch` 를 다른 px 로 푼다.

같은 이유로 **ch 프로브는 숫자 셀에서 재야 한다**(`td.sr-num span`). 이름 셀이 자기
레지스터(600)를 갖게 된 순간, 이름 셀에서 잰 advance 는 숫자의 advance 가 아니다.
가변 폰트에서 굵기가 advance 를 움직이기 때문이다.

### 5.2 `WIDEST.label` 은 낡았다 — 지금은 쿠션이 가려주고 있다

그 상수는 아직 가장 긴 식별자가 9글자 `1s1.5s10s` 라고 말한다. v1 의 스왑 전용
유니버스에서는 맞았다. v2 는 국고/크레딧/본드스왑/국채선물을 더했고 라벨이 한글이다.

실측(ch = 8.791, Pretendard SR 14px/500):

```
가장 긴 라틴 id     1s1.5s10s              62.1 px
가장 긴 한글 이름   10년 국채선물 내재금리   130.9 px   ← 진짜 최대
가장 긴 둘째 줄     1년 평균 3.0801% · 호가  130.6 px

9ch(79.1) + COL_PAD.label(80) = 159.1 px — 둘 다 ~28px 여유로 통과
```

즉 글자수 기준이 열을 ~52px 적게 선언하고 있고, px 쿠션이 국채선물 이름이 잘리지
않게 막고 있다. **고칠 자리는 거기가 아니다** — ch 로 센 최대와 px 로 잰 최대를
말없이 더하면 안 된다. 정직한 수리는 한글 라벨 그룹을 위한 px 단위 `WIDEST` 항목을
따로 두는 것이고, 그건 쿠션이 아니라 폭 모델의 변경이라 여기 적어만 둔다.

한글은 advance 가 폰트 크기에 비례하고 `ch` 에 비례하지 않는다. `RANGE_PAD` 가
글자수가 아니라 실측 px 인 것도 같은 이유다.

### 5.5 셀 **안쪽** 격자도 px 로 — `ch` 가 세 벌이었다 `[검증됨]` 2026-08-14

`<colgroup>` 이 열을 px 로 고정한 뒤에도, 52주 셀 **안쪽**의 하위 격자는 CSS
`calc(6ch + 24px)` 로 남아 있었다. 그 `ch` 는 격자 컨테이너(`.sr-range`) 자신의
폰트에서 풀린다. 돌아가는 앱에서 잰 값:

```
.sr-range      (격자 컨테이너)  16px/600   ch = 10.86   ← 트랙 폭을 정하던 값
.sr-range > *  (실제 숫자)      13px/600   ch =  8.34
td.sr-num span (사다리 프로브)   14px/400   ch =  8.60   ← 사다리가 쓰던 값
```

**트랙은 표 안의 어떤 글자에도 속하지 않는 advance 로 그려지고 있었다** — 사다리
산술보다 26% 넓게. 남는 `1fr` 필러가 흡수하는 동안은 아무것도 이상해 보이지 않다가,
세타가 다섯 번째 트랙으로 들어온 순간 셀이 열을 61px 넘겨 표에 가로 스크롤바가 생겼다.

규칙: **사다리와 렌더는 같은 숫자를 쓴다.** `rangeTemplate(slider, theta, chPx)` 가
측정된 advance 로 px 트랙을 만든다. `ch` 문자열은 첫 측정 이전 한 프레임에만 남는다.

### 5.6 사다리가 쓰는 폭은 `<table>` 의 것, 그리고 셀 인셋 16px `[검증됨]` 2026-08-14

두 가지를 사다리가 쓰고 있지도 않은데 쓰고 있다고 셈했다.

```
호스트 div  926 px
<table>     909 px   ← 차이 17 = 스크롤 컨테이너 테두리 + 세로 스크롤바
52주 <th>   496.2 → 935.0,  안쪽 격자는 512.2 에서 시작   ← 셀 인셋 16
```

그래서 (1) 폭 관측은 호스트가 아니라 `<table>` 에 건다(테이블 폭은 `table-layout:
fixed` 라 열 폭에 의존하지 않으므로 순환이 아니다), (2) 52주 꼬리는 `containerPx −
CELL_INSET` 으로 잰다. 다른 열은 자기 쿠션(라벨 80px)이 이 인셋을 흡수하지만, 52주
셀은 **고정 하위 트랙의 합**이라 쿠션이 없다. 증상이 "숫자 잘림"이 아니라 "표 전체에
가로 스크롤바"인 것도 그 때문이다 — 안쪽 `1fr` 이 먼저 0 이 된다.

### 5.7 표와 미리보기의 폭은 캡이 아니라 **basis** 로 `[검증됨]` 2026-08-14

카드 둘이 `width: 100%` 로 나란히 서면 flex 가 남는 폭을 반씩 나눈다. 1920 화면에서
표 카드는 `maxWidth` 와 무관하게 **928px**(=(1920−48−16)/2)이었고, 그래서 캡을
860→940 으로 올린 것은 화면에서 아무 일도 하지 않았다. 폭은 basis 로 준다:

```
표    flexBasis 980 · flexGrow 0 · maxWidth 980   — 사다리가 필요한 만큼
미리보기 flexGrow 1 · minWidth 380                  — 남는 것 전부
```

980 의 근거는 사다리 산술이다(ch 8.6 실측): 열 470 + 52주 셀 444(227+76+125+인셋 16)
= 표 914, 여기에 표가 카드보다 17px 좁은 몫을 더한다.

---

## 5.4 컴포넌트는 CDS 것만 `[검증됨]` [OWNER 2026-08-13]

`cds.md` 의 첫 규칙: *"CDS 컴포넌트가 존재하면 항상 그것을 우선한다 — **웹에서는
네이티브 HTML 요소도 포함된다**."*

교체한 것:

| 자리 | 전 | 후 | 얻은 것 |
|---|---|---|---|
| 기간 선택 | `<button>` × 5 | **`PeriodSelector`** | 키보드·슬라이딩 인디케이터·활성 wash 를 CDS 가 줌 |
| 메가 항목 | `<button>` + flex 3겹 | **`ListCell`** | `media`/`title`/`description` 이 정확히 참조의 항목 구조 |
| 섹션 버튼 | `<button>` | **`Pressable`** | 누름 접근성 |
| 레이아웃 | `<div>`/`<span>` | **`Box`/`HStack`/`VStack`** | |

`PeriodSelector` 가 가장 큰 이득이다. 직접 짠 알약 줄은 키보드 처리와 인디케이터를
전부 다시 만들어야 했는데, CDS 는 그게 차트 기간용으로 이미 조율돼 있다.

### 남는 네이티브 요소, 그리고 그 이유

**CDS 에 대응물이 없는 것만** 남는다:

- `InstrumentTable` 의 `<tr data-sr-spacer>` · `<td>` · `<colgroup>/<col>` —
  가상화 스페이서 행과 열 폭 선언은 `Table` 이 노출하지 않는 개념이고, 스페이서를
  `TableRow` 로 바꾸면 CDS 가 셀 패딩을 넣어 스페이서 높이가 거짓말을 하게 된다.
- **`ReconStack` 의 표 전체** — `<colgroup>` 으로 트랙을 못박아야 하고, 셀마다
  sticky 오프셋이 필요하고, 날짜·요약 칸이 `rowSpan=3` 이다. 셋 다 `Table` 에 없고,
  게다가 `TableCell` 은 좌우 16px 씩 32px 을 먹어 `ch` 오프셋과 트랙이 어긋난다.
- **워터폴** — CDS 에 워터폴 차트가 없다.
- **케이스 색 대시**(`.sr-casedash`) — "시리즈 색 표식" 이라는 개념이 CDS 에 없다.
  칩 자체는 CDS `Chip` 이고 이 대시만 `start` 슬롯에 들어간다.
- **설정 카드 표면**(`.sr-simcard`) — 면 규칙은 `.sr-card` 와 같지만 **안 자른다**.
  안의 `Select` 드롭다운이 카드 밖으로 나와야 한다(§8.10 과 같은 이유).

#### 실제로 어긴 적이 있다 `[검증됨]` 2026-08-14

레인 5 첫 판이 세그먼트·칩·접기·표를 전부 `<button>` + CSS 로 직접 만들었다. CDS 에
`SegmentedTabs` · `Chip` · `Collapsible` · `Table` 이 다 있는데도 그랬고, 그건 키보드
이동·활성 인디케이터·포커스 링을 다시 만드는 일이었다. 전부 CDS 로 갈아탔고 죽은
CSS 는 지웠다(가드가 잔재를 본다).

갈아탈 때 알게 된 것: **`SegmentedControl` 은 deprecated 다** — 그 파일이 "Please use
Tabs or SegmentedTabs instead" 라고 적어 두었다. `Card` 도 deprecated 다(`ContentCard`
권장). 없어질 컴포넌트로 갈아타면 다음 메이저에서 또 갈아타야 한다.

### CDS 컴포넌트로 갈아탈 때 깨진 것 세 개 `[검증됨]`

교체는 공짜가 아니었다. 실측으로 잡은 회귀:

1. **`Box` 는 `as="span"` 이어도 `display: flex`** — 히어로의 단위 `%` 가 숫자
   아래 줄로 떨어졌다. `display: inline` 을 명시해야 한다.
2. **`HStack` 은 flex 아이템이라 내용만큼만 넓다** — `<div>`(블록, full width)를
   바꾼 순간 내비 바가 1920 → 753px 로 줄고 `margin-left: auto` 가 밀 공간을
   잃었다. `width: 100%` 필요.
3. **둥근 컨트롤은 곧은 밑줄을 못 받는다** — `border-radius: 999px` 위의
   `inset 0 -2px` 그림자가 알약을 따라 휘어 길 잃은 테두리로 읽혔다. 현재 섹션과
   호버는 **같은 모양의 두 색조**로 바꿨다.

---

## 6. CDS 를 쓸 때 아는 함정

1. **`Sparkline.color` 는 원시 색만 받는다.** 문서 그대로: *"Does not work with CDS
   theme color names like fgPrimary or CSS variables."* `var(--sr-up)` 를 넘기면
   **조용히** 실패한다. `useTheme()` 으로 런타임에 풀어서 넘길 것.
2. **`ListCell.intermediary` 는 타입에 "For internal use only" 로 적혀 있다**
   (`dts/cells/ListCell.d.ts:86`). 그런데 CDS 자신의 `Sparkline` 문서가 바로 그
   prop 을 스파크라인 자리로 쓴다. CDS 내부의 모순이고, deprecated 는 아니다.
3. **`Table` 의 `height` 타입이 거짓말한다.** `React.CSSProperties['height']` 로
   타입만 통과하고 구현은 무조건 `` `${height}px` `` 라 `'70vh'` → `70vhpx` →
   `max-height: none`. **픽셀 숫자로 넘길 것.**
4. **`TableCell` 에 `children` 을 직접 넘기면 `justifyContent` 가 DOM 에 안 닿는다**
   (`smartJustifyContent` 가 `title`/`subtitle` 분기에서만 적용). 그래서 숫자 정렬은
   v2 가 `.sr-num` 으로 직접 한다.
5. **`TableRow` 에 `forwardRef` 가 없다.** 행 리스너·포커스는 컨테이너 위임으로.
6. **CDS 는 핸들러를 내부 `Interactable` 에 바인딩한다** → `th.click()` 은 안 먹지만
   실제 클릭은 먹는다. 브라우저 프로브로 `onClick` 이 undefined 라고 갭을 보고하지 말 것.
7. **`space` 는 8의 배수 전제**(`space[1]=8`, `[2]=16`, `[3]=24`). 벗어나면 깨진
   스타일이 나올 수 있다고 CDS 문서가 경고한다. v2 는 `space['1']=6` 으로 한 칸만
   내렸고, 그게 행 높이를 정확히 60 으로 만든다(§2.1).

---

## 7. 검증할 때의 함정

- **테마 토글과 같은 틱에서 대비를 재면 거짓 실패가 나온다.** 토글 직후 바로
  `getComputedStyle` 하면 배경만 이전 테마로 읽혀 `1.04:1` 같은 불가능한 값이 나온다.
  `void document.body.offsetHeight` 로 리플로우를 강제하고 다시 잴 것.
- **rAF 를 기다리지 말 것.** 자동화 탭이 백그라운드면 rAF 가 돌지 않아 CDP 가 타임아웃난다.
- **폰트가 실제로 적용됐는지는 이름이 아니라 폭으로 확인**(§1.2).
- **백엔드 CORS 는 `:3200`/`:3100` 만 허용한다**(`backend/app/main.py`). 다른 포트로
  띄운 프론트는 "FAILED TO FETCH" 로 죽는다 — 데이터가 없는 게 아니라 오리진이 막힌 것.
- `:3000`/`:8000`(동결 krw-fi-pms), `:3100`/`:8100`(braveworld), `:3200`/`:8200`(v2)
  는 각각 임자가 있다. 검증용 서버는 다른 포트에 띄우고 끝나면 내릴 것.

---

## 5.3 열 머리는 700, muted `[검증됨]`

Coinbase `/explore` 자산표 헤더는 **16px/700 · `rgb(91,97,110)`** 이다. 본문 셀이
400/600 이므로 700 은 데이터 안의 세 번째 단계가 아니라 **헤더 행과 그 아래 모든
행을 가르는 것**이다.

CDS 에는 700 토큰이 없다(전 스케일이 400/600). 그래서 `sauronTheme.fontWeight` 가
아니라 `thead th` 규칙으로 둔다 — 테마에 700 을 넣으면 모든 컴포넌트가 세 번째
굵기에 손을 뻗을 수 있게 된다.

**크기는 Coinbase 의 16px 이 아니라 토큰값(13px `caption`) 그대로다.** 13px 값
위에 16px 헤더를 얹으면 헤더가 자기가 이름 붙인 데이터보다 무거워지고, 이 표는
저쪽보다 조밀하다. 분리는 굵기가 하고 크기는 하지 않는다.

---

## 8. 표가 말하는 것 — 2026-08-14 (레인 1)

### 8.1 세타 열 — 폭이 되나와 여기 있나는 다른 질문

3개월 캐리 + 롤다운을 DV01 백만원으로 나눈 값. 숫자는 전부 `backend/app/theta.py`
가 낸다(§16) — 프론트는 `perDv01` 을 찍고 나머지를 툴팁으로 말할 뿐이다.

- **라벨이 정규화 단위를 진다**(`세타/DV01백만`). "세타"만 쓰면 현금으로 읽히는데,
  이 열이 존재하는 이유가 100억 기준과 리스크당 기준이 테너 순위를 **반대로**
  매기기 때문이다(100억이면 10Y 가 최대, 리스크당이면 1Y 가 6배).
- **잉크다, 방향색이 아니다.** 부호 있는 돈이지만 같은 행이 이미 그 색을 금리 방향에
  쓰고 있다. 한 행에서 한 색이 두 뜻을 지는 것보다 없는 게 낫고, `fmtKrw` 의 부호
  글리프가 방향을 진다.
- **값이 없으면 em dash.** 빈 칸은 로딩으로 읽힌다.
- **탭 전체에 값이 없으면 열 자체를 안 그린다.** 포워드·변동성·민평·선물은 스왑이
  아니다. 이 판정은 **사다리의 입력**(`visibleColumns(…, hasTheta)`)이지 사후
  패스가 아니다 — 사후에 끄면 옆 열(위치)이 그리지도 않는 열의 폭을 이미 예약한
  뒤라 애먼 자리에서 떨어진다. 그리고 그건 사다리 드롭이 아니라서 "N열 숨었어요" 를
  올리지 않는다 — 적용되지 않는 열은 숨겨진 게 아니다.
- **정렬 키 없음 [OWNER 2026-08-14].** 52주 열과 같다 — 헤더에 컨트롤이 없다.
- 툴팁은 **표시 정밀도에서 더해진다**: 합계와 롤다운만 반올림하고 캐리는 그 차다.

#### 꼬리의 우선순위 = 52주 세 숫자 → **세타** → 위치 [OWNER 2026-08-14]

세타가 마지막 rung 이라 **제일 먼저 사라지고 있었다**(실측: 뷰포트 1390 아래). 오너가
"바로 눈에 띄게" 요청한 열이 창을 조금만 줄여도 없어지는 상태였다. 위치가 대신
자리를 내준다 — **위치는 옆 세 숫자의 그림**이라 없어져도 같은 사실이 행에 남지만,
세타는 화면 어디에도 없는 양이다. 실측 결과(폭 = 뷰포트):

```
1440+  위치 + 세타        1366  세타(위치 숨음)
1280   52주 세 숫자        1100  52주 통째로 숨음
```

**떨어진 열은 좁혀도 다시 안 나온다.** 위치가 세타보다 좁으니 "세타는 못 들어가는데
위치는 들어가는" 폭이 존재하지만, 창을 줄이는데 열이 **되돌아오면** 고장으로 읽힌다.
사다리는 패킹 문제가 아니라 우선순위다.

세타 트랙 쿠션도 실측으로 줄였다(30 → 20): 선언 최대값 `−1억 2,345만원` 이 실제로는
95.2px 이고 셀 패딩 12 를 더해 107.2 가 필요한데 124.6 을 예약하고 있었다. 그 17px 이
곧 이 열이 보이냐 마느냐를 가르는 마지막 17px 이다.

### 8.2 고시 / 보간 / 개념 없음 — 셋째 상태는 침묵으로 말한다

`Row.quoted` 는 세 상태다: `true` 호가 · `false` 보간 · `undefined` 해당 없음.
앞 패스는 `false` 가 빌더에서 뭉개진다고 읽고 보간을 일부러 안 찍었는데, `??` 는
`false` 를 뭉개지 않았다 — **구분은 내내 데이터에 있었고 화면만 말을 안 했다.**

- 아웃라이트: `derive.py:355` 가 호가 노드면 true, 보간이면 false.
- 포워드: `forwards.py:70` 의 `live` — 양끝이 모두 호가 노드일 때만 고시.
  주요 여섯과 무관한 사실이라 구분선이 대신 말해주지 못한다.
- 스프레드·플라이·변동성·민평·선물: 표시 없음. 커브 **노드**에 대한 사실이라
  그 행들은 애초에 노드가 아니다. `universe.py:144` 의 `"quoted": True` 는 행 모양을
  맞추려고 박아둔 리터럴이라 프론트가 그대로 옮기지 않는다.

### 8.3 커브 배너 — 커브의 사실은 행이 아니라 줄로

아웃라이트의 60% 이상이 52주 상·하위 10% 에 있으면 한 줄(`derive.py::curve_banner`).
**아웃라이트 탭에서만** — 판정 입력이 그 커브뿐이라 국고·크레딧 표 위에 띄우면 눈앞의
표에 대한 말로 읽힌다. 고점권은 상승색, 저점권은 하락색(v1 은 둘 다 상승색이었다).
방향색이라 **카드 안**에 산다(§3.2).

### 8.5 기준선 둘 — CD 91일과 기준금리 [OWNER 2026-07-31]

한 지시이지 두 개가 아니다. CD 는 모든 원화 IRS 호가가 맞물리는 변동 다리이고
기준금리는 그 CD 가 따라가는 것이라, 금리는 **둘 다에 대해 읽거나 둘 다 없이 읽는다.**

- **날짜로 맞춘다, 위치로 맞추지 않는다.** 두 시리즈의 프리뷰는 각자 솎아지므로
  i 번째가 같은 날이 아니다. 그냥 zip 하면 다른 주의 CD 를 이번 주 금리 옆에 그린다.
- **각진 모서리**(`curve: 'stepAfter'`). 정책금리는 평평하다가 뛴다.
- **`through` 를 넘지 않는다.** 그 뒤는 `null` 이고 `connectNulls` 도 끄므로 선이
  끊긴다 — 백엔드가 보증을 끊은 자리를 화면도 끊는다.
- **CD 는 한 번만 받는다**(모듈 캐시). 종목이 아니라 화면의 기준이고, hover 로 표를
  가로지를 때마다 2,600점을 다시 받을 이유가 없다.
- **범례는 실제로 그려진 것만** 이름을 얻는다. CD 를 못 받아온 날 없는 선을 가리키지
  않기 위해서다.

**bp·ratio·가격 차트는 기준선이 자기 %축을 왼쪽에 단다** [OWNER 2026-08-03 / 좌측
2026-08-14]. 종목 축은 오른쪽(레퍼런스의 자리), 기준선 축은 왼쪽. 실측:

```
스프레드   좌 2.5000…2.9000 %     우 0.0…35.0 bp
변동성     좌 2.5000…2.9000 %     우 0.00…3.50 ratio
아웃라이트  좌 (없음)              우 2.5000…4.0000 %   ← 같은 단위는 축 하나
```

> **한나절 동안 "CDS 는 보조축이 없다" 고 적혀 있었고 그건 틀렸다.** 근거로 삼은
> `LineChart.d.ts:71` 의 단수 `yAxis` 는 **그 래퍼의 편의**일 뿐이다. 한 겹 아래
> `CartesianChart` 는 `yAxis` 를 배열로 받고 시리즈가 `yAxisId` 로 축을 고른다
> (`CartesianChart.d.ts:37`, `utils/chart.d.ts::Series.yAxisId`). 래퍼의 타입을
> 라이브러리의 한계로 읽지 말 것 — `LineChart` 가 하는 일은 축 설정을 쪼개
> `<XAxis>`/`<YAxis>`/`<Line>` 을 대신 그려주는 것뿐이라, 그 자식들을 직접 쓰면
> 잃는 것이 없다.

**함정 — 축에 이름을 붙이면 시리즈도 그 이름을 가리켜야 한다.** x 축 설정에만
`id: 'date'` 를 주고 시리즈에 `xAxisId` 를 안 달았더니 CDS 가
`xAxisId ?? DEFAULT_AXIS_ID` 로 스케일을 찾다 빗나갔고, `Line` 이
`if (!xScale || !yScale || !path) return` 으로 **아무 말 없이 사라졌다**. 축과 눈금은
멀쩡히 그려지므로 "선만 없는 차트" 가 되고 콘솔에는 한 줄도 안 남는다. x 축은
하나뿐이니 이름을 주지 않는다(`guards/reference-lines.test.ts` 가 감시).

### 8.5b 커서 리드아웃은 **그림 밖 한 줄**이다 [OWNER 2026-09-21]

떠 있던 흰 카드를 걷고 그림 **위**의 고정 줄로 옮겼다. 부품은
`ui/ChartReadoutStrip.tsx`, 가드는 `guards/chart-readout-strip.test.ts`.

**왜.** 트레이더 보고: 카드가 그림을 가린다. 실측이 그보다 나쁘다 — 카드는 세로
약 220px 인데 오버뷰 열의 그림은 200px, 백테스트 아래 차트는 140px 이라 «그림보다
큰 툴팁» 이었고, 최근 날짜를 짚을수록 가장 중요한 구간을 덮었다.

**고친 방향.** 자리를 더 잘 잡는 쪽이 아니다. 그 길로 가면 카드가 커서를 피해
다니고, 피해 다니는 것은 읽는 동안 글자가 움직인다는 뜻이다. 그림 **밖**으로
내보낸다.

- **그림 안에 hover 로 뜨는 DOM 이 없다.** 커서가 어디에 있는지는 라이브러리의
  축 라벨이 말한다(`crosshair.vertLine.labelVisible` · `horzLine.labelVisible`).
  캔버스가 자기 좌표로 그리는 것이라 가림이 구조적으로 불가능하다.
- **가로선은 안 그린다.** 그림을 가로지르는 선도 덧댄 것이다. 대가는 라이브러리가
  그 라벨만큼 축을 안 넓힌다는 것이고(라벨을 그리는 판정과 축 폭을 잡는 판정이
  서로 다르다 — 번들 실측), 폭은 `CROSSHAIR_LABEL_MIN_W` 로 우리가 잡는다.
- **구슬은 주선 하나.** 기본값이 계열마다 띄워서 최대 여덟 개였다.
- **줄은 안 움직인다.** 높이도 칸 폭도 고정이고 커서를 움직이면 글자만 바뀐다.
  칸 폭은 손으로 센 수가 아니라 **보이는 구간의 고·저를 그 칸의 서식에 통과시켜**
  잰다(`slotChars`).
- **쉴 때는 마지막 봉**(커브에서는 맨 오른쪽 노드)을 읽는다. «비어 있는 상태» 가
  없어야 줄이 생겼다 사라지며 그림 높이를 흔들지 않는다.
- **줄이 범례를 겸한다.** 견본(그려진 색) · 이름(끄는 손잡이) · 값이 한 칸에 선다.
  그림 아래의 칩 줄은 그래서 없어졌다 — 「지금 그려진 것과 끄는 손잡이가 한 자리에
  있으면 둘이 갈릴 수가 없다」는 그 범례의 존재 이유였는데, 정작 **값**이 그 자리에
  없었다.
- **좁아지면 값만 내려놓는다**(기준금리 → CD91 → MA20 → MA10). 칸을 통째로 떨구면
  좁은 창에서 그 계열을 켤 길이 없어진다. 날짜·레벨·당일 변화는 어느 폭에서도 남는다.
- **52주 최고·최저·평균은 줄에 없다.** 커서를 따라 안 바뀌는 값이 셋이나 서면 정작
  커서가 읽는 값들이 먼저 밀려난다. 표의 52주 열과 히어로 밑 통계가 이미 적는다.
  되살리는 자리는 `SHOW_52W_IN_STRIP` 하나이고 **둘째 줄**이다.

**변화는 bp 다 — 레벨의 단위가 아니다.** 같은 수리에서 히어로가 고쳐졌다. 백엔드
(`derive.py::series_history`)도 표(`table/rows.ts`)도 변화를 bp 로 내는데, 접미사를
붙이는 일이 `unitSuffix`(= **레벨**의 단위)라는 별개 호출이라 호출부가 각자
기억해야 했다. 두 자리가 각자 틀렸다 — 카드는 `+2.2`(단위 없음), 히어로는
`+0.2%`(같은 양을 다른 단위로). 이제 `fmtDeltaUnit`/`deltaUnitSuffix` 가 그 규약을
지고, `unitSuffix` 는 레벨 옆에만 쓴다.

> 카드(`ui/ReadoutCard.tsx`)는 **은퇴하지 않았다**. 시뮬·rv·MR·Lab 의 열네 자리가
> 아직 쓴다. 그쪽 계약은 `guards/readout-card-width.test.ts` 가 계속 잰다. 옮길지는
> 표면마다 따로 볼 일이고, 자리가 좁은 그림(140~200px)부터가 후보다.

### 8.6 아무것도 안 고르면 — IRS 파 커브

빈 pane 에 "행을 고르세요" 를 적어두는 대신 커브를 그린다. 커브 보기가 이 제품의
1순위이고, **탭이 바뀌어도 같은 커브다** [v1 OWNER, pass M] — 탭마다 주제가 바뀌는
pane 은 기억할 것이 하나 더 늘어나는 일이고, 나머지 커브들은 옆 표가 이미 숫자로
찍고 있는 것을 알아보는 데 시간이 걸리는 모양으로 다시 말한 것이었다.

- **노드 14개, 3M…10Y.** `displayTenors`(8개, 6M…10Y)는 **벽면 표의 표시 집합이지
  커브가 아니다** — 짧은 끝과 보간 노드가 빠진다. 아웃라이트 전부를 `sortKey` 순서로
  세우고 **1D 만 뺀다**: 콜은 하루짜리 픽싱이지 스왑 노드가 아니고, 등간격 축에서
  1/365년 노드를 6M 옆에 세우면 40bp 낙차가 만기 한 칸으로 보인다.
- **선은 둘** — 데이터 일자와 전일. 여섯 기준 전부는 확대 뷰의 몫이다.
- **마디는 직선.** CDS 기본 `bump` 스플라인은 노드 사이를 지어낸다. 우리가 아는 것은
  노드의 값뿐이다.
- **변화는 백엔드 값**(`deltas.d1`). 화면의 두 레벨을 빼면 이미 반올림된 수의 차라
  표의 어제 열과 어긋날 수 있다(§16).

### 8.7 떠 있는 창 (레인 3)

백테스트(레인 4)와 시뮬레이션(레인 5)이 **둘 다** 쓸 껍데기. 자유는 하나 — **자리**.
크기 조절도, 최소화도, 두 번째 인스턴스도 없다(각각이 제품이 legible 해야 할 상태를
하나씩 늘린다).

- **끌 수 있는 면은 헤더뿐.** 본문 어디서나 끌리면 차트 위 드래그가 스크럽인지
  이동인지 모호해진다.
- **손잡이는 뷰포트를 못 벗어난다.** 본문은 화면 아래로 흘러도 되고 손잡이는 안 된다 —
  나가면 그 창은 되돌릴 방법이 없다(끌 수 있는 면이 헤더뿐이므로). 헤더 CSS 는
  `box-sizing: border-box` 여야 한다: 실측에서 테두리 1px 이 상수 밖에 있어 손잡이
  마지막 픽셀이 화면을 나갔다.
- **자리는 세션 기억, 창마다 따로.** localStorage 가 아니다 — 다른 모니터에서 복원된
  위치는 아무도 못 찾는 창이 된다. 창마다인 이유는 v1 이 모듈 변수 하나로 두었다가
  두 번째 창이 첫 창의 자리를 물려받아 열린 적이 있어서다.
- **Esc 는 한 겹만**(`escapeStack`). 맨 위가 아니면 아무것도 안 한다. 두 겹이 한 번에
  닫히면 작업하던 것까지 잃고, 안쪽이 이벤트를 삼키면 바깥은 영영 못 닫는다.
- **닫힘은 애니메이션에 의존하지 않는다.** 이 리포는 "안 닫히는 창" 을 두 번 겪었고
  원인은 둘 다 exit 완료 보고를 잃은 것이었다. 열림은 상태 하나이고 그 상태가 곧
  DOM 이다.
- **하단 서랍은 접힌 게 기본**이고 탭 바는 접혀 있을 때도 보인다(무엇을 펼칠 수 있는지가
  접힌 상태에서 읽혀야 발견된다). 빈 탭도 **눌린다** — 없다는 사실을 열어서 읽는다.
- 층은 `ui/window/layers.ts` 한 곳이 정한다. 창은 크롬 위, 모달 아래.

**첫 세입자는 확대 창**(`?w=`): 같은 pane 을 크게 그리고, 통계는 서랍으로 내린다.
기구만 만들고 아무도 안 살면 브라우저에서 검증할 방법이 없고, 그러면 레인 4·5 가 그
위에 올라앉는 날 처음으로 시험받는다.

### 8.8 백테스트 (레인 4)

"그때 들어갔으면 지금 얼마였을까". 레인 3 의 창 위에 산다.

- **스스로 실행되지 않는다.** 사람이 실행을 누른다. 한 번이 서버에서 하루 단위 전면
  재평가이고, 날짜를 타이핑하는 중에 일어날 일이 아니다.
- **북이지 한 거래가 아니다** — 열두 줄까지, 줄마다 진입·청산이 따로 논다.
- **담을 수 있는 것은 아웃라이트·스프레드·플라이뿐.** 포워드는 엔진이 거부하고
  (`_validate` 가 'x' 를 전부 막는다) 변동성·민평·선물은 스왑이 아니다. **서버가
  거부하는 것을 화면이 제안하지 않는다.**
- **방향을 부르는 법**: 아웃라이트 페이/리시브 · 스프레드 스티프너/플래트너(다리
  병기) · **버터플라이는 용어를 지어내지 않고 다리만 적는다**(업계 합의가 없다 —
  Clarus 와 다른 데스크가 반대로 쓰고 TraditionData 는 아예 그렇게 경고한다).
- **헤드라인은 북 합계, 차트는 그 선 하나.** 포지션별로는 선이 아니라 숫자다 — 한
  축에 서너 곡선은 아무도 안 읽는다.
- **3분해는 표시 정밀도에서 합계와 맞아야 한다**(`splitKrw`). 실측으로 걸렸다:
  세 항목을 각자 반올림하니 −39,971 + 29,148 + 4,696 = −6,127 인데 헤드라인이
  −6,128 이었다. 이 리포는 같은 1만원 거짓말을 이미 한 번 출하했다.
- **진입 레벨은 실행 전에도 보인다.** 타이핑한 날짜가 실제로 어느 레벨에 꽂히는지가
  누르기 전에 읽혀야 한다. 스냅 규칙은 서버와 같은 것(그 날짜 이후 첫 관측) 하나뿐 —
  둘이면 한 날짜에 진입 레벨이 두 개 뜬다.
- **기계는 접어 둔다.** 다리별 노셔널·DV01 은 두 번째 질문의 답이라 `자세히` 안에
  있다. 크기에는 부호를 붙이지 않는다(250억에 `+` 가 붙으면 손익처럼 읽힌다).
- **링크가 곧 북**(`?bt=`). 인코딩은 서버가 읽는 그 문자열 하나뿐이고 왕복을 가드가
  본다 — 깨지면 붙여넣은 링크가 남의 북을 연다.
- **라이브 백엔드 전용.** 다른 화면은 구운 JSON 을 읽지만 이 답은 입력에 달렸다.

### 8.9 시뮬레이션 (레인 5) `[검증됨]` 2026-08-14

"이 포지션을 이 금리 경로에 두면 어떻게 되나".

#### 형태는 v1 **실물**에서 왔다

첫 판은 v1 의 **소스만** 읽고 만들었다. 계약(페이로드·부호·규칙)은 맞았지만 형태는
지어낸 것이었다 — 설정을 떠 있는 창에 넣고 결과를 그 아래 붙였는데, v1 은 정확히
반대다. 실물을 열어 보고 고쳤다:

| | v1 실물 | 첫 판 |
|---|---|---|
| 설정 | **전체 페이지** — 좌측 카드 열 + 우측 커브 미리보기 | 떠 있는 창 안 |
| 결과 | **떠 있는 창** | 같은 창 아래쪽 |
| 케이스 | Base/Bull/Bear/Crisis **넷이 한 번에** | "나중에" |
| 목표 | **국고 기둥 + 앵커 테너** | IRS 3Y 이동 |
| 결과 본체 | 워터폴 + 성분 누적 3선 | totalPnL 1선 |

그 배치에는 이유가 있다: 설정은 **오래 만지는 것**이라 넓은 자리와 옆의 미리보기가
필요하고, 결과는 **읽고 닫는 것**이라 떠 있어야 뒤의 설정을 다시 만질 수 있다.
(결과 창의 「조건 수정」 버튼이 그 왕복을 이름 붙인 것이다.)

**교훈: 계약은 소스에서 읽고 형태는 화면에서 읽는다.** 소스만 읽으면 "무엇을
보내는가" 는 맞추고 "무엇을 보여주는가" 는 발명하게 된다.

#### 규칙

- **줄 하나 = 상품 하나이지 스왑 다리 하나가 아니다** [v1 OWNER 2026-08-07].
  다리로 펴는 것은 서버다(`/api/instruments/expand`) — DV01 중립 가중에는 커브가
  필요하고 브라우저는 계산하지 않는다(§16). 다리 규칙은 백테스트의 `_legs_for` 와
  **같은 것**이라 두 화면이 같은 "3s10s" 를 같게 이해한다.
- **편 결과를 실행 전에 보여준다.** 1s2s 100억의 다리는 2Y 100억 / 1Y 196억이지
  반반이 아니다.
- **부호는 층이 둘이고 뒤집는 곳은 하나다.** 줄은 `+1 = 호가값을 롱`, 다리는
  `+1 = 고정 수취`. 뒤집기는 백엔드 `instruments.expand` 한 곳이다(실측 확인).
- **par 커브는 안 싣는다**(`irsCurves: []`). 백엔드가 기준일 스냅샷에서 읽는다.
- **짧은 끝은 기준금리 이벤트만 움직인다.** 이벤트가 없으면 1D·3M 마디의 최종 값이
  0 이다 — 기본 시나리오는 "기준금리는 그대로인데 3Y 가 오른다" 이지 평행이동이
  아니다. 원화에서 오버나이트는 기준금리를 따라가고 그건 금통위가 정한다.
  회의 일정은 **백엔드가 낸다**(`policy.upcoming`) — 브라우저에 세 번째 사본을
  두지 않는다. `<datalist>` 라 고르는 것을 돕되 막지는 않는다.
- **`shockMode` 는 언제나 `matrix`.** `parallel` 은 엔진이 swapCurve 를 통째로
  버린다. 실측 차이(3Y 페이 100억·90일·+50bp): **18.9만원**.
- **목표는 국고 기둥에서 말하고 전선에는 3Y 로 환산해 싣는다**(`toWireBp`).
  트레이더는 "IRS 3Y 커브 노드" 가 아니라 "국고 10Y 가 30bp 오르면" 이라고 말한다.
  환산이 상쇄돼 0 에 가까워지면 **막는다** — 조용히 0 으로 떨어뜨리면 설계한
  시나리오와 돌아간 시나리오가 갈린다.
- **실행 하나가 네 케이스를 전부 돌린다** [v1 OWNER 2026-08-10]. 넷이 병렬로 나가고
  **하나라도 실패하면 전체가 실패다** — 셋만 있는 결과 화면은 "비교" 라는 목적을
  반쪽으로 만든다. 케이스가 갖는 것은 목표·스프레드·이벤트뿐이고 기간·앵커는
  공유한다(케이스를 갈아 끼울 때 기간까지 따라 움직이면 비교가 아니다).
- **미리보기와 실행은 같은 함수를 부른다**(`caseShockCurve`). 둘이 갈리면 화면이
  보여준 시나리오와 돌아간 시나리오가 다르고, 그게 미리보기가 막으려던 실패다.
- **케이스 색은 뜻과 맞춘다** — Bull 은 금리 하락이라 파랑, Bear 는 상승이라 빨강.
  원화 관례와 싸우지 않는 유일한 배치다. 한계: 사용자가 Bull 의 목표를 +100 으로
  고치면 색이 이름과 어긋난다. 색은 **씨앗의 뜻**을 말하고 실제 방향은 칩 옆의
  숫자가 말한다. 회색조에서 넷을 못 가르므로 칩이 항상 이름을 같이 싣는다.
- **`--color-chart1`…`4` 는 없는 토큰이다** `[검증됨]`. 첫 판이 그걸 썼고, 무효값이라
  브라우저가 상속색으로 떨어뜨려 **네 선이 전부 같은 회색**이 됐다. CDS 가 심는
  `--color-*` 43개에 chart 계열은 없다 — 있는 것은 `accentBold{Green,Blue,Purple,
  Yellow,Red,Gray}` 다. 아무것도 안 깨져 보이는 그 결함이고, 이 리포가 폰트·면
  토큰에서 이미 세 번 밟았다.
- **시리즈 id 는 겹치면 안 된다** `[검증됨]`. 기준 커브와 Base 케이스가 둘 다
  `base` 였고, CDS 가 하나만 남겨 케이스 선이 조용히 기준 커브를 다시 그렸다
  (실측: 두 `<path>` 의 `d` 가 완전히 동일). 지금은 `now` / `case:*` 다.
- **경로 설계는 D+0 과 마감일을 고정 핀으로 두고 30일 간격의 중간점만 연다.**
  시작은 0 이고 끝은 목표 금리 칸이 정한다. 손댔는지는 **명시 플래그**(키의 존재)로
  안다 — "지금 직선 위에 있으니 안 손댄 것" 으로 추론하면 우연히 직선에 놓인 편집이
  목표를 바꾸는 순간 지워진다. 걸음(5bp)은 목표 칸과 같다: 다른 눈금을 쓰면 손으로
  맞춰 놓은 경로가 목표를 한 번 누를 때마다 어긋난다. 클램프는 ±max(|목표|+50, 100).
- **안 손댄 경유지는 페이로드에 안 싣는다** `[검증됨]`. 화면은 그 자리에 직선 위
  기본값(0.1bp 반올림)을 **보여주지만** 그 값을 보내면 안 된다. 실측(3Y 페이
  100억·180일): 정확 격자는 두 점 직선과 **원 단위까지 같은데**, 0.1bp 로 반올림한
  격자는 −50bp 에서 +1.50%, +100bp 에서 −2.91%, +250bp 에서 −2.39% 다르다. 엔진에
  "이 경로가 직선인가" 를 1e-9 로 재는 판정이 있어, 반올림된 점 하나가 시나리오를
  통째로 다른 갈래로 보낸다. **보여주는 값과 싣는 값이 다르다**가 이 화면의 규칙이다.
- **다리별 par 덮어쓰기** [v1 트레이더 피드백 3, 2026-08-07]. par 가 있던 자리가
  그대로 입력칸이 된다 — 새 칸을 만들지 않는 이유는 그 줄이 이미 "이 다리는 이
  금리로 쳐진다" 를 말하고 있었기 때문이다. 덮어쓴 줄은 **진입 MtM 이 0 이 아니라고
  말한다**: par 진입과 달리 결과의 평가손익에 경로가 만들지 않은 몫이 처음부터
  섞여 있고, 그걸 모르면 숫자를 잘못 읽는다. 되돌릴 때 par 가 얼마였는지도 같이
  적는다. 상품을 바꾸면 비운다 — 남기면 3s10s 의 3Y 금리가 2s5s 의 2Y 다리에
  조용히 붙는다. 덮어쓰는 곳은 **한 곳**(`applyRateOverrides`)뿐이다: 화면과 요청이
  각자 적용하면 보이는 금리와 평가되는 금리가 갈라진다.
- **숫자 칸은 타이핑 중에 파싱하지 않는다** `[검증됨]`. `onChange` 에서 바로
  `Number()` 를 부르면 `"-"` 는 0 이 되고 `"4."` 는 4 가 되어 되쓰인다 — **음수 목표를
  칠 수 없었다.** 커밋은 blur/Enter 이고, 못 읽는 글자는 되돌린다(0 으로 바꾸면 사람이
  친 적 없는 값을 주장하게 된다).
- **`TextCaption` 은 대문자 라벨이다** `[검증됨]` — `text-transform: uppercase`.
  한글은 안 바뀌어서 오래 안 보였는데 라틴 소문자가 들어가는 순간 드러난다:
  "금리를 par에서 옮겼어요" → "PAR에서", "bp · D+180" → "BP · D+180". 문장과 단위는
  `TextLegal` 이다.
- **다리별 기여는 헤드라인과 표시 정밀도에서 안 닫힌다** [OWNER 2026-08-14 — 그대로].
  각 줄은 그 다리에 대한 사실이고, 합을 맞추려고 한 다리의 값을 1만원 비트는 것은
  다른 종류의 거짓말이다. 닫혀야 하는 것은 3분해이고 그건 닫힌다.

#### v1 과 대사한 실측 (같은 조건: 3Y 페이 100억 · D+180 · 국고 3Y +30bp)

```
            v1 실물                    v2
Base        +1,367만                   +1,367만        ✓
Bull        −1.8억                     −1억 7,802만    ✓
Bear        +1.8억                     +1억 7,826만    ✓
Crisis      +5.2억                     +5억 2,129만    ✓
스왑평가     +7,143만                   +7,143만        ✓
스왑캐리     −4,472만                   −4,472만        ✓
스왑롤다운   −1,304만                   −1,304만        ✓
일별 대사 08-14  평가 +710,738 · 캐리 −249,315 · 롤다운 −87,370 · 그날 +374,053  ✓
```

### 8.10 창은 팝업을 자르지 않는다 `[검증됨]` 2026-08-14

CDS `Select` 의 드롭다운은 **포털이 아니다** — floating-ui 로 창(`offsetParent =
.sr-window`)을 기준 삼아 절대 배치된다. 그래서 창의 `overflow` 가 곧 목록의 상한이
된다. `overflow: hidden` 이 모서리를 위해 있었고, 그 대가로 **백테스트 종목 99개가
70px 잘려** 마지막 항목들을 고를 방법이 없었다. 눈에 잘 안 띄는 이유는 잘린 자리가
창 밖이라 "목록이 짧구나" 로 읽히기 때문이다.

- 창은 **안 자른다**. 아래 모서리는 `.sr-window > *:last-child` 가 진다 — 서랍이 있을
  때와 없을 때 바닥에 오는 것이 다르므로 몸통에 고정으로 주면 안 된다.
- 채운 면은 서랍의 탭 바 하나뿐이라 **서랍이 자기 안에서** 자른다. 거기서 자르는 것은
  안전하다: 드롭다운의 컨테이닝 블록은 창이지 서랍이 아니다.
- **드롭다운 폭은 `max-content`** (`ui/window/popup.ts`). 기본값은 컨트롤 폭인데, 값이
  토큰일 때(`10Y`) 맞는 기본값이지 이 제품의 목록은 문장이다 — "스티프너 (2Y 페이 ·
  1Y 리시브)". 137px 에서 "아웃라이트" 가 두 줄로 접혀 행 높이가 36 → 60 이었다.
  컨트롤을 키우면 줄이 화면을 넘으므로 **목록만** 넓힌다(목록은 떠 있어서 줄을 안 민다).

### 8.11 일별 대사 — 하루가 가로줄 셋 `[검증됨]` 2026-08-14

[v1 OWNER, 2026-08-11 — *"탭 1개에 KRD·Bp변화·PnL변화를 몰아야 … 1일차 KRD, BP변화,
PnL를 각각 가로줄로 구성해서 쌓아서 80일치면 240개의 가로줄"*]

**백테스트 창과 시뮬레이션 창이 같은 컴포넌트를 쓴다** — 트레이더 피드백 5 가 요구한
것이 "둘 다에 존재" 였고, 껍데기가 두 벌이면 그 문장이 곧 거짓이 된다(`WindowDrawer`
전례). 다른 것은 어댑터 둘과 기본 정렬 방향뿐이다.

- **하루 = `<tr>` 셋**: KRD(전일 종가 감도 — 추정이 곱한 바로 그 값) · Δbp(그날 변화) ·
  손익(둘의 곱, 선형 추정). 같은 블록 안에서 곱셈으로 닫히므로 눈이 전일 블록으로
  오갈 일이 없다. 렌즈 토글(한 번에 한 지표)은 은퇴했다 — 트레이딩 시스템과 줄 단위로
  맞춰 보는 것이 목적인데 시스템 쪽은 셋이 같이 보인다.
- **원 단위 그대로** [v1 OWNER, 2026-08-10]. 이 제품에서 억/만을 안 접는 **유일한**
  표다: 24,141 이 "2만원" 이면 시스템의 24,141 과 맞는지 말할 수 없다. 자릿수가 곧
  판단이다.
- **전 테너 열이 그대로 선다** [v1 OWNER, 2026-08-12]. 0 인 열은 — 로 선다 —
  리스크가 없다는 사실도 대사의 일부다.
- **범례는 사방 고정** — 헤더는 위, 날짜·구분은 왼쪽, 합계~그날 손익은 오른쪽.
  격자 가운데만 흐른다. 실측: 가로 600 세로 200 을 굴려도 네 변이 그대로다.
- **한 셀 한 채널** — KRD 줄은 배경(부호=색조, 크기=농도, 글자는 잉크), Δbp·손익 줄은
  글자색. 틴트 위의 방향색 글자는 **어떤 농도에서도** 4.5:1 을 못 넘는다(v1 실측:
  30%→3.0, 42%→2.5, 62%→1.8). 농도 조절 문제가 아니라 범주적 규칙이다.
- **기본 정렬이 반대다**: 백테스트는 최신이 위(실제 이력 — 대사는 어제·오늘부터),
  시뮬은 D+0 이 위(미래 경로에는 "최신" 이 없다). 날짜 헤더가 토글이다.
- **이월 앵커** — 마지막 블록은 종가 KRD(다음 영업일로 들고 가는 리스크)만 싣고
  손익은 전부 —. 아직 오지 않은 날의 손익을 0 이라고 말하지 않는다(공란 정책).
- **어댑터는 이름만 바꾼다.** 산술이 없다 — 두 번째 정의를 만들면 화면과 서버가
  갈릴 자리가 하나 늘어난다.

실측으로 닫힌 문장(시뮬 3Y 페이 100억·180일): KRD 합계 −2,581,613 = 테너 합 ·
손익 합계 713,195 = 테너 합 · KRD × Δbp ≈ 손익(표시 라운딩 안에서) · **평가 710,738 +
캐리 −249,315 + 롤다운 −87,370 = 그날 손익 374,053** (정확).

### 8.12 창은 화면 아래를 넘지 않는다 `[검증됨]` 2026-08-14

조각마다 상한을 둬도 합은 안 지켜진다: 머리 44 + 몸통 70vh + 서랍(탭 32 + 몸통 38vh)
= **108vh + 76px**. 백테스트 창에서 일별 대사를 펴자 창이 1,014px 이 됐고 화면은
911 이었다 — 표의 아래 175px 이 화면 밖이고, 손잡이 클램프가 `top ≥ 0` 이라 끌어올릴
수도 없었다. (v1 의 서랍 주석이 예고한 실패다: *"창이 서랍 때문에 화면 밖으로 자라면
손잡이 클램프가 무의미해진다."*)

- 상한은 **자기 자리에서** 잰다: `calc(100vh − top − 24px)`. 아래로 끌어둔 창일수록
  남는 높이가 적다.
- 상한에 닿으면 **몸통이** 준다(`min-height: 0`). 서랍이 아니라 몸통인 이유는 서랍이
  방금 사람이 편 것이기 때문이다.
- **스크롤러는 하나여야 한다.** 서랍 → 대사 스택까지 사슬의 모든 고리가 `min-height: 0`
  인 열 flex 다. 하나라도 블록이면 안쪽이 안 줄고 밖으로 삐져나가 스크롤러가 둘이
  되며, 그 순간 스택의 사방 고정 범례가 깨진다(실측: 서랍 219px 안에서 스택 273px).

### 8.13 화면은 창을 채운다 `[검증됨]` [OWNER 2026-08-14]

전면 관찰에서 나온 것들. 2560×1140(오너 실기기 폭)에서 잰 값이다.

- **페이지는 뷰포트에 묶인다** — `height: 100vh` + `overflow: hidden`. `minHeight`
  로는 flex 가 나눌 **정해진** 높이가 없어서, 재서 내려준 높이가 내용을 키우고 그
  내용이 다시 더 큰 측정값을 만드는 되먹임이 생긴다(실측: 오버뷰 카드가 1,214 화면
  에서 **2,106px** 까지 자랐다). 잘리지 않는 이유는 안쪽이 각자 스크롤하기 때문이다.
- **표와 차트는 남는 높이를 전부 쓴다.** 상수 560 이었고 아래 **428px 이 빈 채**
  표가 1,022px 짜리 내용을 8행만 보여주며 자체 스크롤했다. 지금은 표 949 · 차트 807.
- **높이 측정은 콜백 ref 다**(`ui/useFillHeight.ts`). `useRef` + `useLayoutEffect`
  는 **한 번도 안 쟀다**: 이 페이지는 데이터가 오기 전에 마운트되므로 그때 카드가
  없고, 카드가 나중에 생겨도 의존성이 안 바뀌어 효과가 다시 안 돈다.
- **`ResizeObserver` 만 믿지 않는다** — 렌더링이 멈춘 탭에서는 그 콜백이 한 번도 안
  온다(이 리포가 rAF 에서 겪은 자리와 같다). 붙는 즉시 `clientHeight` 로 한 번 잰다.
- **설정 열 폭은 화면을 따라간다** — `clamp(360px, 24vw, 480px)`. 2560 에서 360px 은
  화면의 14% 이고 그 폭에서는 방향 세그먼트와 다리 줄이 아래로 접혔다. 상한이 480 인
  이유는 입력 칸이 더 넓어져도 안 읽히기 때문이다 — 넓어져야 하는 건 커브 쪽이다.

### 8.14 서체는 **한 벌**이어야 한다 `[검증됨]` 2026-08-14

`body` 에 `font-family` 를 안 정하면, 이 앱이 직접 만든 요소(상단 내비 버튼·일별 대사
표·날짜 칸·서랍 탭)가 **브라우저 기본 한글 폰트**로 그려진다 — `<html lang="ko">`
라서 크롬이 자기 한글 기본값을 쓴다. 실측: 표의 숫자(CDS `TextLabel2`)는 Pretendard
인데 상단 내비의 "Main·Backtest" 는 **Noto Sans KR** 이었다(폭 79.06 vs 78.08).

`--fontFamily-*` 매핑과 **다른 일**이다: 저건 CDS 컴포넌트가 읽는 토큰, 이건 상속으로
내려가는 실제 face. 둘 다 있어야 화면의 서체가 하나가 된다. 폰트 판정은 이름이 아니라
`canvas.measureText` 폭 대조로 한다 — 이름은 스택의 첫 항목일 뿐이다.

### 8.15 Main 은 세 열이다 `[검증됨]` [OWNER 2026-08-14]

전에는 Main 이 Backtest 의 아웃라이트 표를 그대로 보여줘서 **같은 화면이 둘**이었다.
규칙은 v1 그대로다(`ui/OverviewColumns.tsx`):

- **주요 아웃라이트 · 주요 스프레드 · 주요 포워드** 셋. 버터플라이와 변동성은 일부러
  없다 — 세 열이라는 것이 요점이고, 넷째·다섯째를 붙이면 이건 다시 목록이 된다.
- **표의 격자를 그대로 쓴다**(`InstrumentTable`). v1 이 전용 격자를 따로 짰다가 한
  세션에 두 번 어긋난 뒤 되돌린 자리다. 폭이 좁아 열이 빠지는 것은 v2 의 우선순위
  사다리가 알아서 한다.
- **한 열이 한 차트를 갖는다.** 어느 행을 누르면 그 열에서만 그려지고, 각 열은 자기
  첫 행으로 열린다 — 자리가 비어 있지 않고 무엇을 할 수 있는지가 보인다.
- **차트 높이 고정 200** [v1 OWNER] — 숫자가 주인공이고 커브는 각주다. 고정이면
  "세 차트 한 높이" 가 구성상 참이 되어, 재서 맞추던 장치가 필요 없다.
- 표에서 빼야 하는 것은 차트 200 이 아니라 **pane 전체 350** 이다(히어로+기간+여백).
  차트만 빼면 카드가 210px 넘쳐서 커브가 카드 밑에서 잘린다.

### 8.16 상단 내비 — 글자는 상자 가운데에 선다 `[검증됨]` 2026-08-14

**두 참조를 다시 재서** 고친 것들(같은 날, coinbase.com/price/bitcoin · tossinvest.com).

- **정렬**: 항목이 `display: inline-flex; align-items: center` 여야 한다. 없으면
  버튼(40px)과 CDS 타이포그래피 span 의 줄상자(24px)가 어긋나 글자가 상자 위에
  붙고, **글자 중심이 상자 중심보다 8px 위**로 간다. 실측: 브랜드는 32, 내비 항목은
  24 였고 그 차이가 그대로 보였다. 두 참조가 같은 방법을 쓴다 — Coinbase(44px,
  `display:flex`) 34 == 34, 토스(36px, `inline-flex`) 26 == 26. **상자 중심과 글자
  중심이 같다**가 규칙이다.
- **굵기는 안 바꾼다**: Coinbase 내비 라벨 여덟 개가 전부 **16px/600** 이다(전수
  측정). 앞서 한 개만 재고 400 이라고 읽었던 것은 텍스트 노드가 아니라 래퍼 `<a>`
  였다 — n=1 로 규칙을 바꾸지 않는다.
- **화면 모드는 아이콘 버튼**이다. 글리프 문자(`☾`)를 내비 라벨과 같은 알약에 넣으면
  시각 중심도 크기도 어긋난다(문자의 중심은 라틴 글자와 다르고 크기는 폰트가 정한다).
  토스는 이 자리에 **32×32, radius 8, 옅은 틴트** 아이콘 버튼을 둔다. CDS 에
  `sun`/`moon` 이 있다.

### 8.17 메가 패널은 뒤를 덮는다 `[검증됨]` 2026-08-14

**Coinbase 실측**: 패널이 열리면 `rgba(50, 53, 61, 0.33)` 이 바 아래 **전체**를 덮는다.

덮개가 없으면 패널이 "패널" 로 안 읽힌다 — 흰 시트 위의 글자와 그 아래 표가 같은
평면에 있는 것처럼 보이고, 실제로 v2 가 그 상태였다("패널은 다 어디갔어"). 덮개는
장식이 아니라 **"지금은 이 메뉴가 주인공"** 이라는 정보다. 색은 `--sr-scrim` 이고
다크에서는 더 진한 검정이다 — 어두운 바탕에 33% 회색은 아무것도 안 한다.

같이 걸린 것: **`.sr-mega-inner` 가 631px 이었다**(패널은 2,560). `.sr-mega` 가 CDS
`Box` 이고 **CDS Box 는 기본이 `display: flex`**(§5.4 함정 1)라 안쪽 줄이 flex
아이템이 되어 내용만큼만 넓어졌다. 그 탓에 오른쪽 설명 카드가 항목 바로 옆에 붙어
있었다 — 참조는 그걸 시트의 오른쪽 끝에 둔다. `width: 100%` 가 답이고, 내비 바가
같은 이유로 이미 그걸 지고 있었다.

### 8.4 미리보기는 포인터를 따라간다 — 120ms

행 위에 머물면 그 행이, 없으면 고정한 행이 그려진다. **hover 는 URL 에 안 쓴다**
(목적지가 아니다). 지연은 도착에만 걸고 이탈은 즉시다 — 열두 행을 가로질러 열세
번째로 갈 때 열두 번 요청하고 차트가 깜빡이는 걸 막는 것이지, 떠난 행을 계속 보여줄
이유는 없다. 포커스도 같은 경로다(키보드 = 마우스). 표는 **행이 바뀔 때만** 알린다 —
`mouseover` 는 자손마다 오므로, 안 걸러내면 뒤의 타이머가 매번 리셋돼 영원히 안 끝난다.

---

## 미해결

- ~~**숫자 셀의 굵기**~~ — **400 으로 확정 [OWNER 2026-08-14].** 즉 CDS 기본값
  그대로이고 `sauronTheme.fontWeight` 에는 `legal: '500'` 하나만 남는다. 토스 재측정이
  "토스 표 본문 = 600" 을 보였지만 오너는 Coinbase 스케일 쪽을 유지하기로 했다 —
  §2 의 「나머지는 Coinbase」 지시와 같은 방향이다. 되묻지 말 것.
- **스파크라인 열** — `Sparkline` + `fillType="dotted"` 로 갈 수 있으나 행마다
  히스토리가 필요하고 `Row` 에는 없다(`seriesId` 만 있고 `fetchSeries` 는 행당 1회).
  77행이면 77요청이라 데이터 계층 결정이 먼저다.
  참고로 **Coinbase 는 행마다 64×32 SVG 스파크라인을 그린다** `[검증됨]` — 열 자체는
  실제 제품의 관례가 맞다. 문제는 디자인이 아니라 데이터 조달이다.
- **§5.2 `WIDEST.label`** 의 정직한 수리.
- **가로 공간** — 원인이 밝혀졌고(§5.7 flex 반반) basis 로 고쳤다. 1920 화면에서
  표 980 · 미리보기 876 이고 숨는 열은 없다. **오너 화면 2560 에서는 아직 안 봤다** —
  거기서는 미리보기가 1500 을 넘게 받는데, 그 폭에서 차트가 어떻게 보이는지가 다음
  질문이다(표에 캡이 있으므로 남는 폭은 전부 미리보기로 간다).
- **1366 급 화면** — 미리보기 바닥(380)에 먼저 닿고 표가 922 로 줄어 세타가 떨어진다.
  규칙대로지만 실기기에서 본 적은 없다.
