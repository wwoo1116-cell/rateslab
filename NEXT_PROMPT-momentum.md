# 다음 세션 프롬프트 — 2026-09-09 인계 (Momentum 화면 · IRS 발견)

아래 블록을 통째로 붙여 넣으면 이어받을 수 있다.
세부는 `apps\sauron-v2\HANDOFF-momentum-2026-09-09.md` 한 장에 다 있다.

---

## 붙여 넣을 블록

```
2026-09-09 세션을 이어받는다. 원화 금리 데스크, sauron-v2 리포다.

【건드리지 말 것 — 동결】
· `data\krw-macro-vintage\PREREG.md` 가 2026-09-08 에 동결됐다. 채점 대상은
  2026-09-09 이후 영업일뿐, 판정은 채점 750영업일 또는 macro_sign 방향전환 12회 중
  나중이다. **판정일까지 들여다보지 말 것.**
· 그래서 Momentum 화면에 **채점 잠금**이 있다 — 동결일 이후 손익을 **서버가**
  세 다리 다 잘라서 낸다(`backend\app\momentum.py::build_book`). 추세만 남기면
  50/50 과의 차로 매크로를 역산할 수 있어 셋 다 자른다. 프런트에서 자르면 네트워크
  탭에 남으므로 자르는 자리가 서버여야 한다. `guards\momentum-prereg-lock.test.ts`
  가 그 불변식을 잰다. **풀지 말 것.**

【이 세션에서 지은 것 — Strategy 셋째 세입자 Momentum】
URL `?g=strategy&s=momentum`. 위계는 Credit RV·Mean Reversion 과 동일.
  backend\app\momentum.py            계산 전부(§16) · FREEZE 상수 · 채점 잠금
  backend\app\main.py                /api/momentum/{board,history/{key},book}
  backend\data\macro_themes_daily.csv  레인에서 복사(아침 굽기가 갱신할 자리)
  src\momentum\{api.ts,MomentumPage.tsx}
  src\table\tint.ts                  signalTint — **세 번째 눈금**(매트릭스 선례)
  src\lib\staticPaths.ts · src\ui\nav.ts · src\app\page.tsx
  guards\momentum-canon.test.ts(11) · momentum-prereg-lock.test.ts(5)
  guards\nav-strategy-tenants.test.ts  세입자 둘 → 셋
검증 — tsc 통과 · Momentum 가드 24칸 통과 · 백엔드 pytest 1,304 통과.
**자기 대조**: `/api/momentum/book` 이 `RESULT_cta_macro.md` 수를 그대로 낸다
(추세 0.53/0.30 · 매크로 0.51/0.22 · 50/50 0.61/0.49).

【오너 결정 — 뒤집지 말 것】
· 보드는 **두 다리 나란히** — 추세 다리(계약2 × 룩백5) + 매크로 다리(4테마) +
  50/50 한 줄. 등록된 북의 구조 그대로다.
· 칸은 **tint 채움(캐논)**. 0 중심 발산 막대 안은 검토했고 채택 안 함.
· 글리프 `↗`. · **「기존 v2 의 문법을 충실히 따를 것」**.
· **자본 분모를 안 만든다.** 퍼센트 대신 무차원 비율로 비교한다 —
  Sharpe·Calmar·**Martin**·위험맞춤 낙폭. 퍼센트가 필요하면 가정을 옆에 적는다.
· **Ulcer 는 MR 과 같은 잣대**(mrmetrics 식 = 낙폭 경로 RMS). **부호 안 뒤집는다.**
· **공용 모듈 mrmetrics 는 지금 그대로.** Momentum 화면은 자기 `_card` 를 쓴다.
  ⚠ 단 `scripts\cta_macro.py`(연구)는 mrmetrics 를 부른다.
· **IRS 편도 호가폭 = 0.5bp.** 오너가 정한 값이다(추정 아님, 다시 묻지 말 것).
· **IRS 는 대차대조표를 안 쓴다** — 파생이라 증거금·CSA 뿐, 선물과 같은 자리.
· 지평은 2016 부터.

【★★★ 이 세션 최대 발견 — 선물이 추세의 절반을 흘린다】
트레이더 지적 「모멘텀이 잘 나오는 건 연결선물 롤링 때문일 수 있으니 IRS 로
확인하라」에서 시작했다. 코드 `scripts\momentum_roll_check.py` · `momentum_irs.py`.

· **롤 탓이 아니다(기각).** 롤일 38일 손익 합 **−237만(총의 −2.6%)**, 날 수 비중
  1.6%, 롤일 σ 가 평시의 **0.45배**. 롤일 손익을 지우면 오히려 좋아진다
  (Sharpe 0.53→0.55). 벤더가 실제로 이었다 — 조정가 |Δ| 롤일/평시 비 0.50~0.57 인데
  내재금리 |Δ| 비는 1.46~2.75(롤갭이 내재에만 남아 있다).
· **그런데 IRS 로 세우면 훨씬 좋다.** `mkt_irs_close` par 금리를 **−bp** 계열로
  (합성가 금지 — 채권 가격식을 씌우면 IRS 가 아니다), 데스크 IRS 회계(bp × 명목)로:
      2017-01~   선물 0.53/0.30  대  IRS(0.5bp) 0.85/0.63
      2016-01~   선물 0.63       대  IRS        0.94
  일별 손익 상관 +0.892. 선물 비용을 가장 유리한 실측(0.43틱)으로 잡아도 0.54다.
· **원인은 롤도 비용도 계열 뭉개짐도 아니다.** 회전은 거의 같고(0.113 대 0.100),
  IRS 가 비용을 3.6배 더 무는데도(3,454만 대 958만) 이긴다 —
  **비용 전 손익이 18,278만 대 10,039만으로 82% 더 크다.**
  IRS 원계열 차분 AR(1) −0.006/−0.041 · Δ=0 인 날 4.8%/3.7% → 고시 뭉개짐 아니다.
· **남은 후보는 베이시스**(선물 내재금리 − IRS = 퓨처스왑). 이 리포에 유니버스가
  이미 있다. **다음에 열면 이것부터.**

【100억이면 얼마인가 — `scripts\momentum_irs_book100.py`】
2016-01-04 시작, 연 변동성 5%(=5억/년), 노는 현금은 콜금리로 복리.
      IRS   최종 177.3억 · 연 5.51% (캐리 1.92% → 초과 +3.58%p) · 자산 낙폭 4.4억(4.10%)
      선물  최종 159.3억 · 연 4.46% (초과 +2.53%p)              · 자산 낙폭 7.8억(7.06%)
· 첫 포지션 2016-04-26(그 전은 룩백 워밍업이라 캐리만).
· 지난 1년 거래 **9건 중 2건이 전부** — 3Y 페이 2025-09-24~2026-08-13 (2.45→3.84%)
  +4.80억, 10Y 페이 2025-09-25~ 진행 중 (2.73→4.13%) +4.64억. 나머지 7건 합 −1.00억.
  승률 22%에 이긴 둘이 진 일곱을 9배로 덮는 **교과서적 추세 모양**이다.
· **레버리지는 격차를 못 메운다** — Sharpe 는 레버리지로 안 변한다. 그리고 이
  위험 수준에서 선물 증거금이 **평균 9.99억·최대 53.66억(자본의 53.7%)** 이라
  3Y 20배·10Y 10배 여력이 넉넉하지 않다(최대 명목 856억 = 총레버리지 8.6배).
· ⚠ **IRS 증거금(CCP IM·CSA)은 안 쟀다.** 그래서 「IRS 가 자본을 덜 쓴다」고
  말하면 안 된다. 그리고 위 표는 캐리를 양쪽 다 100억 전액에 붙였다 — 선물
  증거금을 대용증권으로 못 내면 선물 쪽이 더 나빠진다.
· 선물의 진짜 강점은 자본이 아니라 **운영**이다(거래소·익명·신용라인 불필요).
  IRS 는 ISDA/CSA·상대방 한도가 붙고 DV01 805만원/bp 를 매일 조정해야 한다.

【★ 아직 안 푼 불일치 — 이게 제일 걸린다】
2026-09-08 에 이 데스크가 브레드스를 재며 「시장당 Sharpe 0.53·상관 0.155 에서
**N=2 면 0.64 가 산술 상한**」이라고 못박았다. 그런데 IRS 추세북이 **0.94** 다.
게다가 IRS 3Y·10Y 는 같은 커브라 상관이 선물보다 **더 높을** 텐데 Sharpe 는 더 높다.
**둘 중 하나가 틀렸다.** 좋은 소식이 아니라 확인할 자리다.

【채점 재점검 — `scripts\momentum_audit.py` · `lane_compare.py`】
MR 레인 Phase 0 진단 다섯 항목을 이 레인에 걸었다. **넷은 구조가 달라 안 걸린다**
(거래 목록이 없고 · Ulcer 부호를 안 뒤집고 · 북 변동성 목표가 위험을 고정하고 ·
낙폭 절반초과가 6~7건). **하나는 걸렸고 고쳤다** — 세 다리의 실현 σ 가 달라
(1.14·0.73·0.81) 원화 낙폭을 그냥 못 비교한다. 위험 맞추면 50/50 개선이 −50% 가
아니라 **−29%** 이고 매크로 낙폭은 추세보다 **더 크다**(4,303 대 3,189만).
→ `maxDrawdownVolMatched`·`ulcerVolMatched` 신설, 카드에 열이 섰다.

교차 비교(공통 창 2020-01-02~2026-09-07, 1,640일): 그냥 놓으면 **창·위험·자기상관**
셋이 어긋난다. 세 번째가 새 발견 — **MR AR(1) +0.210 대 Momentum −0.15**.
√252 곱셈은 날들이 독립일 때만 맞으므로 Lo(2002) 보정을 둘 다에 건다:
      Sharpe → AR보정   MR 1.75 → 1.42 · Momentum 50/50 0.53 → 0.62
성질 넷은 Momentum 이 낫다(자기상관 · 초과첨도 10.3 대 **68.8** · 낙폭 표본 ·
비용 쿠션 14.6배 대 5.04배). **수준은 아직 못 읽는다** — MR 장부가 증거금 상한
무시판이라(SR 1.74, 상한 아래 0.8) 성질만 읽었다.

【규약·함정 — 이 세션에서 배운 것】
· ★ **이 앱의 UI 문법은 해요체다**(MR — 「늘어남을 재는 화면이에요」). 한다체로
  쓰면 틀린다. 명구 의무는 세 세입자가 같은 문법으로 진다.
· ★ **룩백은 손잡이가 아니라 열이다.** 셀렉트로 두면 화면이 「룩백 고르지 마라」
  규율을 어기는 도구가 된다(가드가 `sr-pillbtn`·`<Select` 부재를 잰다).
· ★ **색은 신호 부호가 아니라 «금리» 방향이다.** 가격 상승 = 금리 하락이라 신호
  +1 은 파랑이다. 서버가 `rate = −signal` 을 같이 낸다.
· ★ **격자가 2행인 이유** — `signal_continuous` = 방향(규칙) × 강도(`trend_tstat`)
  인데 강도는 룩백에만 의존하고 규칙과 무관하다. 그리고 등록된 북의 추세 다리는
  **macross 하나**다(`cta_macro.py:145`) — tsmom·donchian 은 `cta_validate.py` 의
  PBO 격자 차원이지 북에 안 든다.
· ★ **두 다리를 곱하지 않는다.** 변조(추세×매크로)는 2026-09-09 NO-GO 였다.
· `barCost` 는 엔진이 이미 **문 금액(양수)** 으로 담는다. 부호를 뒤집으면 음수가 된다.
· 조정가는 **차분만 유효** → 상세 차트는 「차분 누적, 창 시작 100」이다.
· 매크로는 OECD 에디션 규약상 **하루 늦다** → 액면은 그 북의 **자기 as-of 날짜**를
  읽는다(마지막 칸을 읽으면 매일 0억으로 보인다).
· `test_static_agreement.py` 는 dev 서버 끄고 돌려야 한다(이 데스크의 알려진 함정).

【미결】
1. **베이시스가 선물–IRS 격차를 설명하는가** ← 다음에 열면 첫 번째
2. 브레드스 상한 0.64 대 IRS 북 0.94 불일치
3. IRS 증거금(CCP IM·CSA)·상대방 한도 실측
4. MR 수리 대기 [OWNER 「MR 부분 수리 끝나고 다시」] — 상한판(B/50) 위 재측정 ·
   비용 모델 파라미터화 · DSR 첨도 보정. 재개는 `python -m scripts.lane_compare` 한 줄.
5. **돌고 있는 :8200 은 다른 세션 것이라 새 라우트를 아직 안 문다.** 재시작 필요.
6. **아침 굽기에 CSV 복사를 물려야 한다** —
   `data\krw-macro-vintage\out\macro_themes_daily.csv` → `backend\data\`.
7. **커밋 안 함** (HEAD 74d8eb8f). 작업트리에 **남의 세션 것이 섞여 있다** —
   `src\mr\KnobBar·OptimizePane·StrategyWindow·parts` 수정 중이고 MR 가드 7건이
   그 탓에 빨갛다. 담을 땐 **경로 지정 스테이징만**(`git add -A` 금지).

【이 데스크의 상시 제약】
1. 국고 현물 공매도를 하지 않는다[오너]. 선물·IRS 가 양방향이 되는 자리다.
2. 브레드스 N=2. 방향성 상품이 3년·10년 둘뿐이다.
3. 선물 비용 편도 0.5틱(계약별 실측 3Y 0.43·10Y 1.20) · IRS 편도 0.5bp[오너].
4. 수준을 옮기지 마라 — 상한 무시판의 수를 상한판과 나란히 놓지 않는다.
5. 위험을 맞춘 뒤에 비교한다(Man 2016 Figure 7 규약).
```

---

## 이 문서를 쓴 이유

Momentum 화면은 다 지었고 검증도 끝났다. 그런데 **화면을 짓다가 나온 발견이 화면보다
크다** — 같은 신호를 IRS 로 세우면 Sharpe 가 0.63 에서 0.94 로 간다. 그 격차의 원인이
아직 안 밝혀졌고, 밝혀지면 「추세 다리를 무엇으로 세울 것인가」가 열린 결정이 된다.

그래서 이 인계문의 무게중심은 화면이 아니라 **베이시스 한 항목**에 있다.
