# -*- coding: utf-8 -*-
"""전략 **중립** 평가층 — MR·모멘텀·복합 장부를 같은 자로 잰다.

[OWNER 2026-09-09] 「전략 무관 평가층을 만들어라. 단 **진단 먼저, 수리는 그
다음**이다.」 Phase 0(진단)은 `HANDOFF-eval-framework-2026-09-09.md` 가 지고,
이 패키지는 Phase 2·3 이다.

## 왜 새 층인가

지금 성과 카드(Sortino·Calmar·Martin·Ulcer·GPR·Omega·PF)는 **평균회귀에 유리하게
기울어 있다** — 승률이 높고 왜도가 음수인 전략을 구조적으로 위로 올린다. 나중에
모멘텀·복합 장부를 같은 자로 재려면 그 자가 먼저 있어야 한다.

## 이 층이 지는 규율 (오너 원 지시)

- **Profit Factor·Omega 는 아예 안 낸다** — 둘 다 승률의 준-단조변환이다.
- **정규화 안 한 수익률로 비교하지 않는다** — 입력이 «비율» 이지 원(₩)이 아니다.
- **전략마다 다른 비용 가정을 쓰지 않는다** — MR 회전율이 추세추종의 5~20배라
  2bp 대 5bp 가정 하나가 순위를 뒤집는다. 그래서 `cost_bp_roundtrip` 이 입력에
  **적혀 나간다**(계산에 안 쓰더라도 무엇을 가정한 수인지 보고서가 말한다).
- **사전(ex-ante) 스케일링에 전체표본 통계 금지** — 확장창만 쓴다.
- 의존성은 numpy·pandas·scipy 뿐이다.

## 세 지표와 그 자리

    ① DSR (게이트)   왜도·첨도·표본길이·**시행수**를 동시에 보정한다. MR 과
                     모멘텀을 견줄 수 있게 하는 유일한 후보라 게이트다.
    ② PBO (게이트)   CSCV. 「고른 칸이 표본밖에서도 위쪽인가」를 잰다.
    ③ CDaR 비 (순위) MaxDD 는 단일관측 추정량이라 Calmar 를 대신한다.

①·②는 **AND 게이트**다. 하나라도 떨어지면 ③을 순위값으로 내지 않는다 —
`None` + 사유. 게이트를 통과한 전략만 순위가 매겨진다.
"""
from .metrics import (  # noqa: F401
    cdar_ratio,
    deflated_sharpe,
    evaluate,
    min_trl,
    pbo_cscv,
    vol_normalize,
)
