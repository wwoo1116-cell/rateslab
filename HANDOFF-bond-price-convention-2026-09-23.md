# 인계 — 원화 국채·통안 **단가 규약** (2026-09-23, K-Orderbook 레인에서 넘김)

금리 ↔ 단가 환산 규약을 실측으로 닫았다. v2 에서 현물 채권의 값을 «원» 으로
말해야 하는 자리(현물 북 평가·RV 비용·체결 마킹)에 그대로 쓰라.

---

## §0. 붙여 넣을 블록

```
원화 국채·통안 단가 규약. 정본 —
  Projects\apps\sauron-v2\HANDOFF-bond-price-convention-2026-09-23.md   (이 파일)

한 줄 요약 — 단가는 «관행적 복할인» 이다:
  P = [ Σ_k CF_k / (1+r/m)^k ] / (1 + r/m × d/D)       (액면 10,000)
  r = 수익률/m · d = 결제일→다음 이표일 실일수 · D = 그 이표기간 실일수

주기 m 은 테너가 아니라 «발행체 × 이자종류» 가 정한다 —
  국고·외평 이표채  m=2 (06M)      · 통안 이표채  m=4 (03M)
  통안 할인채(DC)   단리 365       · STRIPS·물가채는 이 식 밖(§5)

쿠폰·만기·주기는 DB 에 이미 있다(새로 물릴 자료 없음) —
  infomax.`국채_발행정보`    표준코드·종목명·만기일·표면이율·이자종류·이자지급기간
  infomax.`통안채_발행정보`  같은 칸
  ⚠`국채_발행정보` 489행 중 322행은 STRIPS 다. 실제 이표채는 167종.

정답지가 있다 — NICE피앤아이 Bond Market Daily 2026-09-23 「통안/국고채 단가조정
금리표」의 32행(당일결제 18 + 익일결제 14). 전부 |차| ≤ 0.010원 으로 닫혔다.
식을 고치면 이 32행을 먼저 다시 돌려라(§4 에 값이 통째로 있다).

규율 — 단가를 손으로 «대충» 환산하지 마라. 1원이 몇 bp 인지는 종목마다 다르다
(30년물 0.06bp, 2.7년물 0.38bp). 「1원 ≈ 1bp」 는 듀레이션 1년짜리에서만 맞는다.
```

---

## §1. 식

```
P = [ Σ_{k=0}^{n-1}  CF_k / (1+r/m)^k ]  ÷  (1 + r/m × d/D)
```

| 기호 | 뜻 |
|---|---|
| `CF_k` | 결제일 이후 k 번째 이표일의 현금흐름. 액면 10,000 기준 이표 = `10000 × 표면이율/100 / m`, 만기일에는 원금 10,000 을 더한다 |
| `m` | 연 이자지급 횟수 (§2) |
| `r` | `수익률(%) / 100 / m` |
| `d` | 결제일 → **다음** 이표일까지 실일수 |
| `D` | 그 이표기간(직전 이표일 → 다음 이표일)의 실일수 |

**앞은 복리, 마지막 토막은 단리.** 이표일은 **만기일에서 역산**한다(발행일 기준이
아니다 — 만기 2027-06-10 · m=2 면 …2026-12-10, 2027-06-10).

할인채(통안 DC 등)는 이표가 없으므로

```
P = 10000 / (1 + y × 잔존일수/365)
```

---

## §2. 주기는 «발행체 × 이자종류» 가 정한다 — 테너가 아니다

DB 를 그룹으로 세어 나온 것이지 기억이 아니다.

| 발행체 | 테너 | 이자종류 | 이자지급기간 | 종수 |
|---|---|---|---|---|
| 국고·외평 | 2·3·5·10·20·30·50Y | 이표채 | **06M** (m=2) | 156 |
| 국고 | 물가연동국채 | 이표채 | 06M | 11 |
| 국고 | 원금이자분리채권(STRIPS) | 할인채 | 12M | 322 |
| 통안 | **1Y·2.0Y·3.0Y 전부** | 이표채 | **03M** (m=4) | 185 |
| 통안 | 91·182일 등 단기(DC) | 할인채 | — | (마스터에 없음) |

**★통안을 반기로 놓으면 72원 틀린다.** 실제로 처음에 m=2 로 돌려 통안 두 종목이
`+72.28원`·`+63.26원` 으로 틀렸고, `이자지급기간='03M'` 을 읽고 나서 0.01원 안으로
들어왔다. 국고 감각을 통안에 그대로 옮기지 마라.

---

## §3. 자료 출처 — 새로 물릴 것이 없다

```sql
SELECT 표준코드, 종목명, 발행일, 만기일, 표면이율, 이자종류, 이자지급기간
  FROM infomax.`국채_발행정보`   WHERE 이자종류 = '이표채'     -- 167종
SELECT 표준코드, 종목명, 발행일, 만기일, 표면이율, 이자종류, 이자지급기간
  FROM infomax.`통안채_발행정보`                                -- 185종
```

접속은 `BW_MYSQL_USER/PASSWORD/HOST/PORT` 환경변수(이미 설정돼 있다), DB 는 `infomax`.

- 신선하다 — 국채 발행일 최신 **2026-09-15**, 통안 **2026-09-03**.
- ⚠**489행 중 322행이 STRIPS** 다. `이자종류='이표채'` 로 거르지 않으면 분리채가
  섞인다. 「489종 있다」고 말하면 틀린 말이다.
- 커버리지 실측(K-Bond 책 2026-07-01 이후 세트호가 행 기준): 국고 27,418행 중
  **27,409(100.0%)**, 통안 1,085행 중 **1,085(100.0%)** 가 마스터에 맞았다.
  못 맞힌 9행의 코드는 `26-21`·`26-41`·`31-8` 처럼 있을 수 없는 꼴이라 파서 잡음이다.
- **크레딧(은행채·카드채·회사채)은 이런 마스터가 DB 에 없다.** 은행채는 할인 발행
  종목도 섞여 있어 쿠폰만으로도 안 된다. 여기는 단가를 만들지 말고 비워라.

---

## §4. 정답지 — 고치면 이걸 먼저 돌려라

NICE피앤아이 **Bond Market Daily 2026-09-23** 「4. 통안/국고채 단가조정 금리표(당일결제)」와
「5. (익일결제)」에서 옮긴 값이다. 두 결제일 **32행 전부 |차| ≤ 0.010원** 으로 닫혔다
(0.010원 = 표시 소수 둘째 자리의 반올림 한 칸).

### 당일결제 T = 2026-09-23 (18행)

| 종목 | 쿠폰% | 만기 | 수익률% | NICE 단가 | m |
|---|---|---|---|---|---|
| 국고16-8 | 1.500 | 2026-12-10 | 3.210 | 10006.54 | 2 |
| 국고23-10 | 3.875 | 2026-12-10 | 3.300 | 10122.56 | 2 |
| 외평25-12 | 2.500 | 2026-12-10 | 3.230 | 10055.77 | 2 |
| 국고07-3 | 5.250 | 2027-03-10 | 3.500 | 10098.46 | 2 |
| 국고25-1 | 2.625 | 2027-03-10 | 3.500 | 9969.31 | 2 |
| 국고22-1 | 2.375 | 2027-03-10 | 3.500 | 9957.01 | 2 |
| 국고24-4 | 3.250 | 2027-06-10 | 3.625 | 10066.31 | 2 |
| 외평26-6 | 3.250 | 2027-06-10 | 3.620 | 10066.66 | 2 |
| 국고17-3 | 2.125 | 2027-06-10 | 3.625 | 9955.67 | 2 |
| 국고22-8 | 3.125 | 2027-09-10 | 3.740 | 9953.30 | 2 |
| 외평26-9 | 3.750 | 2027-09-10 | 3.825 | 10006.19 | 2 |
| 국고25-6 | 2.250 | 2027-09-10 | 3.740 | 9868.08 | 2 |
| 통안2709-01 | 3.430 | 2027-09-01 | 3.610 | 10004.03 | 4 |
| 통안2705-01 | 2.900 | 2027-05-09 | 3.300 | 10010.55 | 4 |
| 통안2611-01 | 2.540 | 2026-11-09 | 3.145 | 10023.23 | 4 |
| 통안2701-02 | 2.700 | 2027-01-02 | 3.285 | 10044.92 | 4 |
| 통안2703-03 | 3.430 | 2027-03-03 | 3.285 | 10025.10 | 4 |
| 통안2707-01 | 3.370 | 2027-07-09 | 3.600 | 10051.57 | 4 |

### 익일결제 T+1 = 2026-09-28 (14행)

> ⚠ 09-24~09-27 이 연휴라 T+1 이 09-28 이다. **T+1 은 «다음 영업일» 이지 «내일» 이 아니다.**

| 종목 | 수익률% | NICE 단가(T+1) |
|---|---|---|
| 국고16-8 | 3.210 | 10010.90 |
| 국고23-10 | 3.300 | 10127.09 |
| 외평25-12 | 3.230 | 10060.18 |
| 국고07-3 | 3.500 | 10103.27 |
| 국고25-1 | 3.500 | 9974.06 |
| 국고22-1 | 3.500 | 9961.75 |
| 국고24-4 | 3.625 | 10071.26 |
| 국고17-3 | 3.625 | 9960.56 |
| 국고22-8 | 3.740 | 9958.36 |
| 국고25-6 | 3.740 | 9873.10 |
| 통안2709-01 | 3.610 | 10008.96 |
| 통안2705-01 | 3.300 | 10015.02 |
| 통안2611-01 | 3.145 | 10027.50 |
| 통안2703-03 | 3.285 | 10029.60 |

(쿠폰·만기·m 은 당일결제 표와 같다.)

---

## §5. 함정

1. **★통안 = 03M.** §2. 국고 감각을 옮기면 70원 틀린다.
2. **★STRIPS 322종이 `국채_발행정보` 에 섞여 있다.** `이자종류='이표채'` 로 걸러라.
3. **물가연동국채 11종**은 06M 이표채지만 **원금이 물가에 연동**된다. 참조지수 없이
   이 식을 쓰면 틀린 값이 조용히 나온다. 자료를 물리기 전에는 **비워라**.
4. **★「1원 ≈ 1bp」는 거짓이다.** 1원이 몇 bp 인지는 듀레이션이 정한다 —
   30년물(26-2) 0.06bp · 4.5년물(26-3) 0.24bp · 2.7년물(26-5) 0.38bp.
   이 어림으로 손익을 매기면 장기물에서 자릿수가 틀린다.
5. **이표일은 만기일 역산**이다. 발행일에서 앞으로 세면 마지막 토막이 어긋난다.
6. **결제일이 값을 바꾼다.** 같은 금리라도 T 와 T+1 의 단가가 4~5원 다르다
   (국고16-8: 10006.54 → 10010.90). 장외 국채 관행은 **익일결제**다. 화면에 단가를
   띄울 때 «어느 결제일인지» 를 같이 말하지 않으면 그 값은 읽는 사람마다 달라진다.
7. **크레딧은 이 식의 밖이다.** 마스터가 없고, 은행채는 할인 발행도 섞인다.

---

## §6. 참조 구현 (그대로 옮겨도 된다)

```python
# -*- coding: utf-8 -*-
"""수익률 -> 단가(액면 10,000). 관행적 복할인.
정본: HANDOFF-bond-price-convention-2026-09-23.md §1~§2"""
from datetime import date
from dateutil.relativedelta import relativedelta


def coupon_dates(maturity: date, settle: date, m: int) -> tuple[list[date], date]:
    """결제일 이후의 이표일들(오름차순)과 «직전» 이표일. 만기에서 역산한다."""
    out, d = [], maturity
    while d > settle:
        out.append(d)
        d -= relativedelta(months=12 // m)
    return sorted(out), d                    # d = 결제일 직전 이표일


def price(coupon_pct: float, maturity: date, ytm_pct: float,
          settle: date, m: int, face: float = 10_000.0) -> float:
    """이표채 단가. m = 연 지급횟수(국고·외평 2 · 통안 4)."""
    cfs, prev = coupon_dates(maturity, settle, m)
    c = face * coupon_pct / 100.0 / m
    r = ytm_pct / 100.0 / m
    D = (cfs[0] - prev).days                 # 이번 이표기간 실일수
    d = (cfs[0] - settle).days               # 결제일 -> 다음 이표일
    acc = sum((c + (face if cf == maturity else 0.0)) / (1 + r) ** k
              for k, cf in enumerate(cfs))
    return acc / (1 + r * d / D)             # ★마지막 토막은 단리


def price_discount(ytm_pct: float, maturity: date, settle: date,
                   face: float = 10_000.0) -> float:
    """할인채(통안 DC 등). 단리 365."""
    return face / (1 + ytm_pct / 100.0 * (maturity - settle).days / 365.0)


def bp_per_won(coupon_pct, maturity, ytm_pct, settle, m) -> float:
    """이 종목에서 «1원» 이 몇 bp 인가. ★종목마다 다르다 — §5-4."""
    p0 = price(coupon_pct, maturity, ytm_pct, settle, m)
    p1 = price(coupon_pct, maturity, ytm_pct + 0.01, settle, m)   # +1bp
    return 1.0 / abs(p0 - p1)                                     # bp / 원
```

---

## §7. 어디서 왔나 · 남은 것

- 왔다 — **K-Orderbook(K-Bond 호가) 레인**. 그쪽에서 「세트호가(0.5bp 짝)의 중간값을
  단가로 보여 주고 O/X 로 검산한다」를 짓다가 이 규약이 필요해서 닫았다.
  그쪽 정본은 `Projects\apps\kbond\PROMPT_next_2026-09-22.md`.
- 열려 있는 것 —
  - **[a] 결제일 규약.** 화면·평가에서 T 를 쓸지 T+1 을 쓸지 v2 가 정해야 한다.
    식은 둘 다 닫힌다(§4). 장외 국채 관행은 익일결제다.
  - **[b] 물가연동국채** 참조지수. 지금은 비우는 것이 정직한 미구현이다.
  - **[c] 크레딧 마스터.** DB 에 없다. 물리면 은행채 할인 발행부터 갈라야 한다.
