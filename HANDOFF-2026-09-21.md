# 인계 — 2026-09-21 (sauron-v2)

아래 블록을 통째로 붙여 넣으면 이어받을 수 있다.

---

## 붙여 넣을 블록

```
2026-09-21 세션을 이어받는다. 원화 금리 데스크, `C:\Users\infomax\Projects\apps\sauron-v2`.

【지금 상태 — 제일 먼저 알 것】
· 로컬에 **62 커밋 미푸시**다. 푸시 = Vercel 배포이고 **오너 지시가 있어야 한다**.
· 작업 트리는 깨끗하다. 남은 미커밋 둘(`docs/LIT_*_2026-09-17.md`)은 **또 다른
  세션 것**이라 건드리지 않았다. 계속 두면 된다.
· 게이트 전부 초록: tsc 0 · eslint 0 · vitest **1,822**(110파일) · next build ·
  pytest **1,594**(`--ignore=tests/test_static_agreement.py`).

【★★★ 배포 전에 반드시 읽을 것 — 순서가 있다】
1. **백엔드 재기동이 먼저다.** 프로덕션 백엔드(:8200)는 이 작업 트리에서 돌기
   때문에, 재기동하는 순간 오늘 백엔드 변경이 전부 올라간다:
     · 새 라우트 `/api/paper*`(다섯) · `/api/momentum/sleeve`
     · `cache.SCHEMA_VERSION` 19 → 20 → **디스크 캐시가 전부 한 번 다시 구워진다**
   배포된 프런트(옛 코드)는 새 라우트를 안 부르므로 기능상 안전하다. 첫 요청들이
   느린 것만 감수하면 된다.
2. 그 다음에 프런트를 푸시한다.
3. 재기동 방법: `:8200` 리스너 PID 를 직접 죽이고 `Start-ScheduledTask
   SauronV2Backend`. ⚠ `Stop-ScheduledTask` 로는 uvicorn 이 안 죽는다
   (`backend/refresh.ps1` 머리에 실측 근거).

【★★★ 운영 결함 — 09-22 에 고쳤다. 코드는 섰고 스케줄만 오너 결정 대기】
**배포 화면의 Main·Backtest 가 하루 늦은 데이터를 내주고 있었다**(09-17, 화면
오른쪽 위에 「하루 늦음」). 시니어 트레이더가 「rateslab 안 들어가진다」고 한 것이
이것일 가능성이 크다 — 접속 자체는 멀쩡했다(:8200·Funnel·Vercel 전부 200, 익명
요청도 로그인 벽 없음, 브라우저로 열면 렌더까지 정상).

원인은 `backend/refresh.ps1` 머리가 이미 적어 둔 그 병이다:
  · 백엔드는 **뜰 때 SQL 을 한 번만 읽고** 다시 읽는 자리가 없다.
  · 오늘 순서 = 07:49 기동(그때 SQL 은 09-17) → 07:54 `SauronV2Refresh`(결과 0)
    → **09-18 적재가 그 뒤에 도착** → 종일 09-17.
  · 증거: 같은 코드·같은 SQL 인데 16시에 띄운 개발 백엔드(:8299)는 09-18 을 본다.
  · refresh 는 「SQL 이 더 새로울 때만 재기동」하고 **60분까지만** 기다리는데
    오늘 적재가 08:00 이후였다.
⚠ 증상이 화면마다 다르다: **Strategy 쪽은 09-18 로 멀쩡하다**(그 화면들은 SQL 을
라이브로 읽는다). 기동 스냅샷을 쓰는 **Main·Backtest 만** 늦는다.

▶ 09-22 에 한 것 (정본은 `BACKEND.md` → 「아침 데이터 갱신 — 스냅샷 백엔드의 하루」):

· **진단이 한 줄 더 깊었다.** 이건 「09-17 하루의 순서 사고」가 아니라 **열흘째
  매일**이었다 — refresh.log 의 「이미 최신이에요」가 09-10 부터 09-22 까지 열 줄.
  아침마다 PC 가 부팅(07:37)하며 백엔드가 그때의 SQL 을 읽고, 07:42 의 refresh 는
  둘이 같은 것을 보고 종료한다. 같은 것은 참이었고, 거짓은 그 상태를 «최신» 이라
  부른 것이다 — SQL 자신이 기대 전영업일을 안 들고 있었다.
· **대기(60분)를 늘리는 것은 답이 아니었다.** 그 루프는 한 번도 돈 적이 없다 —
  판정 두 줄이 ISO 문자열에 대해 완전·배타적이라 `Start-Sleep 300` 은 **도달 불가**
  였다. 고친 방향은 「더 오래 기다리기」가 아니라 **「다시 보기」**다.
· 판정을 PowerShell 에서 `scripts/check_close.py` 로 옮겼다(파이썬엔 시험이 있다 —
  `tests/test_refresh_decide.py` 여덟). 다섯 상태고 새로 생긴 것이 **`waiting`** 이다.
· 비교하는 두 수를 같은 계산에서 뽑는다(`wouldServe` = 서버가 부팅 때 지나는
  `load_dataset_merged` 의 asof). 옛 `irs_close_rows()[-1]` 은 raw MAX 라서
  전일종가 컷·엑셀 병합을 안 지났다 — 30분마다 보는 지금은 그 오독이 **종일 재기동**
  이 된다.
· 기다림은 스케줄러의 반복 트리거가 한다 — `backend/refresh_schedule.ps1`(새 파일,
  기본값이 «보기», 바꾸려면 `-Apply`, 되돌리기 `-Revert -Apply`).
· `refresh.ps1 -DryRun` 신설 — 판정만 하고 데스크의 백엔드를 안 죽인다.

▶ **남은 것 = 오너 결정 하나.** 반복을 걸면 전영업일 종가가 도착한 **그 회차에
백엔드가 재기동**되고, 그것이 곧 「배포 순서 1단계」다(위 【배포 전에 반드시 읽을
것】). 걸기 전에는 오늘도 화면이 09-18 이다.

【오늘 한 일 — 커밋 순】
· `c22540ee` 등록 슬리브를 화면에 (오전)
· `d9792027` **Strategy/Momentum 이 IRS 50/50 북으로 갈아탔다** — KTB 거울 면
  완전 은퇴. 세입자 넷 → 셋. KTB 면이 주던 것(룩백 격자·테마 부호 북·표본내
  성과)을 그대로 들고 왔다. `nav.ts::RETIRED_STRATEGY` 가 옛 주소를 넘긴다.
· `23bcb58d` MR 계획면을 진입 후보 / 보유 포지션으로 가르고 **낱말을 데스크
  표준으로**(늘어남→괴리도 · 지금 할 일→시그널 · 밴드 안→밴드 내 · 밖→이탈 ·
  재진입→복귀 · 채점→산출 · 세울 북→포지션 규모 …).
· `8f77e01a` **Portfolio 탭** — Strategy 와 같은 위계. 등록하면 조건이 얼고 그
  뒤로는 시장만 움직인다(표본밖 기록). 규칙 북 + 수동 북, 히어로는 **차이**.
  ⚠ **이 리포의 첫 쓰기 라우트**다. 장부는 `backend/data/paper_book.json`(추적
  안 함). 지우는 라우트는 두지 않았다(생존 편향).
· `3488e10d`·`07a3eb0a`·`7424a164` 테너 231조합 탐색과 그 판정
· `754894ca` Portfolio 가드
· `ab4c0498` **청산·손절을 별도 패널로** — 행이 아니라 **사실**을 가른다(MECE).
  순위 패널은 25계열 전부, 청산·손절 패널은 나가는 문만. 겹치는 것은 계열 이름뿐.
· `5ea6ae0f` (다른 세션) 리드아웃을 그림 밖 한 줄로 — Main 미리보기·Backtest
· `9a21bd37` 그 이관을 **남은 여섯 표면**에 마저 적용

【★★★★ 테너 탐색의 결론 — 등록은 안 건드렸다】
정본 `backend/output/MOMENTUM_TENOR_SEARCH_2026-09-21.md`, 레인 §9½.

  등록 3Y+10Y    N_eff 1.04 · SR 1.000 · CDaR 비 1.140 · 88/231위
  최고 칸 3Y     N_eff 1.00 · SR 1.118 · CDaR 비 1.332 · 1위
  전 테너 등가중  N_eff 1.07 · SR 1.060 · CDaR 비 1.210 · **31/232위**

답은 **「고르지 말라」**다. 대조군이 고른 231칸 중 201개를 이기면서 자유도를
하나도 안 쓴다. 다리를 늘리면 중앙값만 오르고(0.903→0.950→1.115) 천장은 안
오른다(1.332→1.312→1.309).

★그리고 이 측정의 제일 큰 수확은 **순위자가 답을 정했다**는 것이다. 레인 기본
순위자(`vol_normalize` 위 CDaR 비)로는 1Y 단독 1위·등록 132위였고 그 순위를
버렸다 — 확장창(`min_obs=60`)이라 배수가 2017 만 11~15배로 튀어 그 한 해가 모든
칸의 낙폭 꼬리를 차지했고, 그래서 비율이 분자만 따라갔다(Spearman(정규화 CDaR,
SR) = **+0.964**). 등록 칸이 첫해만 버려도 0.194 → 1.246(6.4배)다. 확증: 그
순위자가 1위로 올린 1Y 는 원계열 MDD/연변동성이 3.24(등록 칸 1.03)다.
⚠ 정규화를 뺀 근거는 **이 레인에만** 있다(북 변동성이 이미 맞춰져 있다).
다른 레인에 옮기지 말 것. `evaluation/metrics.py` 는 안 고쳤다(남의 레인 코드).
⚠ **위약(순환이동)을 안 쟀다** = 레인 게이트 셋 중 하나가 빈 채다.

★레인 규율 §10-2 를 다시 썼다: N=2 상한의 정체는 「상품이 둘」이 아니라
**「인자가 하나」**다(ρ̄ 0.933 · 최원거리 1Y–10Y 0.749 · 열한 다리도 N_eff 1.07).
⇒ **아웃라이트 테너를 늘리는 어떤 설계도 이 상한을 못 연다.** 여는 것은 다른
인자뿐이다(커브·플라이·타 자산군 — 안 쟀다).

【리드아웃 이관 — 지금 어디까지】
떠 있는 카드(`ReadoutCard`)를 그림 위 고정 줄(`ui/ChartReadoutStrip`)로 옮겼다.
지금 **`placeReadout` 을 지나는 표면은 0**, 카드를 쓰는 곳은 **`rv/RvScatter`
하나**다. `guards/readout-card-width.test.ts` 가 그 수를 세고 있어서, 또 옮기면
그 줄이 빨개진다(고치는 것이 곧 기록이다).

산점도가 마지막인 데는 이유가 있다 — 고정 줄의 계약은 「한 날 + 그날의 값들」인데
산점도는 축이 둘이라 커서 밑의 「한 날」이 없다. 옮기려면 줄의 계약부터 바꿔야 한다.

【★ 오너 판정이 필요한 것 — 앞 세션이 남기고 내가 확인한 것】
**크로스헤어 자석과 구슬이 서로 다른 수를 가리킬 수 있다.** 가격 축 라벨은
`CrosshairMode.Magnet` 이라 커서에서 **가장 가까운 계열**의 값에 붙고, 구슬
(`beacon`)은 주선에 있다. 둘 다 참이지만 이 리포는 「같은 화면에 두 수」에
민감하다. 자석을 끄면(Normal) 라벨이 「커서 높이의 자유 가격」이 되어 어긋남이
없어지는데 **15개 차트 전역 변경**이라 앞 세션이 안 건드렸다. 내가 여섯 표면을
더 옮겼으므로 이제 이 문제가 **거의 모든 시계열 화면**에 걸린다. 판정 필요.

【열려 있는 오너 결정 — 오늘 이전부터】
1. 모멘텀 북을 실제로 켤 것인가 + 원장 첫 채점 행(§4-2, 09-16부터 열림)
2. k_mr 축소를 MR 레인에 지시할 것인가(§4-1) — 모멘텀 화면이 매일 「안 줄이면
   못 선다」를 붉게 적고 있는 그 가정
3. 테너: 위약을 돌릴 것인가 · 「전 테너 등가중」으로 새 사전등록을 열 것인가
4. `SHOW_52W_IN_STRIP = false` 로 꺼 둔 52주 세 줄을 되살릴 것인가(되살리면
   자리는 리드아웃 줄의 둘째 줄)

【환경 메모】
· 개발 백엔드 `:8299`, 개발 프런트 `:3200` 이 떠 있다. `.env.development.local`
  이 `:8299` 를 가리킨다. **개발 서버는 뜰 때 한 번만 그 파일을 읽는다** —
  고치면 재기동해야 하고, 안 하면 「FAILED TO FETCH」가 난다(오늘 실측).
· ⚠ **`next build` 와 `next dev` 가 `.next` 를 공유한다.** 개발 서버가 떠 있는
  채로 빌드를 돌리면 개발 화면이 CSS 404 로 깨진다(오늘 한 번 밟았다). 빌드 뒤
  `rm -rf .next` + 개발 서버 재기동.
· 화면 감사에서 믿을 것은 스크린샷이 아니라 **DOM** 이다. 오늘도 둘을 그렇게
  잡았다: 자라려는 칸이 높이 0 으로 눌린 것(`scrollHeight` 대 `clientHeight`),
  그리고 CDS `Select` 가 값이 목록에 없을 때 **글자 없이** 서는 것.
· 가드를 새로 쓰면 **위반을 합성해 하나씩 넣어 본다.** 오늘 내가 쓴 가드 중
  둘이 헛돌았고(정규식이 괄호에서 멈춤, 주석 속 낱말에 걸림) 그 방법으로 잡았다.
  초록인 가드가 헛도는 가드일 수 있다.

【정본 문서】
  NEXT_PROMPT-macro.md   모멘텀 레인(§9 화면 · §9½ 테너 · §10 상시 규율)
  NEXT_PROMPT-momentum.md  KTB 모멘텀 레인 — **머리에 은퇴 표시**를 붙였다
  docs/MR_LANE_STATE.md  MR 레인(§5¾-C~F — 계획면 · FSW · 패널 분리 · Portfolio)
  CLAUDE.md              UI 캐논. 리드아웃 줄이 새 캐논, 카드는 「새 표면 금지」
```

---

## 이 파일을 지운 뒤

인계가 끝나면 이 파일은 지운다 — 리포에 인계문이 쌓이면 어느 것이 지금인지
읽는 사람이 모른다. 서 있는 사실은 위 「정본 문서」 넷에 이미 들어가 있다.
