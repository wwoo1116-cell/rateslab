# Momentum — Strategy 셋째 세입자 붙이는 계획 (2026-09-09)

[OWNER 2026-09-09 — 「v2 strategy tab 밑에 지금까지 한 거 momentum으로 붙이는 계획.
위계상으로는 RV, MR과 동일」]

아직 코드는 안 썼다. 이 문서는 **무엇을 붙이고 무엇을 안 붙이는지**를 먼저 정한다.

---

## 1. 위계와 배관 — 기존 기계를 그대로 쓴다

세입자 기계는 이미 두 번 돌았다(Lab 2026-08-20 → Strategy 2026-08-24·08-25).
셋째는 **새 문법을 만들지 않는다.**

| 자리 | 지금 | 바뀔 것 |
|---|---|---|
| `src/ui/nav.ts` `StrategyId` | `'credit-rv' \| 'mean-reversion'` | `\| 'momentum'` 추가 |
| `STRATEGY_ITEMS` | 둘 | 셋째 항목(label·desc·glyph) |
| `isStrategyId` | 둘 | 셋 |
| `DEFAULT_STRATEGY` | `'credit-rv'` | **안 바꾼다** — 예전 `?g=strategy` 링크가 가리키던 화면 |
| `PANELED` | 이미 `'strategy'` 포함 | 변화 없음 |
| `src/app/page.tsx` | `strategy === 'credit-rv' ? <RvPage/> : <MrPage/>` | 3분기로 |
| `guards/nav-strategy-tenants.test.ts` | `toEqual(['credit-rv','mean-reversion'])` | **가드가 먼저 빨개진다** — 이 리포 규칙대로 가드부터 고친다 |

URL 은 `?g=strategy&s=momentum`. 탭을 늘리지 않는다(「섹션은 유도값」 규칙).

**글리프 후보 `↗`.** 한 글자 제약을 만족하고 뜻이 곧다. ⚠ 다만 `table/tint.ts` 의
`directionGlyph` 가 변화 셀에서 이미 `↗↘` 를 쓴다 — 「같은 것을 두 그림으로 부르지
않는다」의 반대편(다른 것을 같은 그림으로 부름)에 걸릴 수 있다. 오너 확인 사항.

---

## 2. 이 화면은 무엇인가 — 명구 의무

두 이웃 세입자에게는 명구가 있다: Credit RV 「랭킹이지 투자판단이 아니다」,
MR 「측정이지 신호가 아니다」. Momentum 도 하나 져야 하는데, **이 화면은 앞의 둘과
성질이 다르다** — 추세는 실제로 신호다. 그래서 거짓말을 하지 않는 명구는 이것이다:

> **방향과 «강도»를 재는 화면이다. 단독 전략이 아니고, 크기·진입·손절을 말하지 않는다.**

근거는 이 데스크의 실측이다.
- 선물 추세 **단독** 은 자본비용 전액 규약에서 MMF 초과 −1.07 ~ +0.97%/년
  (`research\ktb-tsmom\out\vs_mmf_run.txt`) — 단독 전략으로 부르면 안 된다.
- 엔진 머리에 오너가 이미 그 쓰임새를 적어 뒀다 [OWNER 2026-09-07,
  `ctabacktest.trend_tstat` 주석] — 「손익을 내는 전략이 아니라 **트레이더의 확신을
  돕는 도구**」, 그리고 확신 도구가 필요한 것은 «켜짐/꺼짐» 이 아니라 «얼마나 강한가».

**그래서 화면의 주인공은 손익 곡선이 아니라 «지금 얼마나 강한가» 다.**

---

## 3. ★★★ 채점 잠금 — 동결 사전등록을 화면이 훔쳐보게 하면 안 된다

`data\krw-macro-vintage\PREREG.md` 가 2026-09-08 에 동결됐다. 채점 대상은
**2026-09-09 이후 영업일뿐**이고, 최소 판정 조건은 채점 750영업일 또는 macro_sign
방향전환 12회 중 나중이다. 그 문서가 「판정일까지 들여다보지 말 것 — 중간에 보는 것
자체가 사전등록을 훼손한다」고 못박고 있다.

**화면을 그대로 만들면 그 규율을 매일 어기게 된다.** 그래서 잠금을 배관에 넣는다.

| 보여도 되는 것 | 막아야 하는 것 |
|---|---|
| 오늘의 신호 방향·강도(포지션을 잡는 데 필요) | 동결일 **이후** 매크로 모멘텀 누적 손익 |
| 표본내 성적(2017-01-06~**2026-09-08**), 「표본내」라고 화면이 말함 | 동결 이후 구간의 Sharpe·Calmar·적중률 |
| 네 테마 오늘 부호 | 「사전등록이 이기고 있나」로 읽히는 어떤 표시도 |

구현: 백엔드가 `FREEZE_DATE = '2026-09-08'` 를 들고 **그 이후 손익 계열을 응답에서
잘라낸다**(프런트에서 자르면 네트워크 탭에 그대로 보인다 — 자르는 자리가 서버여야
한다). 화면에는 그 자리에 「사전등록 채점 중 · 판정일까지 잠김」 한 줄이 선다.
가드 `guards/momentum-prereg-lock.test.ts` 가 그 불변식을 잰다.

이건 기능 제한이 아니라 **이 화면이 존재해도 되는 조건**이다.

---

## 4. 화면 문법 — 캐논에서 임포트한다

CDS 는 `@coinbase/cds-web@9.22.0`(runtime web). 새 부품을 만들지 않는다.

| 무엇 | 무엇을 쓰나 |
|---|---|
| 레이아웃 | `@coinbase/cds-web/layout` — `Box`·`HStack`·`VStack` |
| 표 | `@coinbase/cds-web/tables` + `table/rowHeight.ts` 의 `ROW_H`(60), 클래스는 `.sr-rv-table` 계열 |
| 글자 | `@coinbase/cds-web/typography` `Text` (shorthand 신규 사용 금지) |
| 조건 바 한 칸 | `ui/Cond.tsx` (공용) |
| 열 머리 뜻풀이 | `ui/ThHelp.tsx` (공용) |
| 사실 스트립 | `ui/Stat.tsx` `Stat`·`StatColumn` — 차트 **아래** |
| 방향·강도 셀 | `table/tint.ts` **네 부품 한 벌** — `tintStyle`+`directionClass`+`directionGlyph`+`unsignedDelta` |
| 값 서식 | `lib/format.ts` `fmtLevel`·`levelHeadText`·`levelHeadTitle` |
| 시계열 차트 | `chart/TimeChart.tsx` (CDS 아님 — lightweight-charts, 팔레트는 `chart/palette.ts`) |
| 커서 리드아웃 | `ui/ReadoutCard.tsx` + `.sr-plot` |
| 로딩·에러 | `ui/DataState.tsx` |
| 떠 있는 창 | `ui/window/FloatingWindow` (장부 창을 열 때만) |

배치는 MR 과 같다 — **조건 바 + [표 | 상세] 2열, 페이지는 스크롤하지 않는다.**

### 표의 축 — ★ 룩백은 손잡이가 아니라 «열» 이다

이 레인의 규율이 「룩백을 고르지 마라」다(매년 전진 선택하면 7년 합 −3,230만,
`ktb-crisis-alpha-verify`). 그래서 룩백을 셀렉트로 두면 **화면이 그 규율을
어기는 도구가 된다.** 대신 다섯 룩백을 전부 열로 세운다.

```
행 = 계약(3Y·10Y) × 신호가족(tsmom·macross·donchian)   → 6행
열 = 20 · 40 · 60 · 120 · 250 (각 칸 = −1~+1 강도) + 합성(등가중) + 유지일수
```

칸 값은 `signal_continuous` 의 연속 강도(방향 × min(|t|/4, 1))다. 이진 부호가
아니라 강도라야 「얼마나 강한가」에 답한다. 색은 tint 네 부품 한 벌.

### 매크로는 «곱하지 않고 나란히» 둔다

오늘 실측에서 변조(추세×매크로)는 NO-GO 였다(`RESULT_cta_macro_mod.md` —
N=2 구조 때문에 북 변동성 목표가 되돌린다, 추세와 상관 0.977). **화면이 둘을 곱해
한 숫자로 만들면 우리가 방금 기각한 설계를 화면이 몰래 채택하는 셈이다.**
그래서 매크로는 표 아래 별도 한 줄 — 네 테마 오늘 부호와 `macro_sign` 을 «순풍/역풍»
으로만 적는다. 추세 값과 합치지 않는다.

---

## 5. 백엔드 — 숫자는 전부 서버가 끝낸다(§16)

새 모듈 `backend/app/momentum.py`, 라우트 셋:

| 라우트 | 내용 |
|---|---|
| `GET /api/momentum/board` | 오늘 격자 — 6행 × 룩백 5칸 강도·합성·유지일수·현재 액면, 그리고 매크로 한 줄 |
| `GET /api/momentum/history/{key}` | 한 행의 가격(조정가)·신호 강도 이력 — 차트용 |
| `GET /api/momentum/book` | **표본내** 성적(추세·매크로·50/50·균형) — 동결일 이후는 잘려서 나온다 |

- 엔진은 `app/ctabacktest.py` 를 그대로 부른다(`signal_continuous`·`book_simulate`).
  화면용으로 산술을 다시 짜지 않는다 — 두 벌이 되면 한쪽만 낡는다.
- 가격은 `app/futures.py` 의 `price_adj`(**차분만 유효** — 레인 규약).
- 캐시는 기존 `app/cache.py`/`df_cache.py` 결. 뜰 때 한 번 + 07:00 `SauronV2Refresh`.

### ⚠ 자료 경로 문제 하나 — 매크로 신호가 리포 밖에 있다

`macro_themes_daily.csv` 는 `Projects\data\krw-macro-vintage\out\` 에 있고 **그
레인은 git 리포가 아니다.** 배포된 백엔드에는 그 경로가 없다. 셋 중 하나여야 한다.

1. **아침 굽기가 `backend/data/` 로 복사한다** ← 권장. `backend/data/raw/bigfoot_*.csv`
   가 이미 그 선례다. 굽기가 실패하면 파일이 낡을 뿐 화면은 산다.
2. 백엔드가 리포 밖 절대경로를 읽는다 — 로컬만 되고 배포에서 죽는다. 안 된다.
3. 매크로 줄을 아예 빼고 추세만 — 화면이 반쪽이 된다.

1번을 택하되 **없으면 매크로 줄만 비고 추세 격자는 뜬다**(우아한 저하). 「값이
없는 것과 0 인 것을 섞지 않는다」 규율 그대로 — 빈 줄이 왜 비었는지 말한다.

---

## 6. 레인 — 셋으로 자른다

### 레인 A · 배관과 빈 화면 (가장 작게 배포 가능)
`nav.ts` 세입자 등록 · `page.tsx` 3분기 · `src/momentum/MomentumPage.tsx` 껍데기 ·
가드 `nav-strategy-tenants` 갱신. 화면은 「무엇을 재는 곳인가」 한 줄과 빈 상태.
**여기까지가 위계 요구(RV·MR 과 동일)를 실제로 만족시키는 지점이다.**

### 레인 B · 신호면 (주인공)
`/api/momentum/board`·`/history` · 6행 격자 표 · 상세 차트 · 사실 스트립 ·
매크로 한 줄. 가드: `momentum-board.test.ts`(격자 축·룩백이 손잡이가 아님),
`momentum-canon.test.ts`(rv 표 문법·tint 네 부품·ROW_H).

### 레인 C · 장부면 + 채점 잠금
`/api/momentum/book` · 표본내 성적 카드 · **채점 잠금** 한 줄.
가드: `momentum-prereg-lock.test.ts` — 동결일 이후 손익이 응답에 없음을 잰다.

---

## 7. 안 하는 것 (지금은)

- **MR 과의 결합 북(추세 슬리브 w=0.25).** 아직 증거금 상한판 위에서 안 쟀다
  (`ktb-tsmom\HANDOFF_2026-09-08.md` §2-①). 안 믿는 숫자를 화면에 올리지 않는다.
  그 측정이 끝나고 PREREG_04 가 서면 그때 레인 D.
- **룩백·신호 고르기 손잡이.** 위 4절의 이유.
- **추천·진입·손절 문구.** 2절 명구.
- **동결 이후 채점 표시.** 3절.
- 스마일 화면화 — 스마일은 «동시» 라 화면에 올리면 예측으로 읽힌다. 09-08 에 닫은 갈래.

---

## 8. 오너 결정 대기

1. **글리프 `↗` 로 갈까** — 변화 셀의 `directionGlyph` 와 그림이 겹친다(1절).
2. **표의 행 축** — 「계약 × 신호가족 6행」이 맞나, 아니면 **10Y 만** 두고 3행으로
   갈까. 근거: KTB3 단독 SR 0.01 대 10Y 0.67 로 **3년물 추세는 이미 닫힌 갈래**다.
   6행으로 두면 화면이 죽은 갈래를 매일 보여주게 된다.
3. **레인 A 만 먼저 배포할지**, B 까지 묶어 한 번에 낼지.

---

# 9. UI/UX 설계 — 외부 근거와 함께 (2026-09-09 리서치)

이 리포 규칙 8 이 요구하는 대로 **라이브러리 중립 판단은 인용 가능한 외부 표준**에
둔다. 아래 근거는 전부 출처가 있고, 결론이 데스크 실측과 어긋나면 실측이 이긴다.

## 9.1 먼저 — 우리 팔레트는 외부 방법론을 이미 통과한다

발산형 인코딩의 표준 요구는 「대립하는 두 hue + **중립 회색** 중점」이고, 금지는
「양극을 둘 다 cool 로」·「중점에 hue 를 둠」·「무지개」다. `direction.css` 의
적(#d92d3c)/청(#0064ff) + 회색 중점은 **셋 다 통과**한다. 색각이상 분리도 이미
데스크가 Viénot–Brettel–Mollon + CIEDE2000 으로 직접 쟀다(정상 44.75 · protan 50.90 ·
deutan 65.37, DESIGN.md §3.1). **새 팔레트를 들이지 않는다** — 리포 규칙 9(병렬
디자인 소스 금지)와 §3.1 동결이 그대로 산다.

## 9.2 격자의 형 — 「Deviation」이지 「Magnitude」가 아니다

FT Visual Vocabulary 의 분류로 우리 격자의 일은 **Deviation** 이다: 고정 기준점
(여기서는 0)에서의 ±. 그 칸의 권장 형은 diverging bar · spine chart 다.
일반 도해 방법론도 같은 답을 준다 — 「기준선 위/아래 → 발산(diverging)」,
「격자 → heatmap」.

그런데 **색으로 크기를 읽는 것은 길이로 읽는 것보다 부정확하다**(Cleveland–McGill
서열). 이 화면의 질문이 하필 「**얼마나** 강한가」이므로 크기를 색에 맡기면 화면이
제 질문에 약하게 답한다. 그래서:

> **칸 = 0 중심 발산 «막대» + 무부호 숫자. 색은 방향만, 크기는 길이가 진다.**

⚠ 이건 캐논 이탈이다 — 변화 셀은 tint 네 부품 한 벌(`table/tint.ts`)이 문법이다.
이탈 근거를 주석에 남긴다(캐논 규칙 3): **tint 는 «변화» 셀의 문법이고 이 칸은
«신호 강도» 라는 다른 물건**이며, 채널을 색(방향)과 길이(크기)로 나누면 WCAG 2.2
§1.4.1(색 단독 금지)이 부품을 더 붙이지 않아도 자동으로 만족된다.
방향 글리프와 무부호 숫자는 그대로 쓴다.

## 9.3 ★ 이 화면의 «주장» — 주인공은 합성값이 아니라 «속도 프로파일의 모양»

Baltas–Kosowski(FAJ 2018, CFA Institute 요약)의 ATSMOM 결과가 이 화면의 축을
정당화한다 — **「신호 방향이 아니라 신호 강도」를 여러 시평(horizon)에서 모으는 것**이
요점이고, 그 모형이 설명한 CTA 수익의 **절반가량이 «속도 요인»**에서 왔다.

그리고 이 레인의 규율이 「룩백을 고르지 마라」다(전진 선택 7년 −3,230만). 둘을
합치면 화면 설계가 저절로 나온다: **다섯 룩백을 손잡이가 아니라 x축으로 세우고,
왼쪽 빠름 → 오른쪽 느림으로 고정**한다. 그러면 행 하나가 곧 속도 프로파일이고,
그 **모양**이 숫자 하나로는 안 보이는 것을 말한다.

```
■■■□□  ← 빠른 쪽만 섰다 = 전환 초기
□□■■■  ← 느린 쪽만 남았다 = 소진·되돌림
■■■■■  ← 속도 합의 = 강한 추세
■■□▨▨  ← 갈렸다 = 「모른다」
```

**등가중 합성 하나만 보여주면 이 정보가 사라진다.** 그래서 합성은 hero 가 아니라
맨 오른쪽 한 칸으로 붙이고, 화면의 주인공은 프로파일이다. (dataviz 방법론의
「단일 현재값은 stat tile」 원칙을 여기서는 **일부러 따르지 않는다** — 이 화면의
주장이 「단일값이 주인공이 아니다」이므로 hero 를 합성값으로 두면 화면이 스스로를
반박한다.)

사실 스트립(`ui/Stat.tsx`) 네 칸: 방향 · 합성 강도 · 유지일수 · **속도합의 n/5**.
넷째가 이 화면 고유의 사실이다.

## 9.4 행 안의 스파크라인 — 높이를 실측이 정한다

Heer·Kong·Agrawala, *Sizing the Horizon*, CHI 2009(Best Paper)의 결과는 이 자리에
바로 물린다:

- **차트 높이 24px 미만에서는 2밴드 미러 horizon 그래프가 선 그래프보다 정확**했다.
- 2밴드의 최적은 **12px 과 6px**(둘이 거의 동등).
- **밴드 3개를 넘기면 오차와 시간이 같이 는다.**

우리 행 높이는 60px 이고 이름 두 줄 문법(§2.1·§4.1)이 그 안에 있으므로 스파크라인에
줄 수 있는 높이는 **20px 안팎** — 정확히 horizon 이 선 그래프를 이기는 구간이다.
그리고 우리 값은 0 중심 발산이라 미러(위=적·아래=청)가 자연스럽다.

⚠ 다만 캐논 차트는 `lightweight-charts`(`chart/useLwChart.ts` 의 `canonOptions`)이고
horizon 은 그 라이브러리의 형이 아니다. **선택지는 둘이고 실측으로 정한다**(DESIGN.md
방식 — 만들어서 재보고 적는다):
  (가) 높이를 24px 이상 확보하고 0 기준선 있는 **선**으로 간다(캐논 유지)
  (나) 2밴드 horizon 을 만든다(캐논 이탈, 근거는 위 논문)
행 높이 60px 을 건드리는 것은 §2.1 동결이라 (가)가 되려면 이름 줄과 경쟁한다.
**재보기 전에는 어느 쪽도 규칙이 아니다.**

Tufte 의 원 용법(스파크라인은 표 안 숫자 옆에 묻는다)이 (가)·(나) 둘 다에 걸린다 —
축·눈금·범례 없이 숫자 옆에 붙인다.

## 9.5 매크로 줄은 «다른 표기»로 — 곱하기를 눈이 대신하지 않게

오늘 실측에서 변조(추세×매크로)는 NO-GO 였다. **격자와 같은 색·같은 셀 모양으로
매크로를 그리면 독자의 눈이 자동으로 곱한다** — 우리가 방금 기각한 설계를 화면이
몰래 채택하는 셈이다. ISO 24896:2026(IBCS 기반 의미 표기)의 요지가 「같은 개념은
항상 같은 표기」이고, 그 대우가 **다른 개념은 다른 표기**다.

→ 매크로는 격자 아래 **칩/태그 한 줄**(네 테마 부호 + `macro_sign`), 셀 문법을
쓰지 않는다. 「순풍/역풍」이라는 낱말로만 추세와 관계 맺는다.

## 9.6 합성 강도 한 칸의 형 — 게이지가 아니라 bullet

합성 강도를 「지금 값 + 기준 범위」로 보이려면 Few 의 **bullet graph** 가 표준이다 —
게이지·미터를 대체하려고 만든 형이고, 좁은 공간에서 값·기준·정성 구간을 같이 낸다.
포화점(|t|/4 = 1)이 그 「기준」 자리다. 게이지·도넛은 쓰지 않는다.

## 9.7 접근성 — 이미 리포 규칙인 것과 새로 붙는 것

- WCAG 2.2 §1.4.1: 색 단독 금지 → 9.2 의 색+길이+글리프+무부호 숫자로 만족.
- §1.4.11: 그래픽 3:1 → 막대·스파크라인 잉크가 면 대비 3:1 을 넘는지 잰다.
- 격자는 **표**이므로 terminal 레인의 roving tabindex 규칙(그림 = 위젯 하나) 대상이
  아니다. 표준 표 탐색으로 충분하다.
- 스파크라인이 캔버스면 「읽을 DOM 이 없다」 규칙이 걸린다 → `.sr-a11y-only` 의
  `aria-live` 한 줄이 짚은 행의 방향·강도를 말한다(CLAUDE.md 차트 규칙 7·키보드 7).

## 9.8 출처

FT Visual Vocabulary (Financial Times Visual Journalism Team) —
https://github.com/Financial-Times/chart-doctor/tree/main/visual-vocabulary ·
Heer, Kong, Agrawala, "Sizing the Horizon", CHI 2009 (Best Paper) —
https://dl.acm.org/doi/10.1145/1518701.1518897 ·
Baltas & Kosowski, "Trends' Signal Strength and the Performance of CTAs",
Financial Analysts Journal 2018 (CFA Institute 요약) —
https://rpc.cfainstitute.org/research/financial-analysts-journal/2018/ip-v4-n1-3-trends-signal-strength ·
ISO 24896:2026 Notation for business reporting (IBCS 기반) — https://www.ibcs.com/iso-24896/ ·
Few, "Bullet Graph Design Specification", Perceptual Edge —
https://www.perceptualedge.com/articles/misc/Bullet_Graph_Design_Spec.pdf ·
Tufte, "Sparkline theory and practice" — https://www.edwardtufte.com/notebook/sparkline-theory-and-practice-edward-tufte/ ·
WCAG 2.2 §1.4.1 · §1.4.11.

## 9.9 이 절이 만든 새 결정 대기 항목

4. **격자 칸을 tint 채움(캐논)으로 갈까, 0 중심 발산 막대(9.2)로 갈까.**
   캐논 이탈이라 오너 판단 자리다.
5. **스파크라인을 행에 넣을까** — 넣으면 9.4 의 (가)/(나) 실측이 선행한다.
   안 넣으면 격자만으로도 화면은 선다(상세 차트가 오른쪽에 있으므로).

---

# 10. 지었다 — 2026-09-09 (레인 A+B+C)

## 오너 결정 넷

| 항목 | 결정 |
|---|---|
| 보드 구성 | **두 다리 나란히** — 추세 다리 + 매크로 다리 + 50/50 한 줄 |
| 칸의 형태 | **tint 채움(캐논)** — 발산 막대 안(§9.2)은 채택 안 함 |
| 글리프 | **`↗` 그대로** |
| 범위 | **A+B+C 전부** |
| 추가 | **「기존 v2 의 문법을 충실히 따를 것」** |

## ★ 짓다가 바로잡은 것 셋

1. **신호가족 축이 틀렸다** [오너 지적]. 등록된 북의 추세 다리는 **macross 하나**다
   (`cta_macro.py:145`). `tsmom`·`donchian` 은 `cta_validate.py` 의 PBO 격자 차원이지
   북에 안 든다 — 목업의 `n/3` 은 안 쓰는 신호를 화면에 올린 것이었다.
2. **매크로가 각주로 내려가 있었다.** AQR 50/50 은 **두 북**이라(손익 수준 결합)
   매크로는 절반짜리 다리다. 칩 줄이 아니라 같은 표 문법의 «다리»로 세웠다.
   「곱하지 않는다」는 그대로 산다 — 결합된 신호는 화면 어디에도 없다.
3. **명구가 한다체였다.** 이 앱의 UI 문법은 **해요체**다(MR — 「늘어남을 재는
   화면이에요 — 투자판단이 아니에요」). 전부 해요체로 맞췄다.

## 실측으로 잡은 버그 하나

매크로 다리 액면이 **0억**으로 나왔다. 매크로는 OECD 에디션 규약상 가격보다 하루
늦게 서는데(09-07 대 09-08) 마지막 칸을 그냥 읽어서다. 「매크로가 빠졌다」가 아니라
「소스 날짜가 다르다」이므로, 그 북의 **자기 as-of 날짜** 액면을 읽게 고쳤다 —
MR 이 소스별 as-of 를 행마다 따로 세우는 그 규칙과 같다.

## 지은 것

| 파일 | 무엇 |
|---|---|
| `backend/app/momentum.py` | 계산 전부(§16) · `FREEZE` 상수 · 채점 잠금 |
| `backend/app/main.py` | `/api/momentum/board` · `/history/{key}` · `/book` |
| `backend/data/macro_themes_daily.csv` | 레인에서 복사(굽기가 갱신할 자리) |
| `src/momentum/api.ts` · `MomentumPage.tsx` | 타입·페처 · 화면 |
| `src/table/tint.ts` | `signalTint` — **세 번째 눈금**(매트릭스 선례) |
| `src/lib/staticPaths.ts` · `src/ui/nav.ts` · `src/app/page.tsx` | 세입자 배관 |
| `guards/momentum-canon.test.ts` | 캐논 8칸 |
| `guards/momentum-prereg-lock.test.ts` | 채점 잠금 5칸 |
| `guards/nav-strategy-tenants.test.ts` | 세입자 둘 → 셋 |

**자기 대조** — `/api/momentum/book` 이 `RESULT_cta_macro.md` 의 수를 그대로 낸다:
추세 0.53/0.30 · 매크로 0.51/0.22 · 50/50 0.61/0.49. 화면이 등록된 북과 같은
산술 위에 서 있다는 뜻이다.

## 남은 것

- **아침 굽기에 CSV 복사를 물려야 한다.** 지금은 손으로 한 번 복사했다.
  `data\krw-macro-vintage\out\macro_themes_daily.csv` → `backend\data\`.
- **돌고 있는 백엔드(:8200)는 다른 세션 것이라 새 라우트를 아직 안 문다.**
  재시작해야 화면이 뜬다.
- 남의 레인 가드 7건이 빨갛다(`mr-book`·`mr-canon`·`mr-optimize`·`shared-controls`)
  — `src\mr\KnobBar·OptimizePane·StrategyWindow·parts` 가 다른 세션에서 작업 중이다.
  이 레인과 무관하다.

---

# 11. 채점 재점검 — MR 수리 대기 (2026-09-09)

MR 레인에서 Phase 0 진단이 나왔다(PF 부풀림·Ulcer 부호/단위·시행 횟수·원화 계열·
MaxDD 표본). 그 다섯을 이 레인에 그대로 걸어 봤다 — `scripts/momentum_audit.py`.

## 넷은 안 걸린다 (구조가 달라서)

| | MR | Momentum |
|---|---|---|
| 미청산 제외 | 거래 목록으로 채점 → 걸림 | 일별 시가평가뿐 → **거래 목록이 없다** |
| Ulcer 부호·단위 | 화면이 `-perf.ulcer` 로 뒤집음 | 안 뒤집는다(가드가 잰다) |
| 원화 계열 정규화 | 장치 없음 | **북 변동성 목표**가 위험을 고정 |
| MaxDD 표본 | 절반 초과 3건 | 추세 6 · 50/50 7건 |

## 하나는 걸렸다 — 고쳤다

세 다리의 **실현 변동성이 서로 달랐다**(목표 대비 1.14 · 0.73 · 0.81). 그 상태로
낙폭만 보면 「섞으면 반이 된다」로 읽히는데 얼마는 **덜 걸어서 덜 아팠던 것**이다.

    최대낙폭     추세 3,189만 · 매크로 2,770만 · 50/50 1,605만
    위험맞춤     추세 3,189만 · 매크로 4,303만 · 50/50 2,252만
                                  ↑ 매크로는 오히려 «더» 크다

→ `maxDrawdownVolMatched`·`ulcerVolMatched` 를 서버가 같이 내고 카드에 열이 섰다.

## 오너 결정 셋 (2026-09-09)

1. **자본 분모를 안 만든다.** 퍼센트 지표 대신 무차원 비율로 비교한다 —
   Sharpe·Calmar·**Martin**·위험맞춤 낙폭. 퍼센트가 꼭 필요하면 가정을 옆에 적는다.
2. **Ulcer 는 MR 과 같은 잣대로** 낸다(`mrmetrics` 의 그 식 = 낙폭 경로의 RMS).
   부호는 안 뒤집는다.
3. **공용 모듈(mrmetrics)은 지금 그대로.** Momentum 화면은 자기 `_card` 를 쓴다.
   ⚠ 단 `scripts/cta_macro.py`(연구)는 mrmetrics 를 부른다.

## 교차 비교 — `scripts/lane_compare.py`

공통 창 2020-01-02 ~ 2026-09-07(1,640일)에서 같은 배터리를 둘에 돌렸다.
그냥 놓으면 셋이 어긋나서 셋을 맞췄다: **창 · 위험 · 자기상관.**

세 번째가 새로 나온 것이다 — **MR 은 AR(1) +0.210, Momentum 은 −0.15.** 일별
Sharpe 에 √252 를 곱하는 것은 날들이 독립일 때만 맞으므로, 한쪽만 부푼 자를
그대로 쓰면 같은 기준이 아니다. Lo(2002) 보정을 둘 다에 걸었다.

|  | Sharpe | AR보정 | Calmar | Martin |
|---|---:|---:|---:|---:|
| MR 통합(BSS 9다리) | 1.75 | **1.42** | 1.77 | 14.98 |
| Momentum 추세 | 0.44 | 0.51 | 0.39 | 0.77 |
| Momentum 50/50 | 0.53 | **0.62** | 0.42 | 0.89 |

성질(지표를 믿어도 되나)에서는 **Momentum 이 네 곳 다 낫다** — 자기상관
(−0.15 대 +0.21) · 초과첨도(10.3 대 68.8) · 낙폭 표본(5 대 4) · 비용 쿠션
(14.6배 대 5.04배). 수준은 아직 못 읽는다(아래).

## MR 수리가 끝나야 풀리는 것 [OWNER 2026-09-09 — 「MR 부분 수리 끝나고 다시」]

1. **증거금 상한판(B/50) 위에서 다시 재기.** 지금 MR 장부는 상한 무시판이라
   (통합 SR 1.74, 상한 아래 0.8) 「누가 크냐」를 말할 수 없다. 성질만 읽었다.
2. **비용 모델 파라미터화.** MR 의 비용 쿠션 5.04배만 자기 전체 창의 값이다 —
   장부 캐시가 일별 비용을 안 들고 있어 공통 창에서 다시 못 잰다. 유일하게
   못 맞춘 칸이고, MR Phase 1 의 ②·③ 이 정확히 이 자리다.
3. **DSR 을 계산할 때 첨도 보정.** MR 첨도 68.8 을 정규 가정으로 다루면 MR 이
   부당하게 유리해진다.

재개하면 `python -m scripts.lane_compare` 한 줄이면 된다.
