# 인계 — 2026-09-22 (sauron-v2)

아래 블록을 통째로 붙여 넣으면 이어받을 수 있다.

---

## 붙여 넣을 블록

```
2026-09-22 세션을 이어받는다. 원화 금리 데스크, `C:\Users\infomax\Projects\apps\sauron-v2`.

【지금 상태 — 제일 먼저 알 것】
· **전부 푸시·배포됐다.** `origin/main` = 로컬 `master` = `ec76af41`, 미푸시 0.
  rateslab 에서 실제로 열어 새 화면이 서는 것까지 확인했다(아래 「확인한 것」).
· 프로덕션 백엔드 `:8200` 은 **스케줄 태스크 `SauronV2Backend` 소유**로 새 코드를
  돌고 있다. 개발 `:8299`·프런트 `:3200` 도 떠 있다. Funnel 200.
· 작업 트리는 깨끗하다. 미커밋 둘(`docs/LIT_*_2026-09-17.md`)은 **다른 레인 것**이라
  안 건드렸다. 계속 두면 된다.
· 게이트: tsc 0 · eslint 0 · vitest **1,822**(110파일) · pytest 전수
  **1,430 passed · 7 skipped · 1 xfailed**(마지막 커밋 뒤 실측, 11분 26초,
  `--ignore=tests/test_static_agreement.py`).
  ⚠ `test_static_agreement.py` 는 **살아 있는 :8200 을 요구**한다 — 쪽지(dev marker)의
  PID 가 그 포트의 프로세스와 같아야 통과하므로, 프로덕션이 떠 있는 동안은 항상
  거절한다. 그게 그 시험의 설계다(라이브에 대고 테스트하지 않는다).
  ⚠ 전수 시간이 **11분~36분으로 크게 흔들린다**(같은 시험, 같은 날 실측). SQL 을
  타는 시험이 많아 DB 응답에 달려 있다 — 느리다고 뭐가 깨진 것이 아니다.
  ⚠ 부하가 걸린 채로 vitest 를 같이 돌리면 `virtualization`·`surface3d`·
  `model-method-rendered` 가 흔들린다(단독으로는 28/28 통과) — 동시에 돌리지 말 것.

【오늘 한 것 — 커밋 넷】
  f145bb5e  포트폴리오를 포지션 카드 하나로 줄인다 [OWNER "지금은 일단"]
  1da3def1  부호 규약을 뒤집는다 — 스왑 − 채권현물/국채선물 [OWNER]
  3a4bb90a  체결일은 오늘이지 자료의 날이 아니다 [OWNER]
  ec76af41  청산 레벨도 내가 적는다 [OWNER]

정본은 코드와 두 곳에 있다:
  docs/MR_LANE_STATE.md **§5¾-I**   부호 규약 뒤집기(전말·검정·드러난 옛 결함)
  backend/app/paper.py::score_leg   체결/마크 두 시계 — 이 파일 머리가 근거다

【★★★★ 규약이 뒤집혔다 — 이걸 모르면 모든 수를 반대로 읽는다】

    BSS  IRS − 국고   ·  FSW  IRS − 선물내재  ·  ASW  IRS − 민평
    FUT · IRC · IRF · 신용SP 는 **그대로**(스왑 대 현물이 아니다)

틀려서 바꾼 게 아니다 — 바로 앞 커밋의 전수조사(§5¾-H)가 「부호 규약 넷 다 맞다」로
닫은 자리이고, 데스크가 부르는 말로 옮긴 것이다 [OWNER "뜻이 뒤집히는게 정상이야"].

**계열 부호만 바꾸면 안 되는 자리가 넷이었다.** 새로 계열을 붙이거나 부호를
만질 일이 생기면 이 넷을 같이 본다:
 1. `mr.TRADABLE_DIRS["bss"]` — 금지는 부호가 아니라 「국고를 파는가」에 걸려 있다.
    한쪽만 뒤집으면 **대차매도가 조용히 열리고** 백테스트가 대차료 0 으로 늘 이긴다.
 2. `mrcarry` 캐리 두 항 — 캐리는 「`position=-1` 의 캐리」이고 엔진이 `-position` 을
    곱한다. `-1` 이 가리키는 **거래가 바뀌면** 캐리도 부호를 바꾼다.
 3. `mrcarry.LEG_NAMES` 차례 — 대사표가 **이름 차례로 값을 첨자**한다. 안 바꾸면
    이름과 값이 엇갈린 채 선다(예외 없음).
 4. `futures.py` 의 FSW 다리(`fut_dir`·`fsw_swap_leg`) — 「+1 = 스프레드 확대」가
    계속 참이려면 다리도 같이 뒤집혀야 한다.

**검정이 서 있다**: `backend/tests/test_mr_convention.py`(17건). 계열과
`TRADABLE_DIRS` 를 같이 뒤집으면 `|z|` 불변이라 거래도 돈도 같아야 한다 —
HEAD worktree 대조에서 총손익 24,797,740원 · 낙폭 10,573,664 · 승률 0.6190 ·
거래 21건이 **양쪽 동일**, 방향 라벨만 반대였다.

캐시는 **v22**. 「칸은 그대로고 그 칸의 수가 부호를 바꾸는」 승급이라, 안 올리면
화면이 새 어휘로 옛 부호를 읽는다(에러 없이 방향만 정확히 거꾸로).

【★★★★★ 오늘 배운 것 — 규약 뒤집기는 «부호 감사기» 다】
대조표에서 **다른 칸이 전부 뒤집히는데 2.5Y 만 Δ 가 그대로였다.** 민평엔 있고
IRS 엔 없는 만기가 셋이라(2.5Y·20Y·30Y) 그 칸의 node 계열이 자산스왑 스프레드가
아니라 **민평 커브(%)** 였고, 자산스왑 북의 Δ 배율은 1.0(이미 bp)이라 그 칸만
«%를 bp 로 읽는» 100배 오차였다. 축도 달랐다(합계 줄은 스프레드 축, 그 칸만 채권).
작아서 안 밟혔다 — 돈엔 안 들어가고 추정·잔차에만 섰다.

  ⇒ **모든 칸이 같이 뒤집혀야 하는데 안 뒤집히는 칸은 애초에 다른 규약 위에 있던
     것이다.** 이 결함은 「BSS 를 뒤집어라」와 아무 상관이 없고, 뒤집지 않았으면
     안 보였다. 다음에 규약을 만질 일이 있으면 그 기회를 감사로 쓸 것.

【★★★★ 페이퍼 북에는 시계가 둘이다 — 체결(장중)과 마크(종가)】
> [OWNER] "민평 종가는 9월 21일까지 들어와있지만, 오늘 장중에 내가 스왑 금리를
> 보고 포지션을 진입했다면 그건 22일날 진입한거임"

새 칸을 만들 때마다 물을 것: **이 날짜는 「내가 한 일의 날」인가 「자료가 찍힌
날」인가.** 셋이 한꺼번에 서 있었다:
· 체결일 기본값이 `asof`(자료의 날)였다 → 오늘 한 거래가 **어제 날짜로** 들어갔다.
  이제 서버가 `today` 를 따로 싣고 화면이 그쪽을 쓴다. 조건 바가 두 날을 나란히 적는다.
· **마크가 체결일보다 앞서도 손익을 매겼다** — `mark_of` 는 처음부터 `(날짜, 값)` 을
  냈는데 `build_sheet` 가 날짜를 `_` 로 **버리고** 있었다. 「어제 종가 − 오늘
  체결가」는 손익이 아니라 시간을 거꾸로 센 수이고, 부호까지 그럴듯하다.
  실측: BSS 2Y 두 다리를 오늘 담으면 **1,300만원대**가 섰다. 이제 「아직」이다.
  ★**청산한 다리는 안 막는다** — 청산 레벨은 내가 적은 것이라 종가를 안 쓴다.
· 청산 레벨 칸이 없었다 — 라우트는 처음부터 레벨을 받았고 주석도 「내가 적는다」
  였는데 화면은 **종가 버튼 하나뿐**이었다. 그래서 한 거래 안에 「진입은 내 체결가 ·
  청산은 종가」라는 **두 규약**이 섰다. 비워 두면 종가로 떨어져 종전 동작이 산다.

같이 나온 「없는 일을 했다고 적는」 자리 셋을 422 로 막았다 — `close_leg` 가
**없는 다리·이미 닫힌 다리를 조용히 넘겼다**(200 + 「닫았어요」 + 장부 그대로),
청산일이 **체결일보다 앞설** 수도 **미래일** 수도 있었다. **같은 날은 안 막는다**
(오늘 열고 오늘 닫는 거래는 정상 — 시험이 그 핀이다).

【⚠⚠ 스크린샷으로 겹침·잘림을 판정하지 말 것 — 오늘 두 번 오판했다】
1870 CSS px 화면이 1568 px 스크린샷으로 줄면서 인접 요소가 붙어 보이고 JPEG 가
경계를 뭉갠다. 두 번 다 DOM 으로 재니 멀쩡했다(손익 글자 1204~1626 · 청산 칸
1642~). 도구는 셋: `getBoundingClientRect` · `Range.getBoundingClientRect`
(글자의 실제 폭) · `elementFromPoint`(덮는 것이 있나). `scrollWidth==clientWidth`
는 `overflow:visible` 에서 못 믿는다 — 글자 range 를 직접 재라.

【⚠ 셸 함정 둘 (오늘 밟았다)】
· **한글이 Git Bash `curl -d` 를 못 지난다.** `/api/paper/reset` 의 확인 낱말
  「초기화」가 깨져 400 이 났다. `python urllib` 로 UTF-8 인코딩해 보낼 것.
· `grep ... | head` 로 「있나 없나」를 세지 말 것 — `head` 는 늘 0 을 내서 `&&`
  뒤가 무조건 돈다. 오늘 배포 번들 검사가 그렇게 헛돌았다. `grep -c` 로 셀 것.

【확인한 것 — rateslab 에서 직접】
  Strategy→MR     BSS 2Y `IRS − 국고` −1.0bp · IRS 3.9950 · 국고 4.0050
                  「−1.4bp 이하 시 IRS 페이 · 국고 매수」
                  퓨처스왑 3Y `IRS − 선물내재` −10.5 · 「IRS 페이 · 선물 매수」
                  BSS 통합 `IRS − 국고 전 만기 묶음` · 커브·플라이는 그대로
  Backtest→퓨처스왑 방향 세그먼트 「IRS 페이 · 선물 매수 / IRS 리시브 · 선물 매도」
                  진입 레벨 `−10.5bp / IRS − 내재`
  Portfolio       규칙·수동 북 없음 · 조건 바 「오늘 2026-09-22」 · 체결일 기본값 오늘
★프런트 번들이 새것이라는 결정적 증거는 Portfolio 다 — 북 제거와 「오늘」 칩은
 순전히 프런트 변경이라 Vercel 빌드가 안 갔으면 못 보인다.

【어제 인계문에서 **닫힌** 것 — 다시 열지 말 것】
· **아침 갱신 결함(하루 늦은 Main·Backtest)은 운영까지 섰다.** `SauronV2Refresh`
  반복 트리거가 실제로 걸려 있다 — `PT30M` / `PT12H`, 오늘 16:30 실행 결과 0,
  다음 17:00. 어제 인계문의 「▶ 남은 것 = 오너 결정 하나(반복을 걸 것인가)」는
  **닫혔다.** 근거·판정 다섯 상태는 `BACKEND.md` 와 `scripts/check_close.py`.
· 리드아웃 이관은 **`rv/RvScatter` 하나만 남았다**(`placeReadout` 을 지나는 표면
  0). 산점도가 마지막인 데는 이유가 있다 — 고정 줄의 계약이 「한 날 + 그날의
  값들」인데 산점도는 축이 둘이라 커서 밑의 「한 날」이 없다. 옮기려면 줄의
  계약부터 바꿔야 한다.

【★ 열려 있는 오너 결정 — 오늘 것】
1. **청산일 칸.** 지금은 레벨만 내가 치고 청산일은 **오늘 고정**이다. 어제 청산한
   것을 오늘 적는 경우가 생기면 체결일 칸과 같은 모양으로 넣으면 된다(서버는 이미
   `check_exit` 로 «미래 금지 · 체결일보다 앞서기 금지» 를 본다).
2. **규칙 북·수동 북을 언제 되살릴 것인가.** [OWNER "지금은 일단"] 화면에서만
   뺐고 서버(`paper.py`)는 한 줄도 안 건드렸다. 되살릴 때 **새로 만들지 말 것** —
   `guards/portfolio-canon.test.ts` 에 「돌아오면 이 줄을 되살려라」가 적혀 있고,
   빠진 임포트 둘(`@/chart/TimeChart`·`@/mr/parts`)도 거기 적혀 있다.
3. **백테스트 북 「진입 레벨」 칸이 청산일 입력과 1px 겹친다**(`gap = −1`). 글자는
   안 잘린다(상자 96px, 글자 49·56px). 이 리포 얼라인 규칙 7(컨트롤 사이 빈틈)
   대상이라 잡을지 판정 필요 — 이번 변경과 무관한 기존 간격이다.

【열려 있는 오너 결정 — 이전부터 (앞 인계문에서 살아 있는 것)】
4. 모멘텀 북을 실제로 켤 것인가 + 원장 첫 채점 행
5. k_mr 축소를 MR 레인에 지시할 것인가 — 모멘텀 화면이 매일 「안 줄이면 못 선다」를
   붉게 적고 있는 그 가정
6. 테너: **위약(순환이동)을 안 쟀다** = 레인 게이트 셋 중 하나가 빈 채다.
   돌릴 것인가 · 「전 테너 등가중」으로 새 사전등록을 열 것인가
   (탐색의 답은 **「고르지 말라」** — 등가중이 자유도 0 으로 231칸 중 201개를
   이긴다. 정본 `backend/output/MOMENTUM_TENOR_SEARCH_2026-09-21.md`)
7. 크로스헤어 자석(`CrosshairMode.Magnet`)과 구슬이 다른 수를 가리킬 수 있다 —
   끄면 15개 차트 전역 변경이라 아직 아무도 안 건드렸다
8. 닿지 않는 `reverse_exit` 게이트를 지울 것인가(`mrbacktest.py:137`)

【환경 메모】
· 배포 순서는 **백엔드 재기동이 먼저**, 그 다음 프런트 푸시다. 프로덕션 백엔드는
  이 작업 트리에서 돌기 때문에 재기동하는 순간 백엔드 변경이 전부 올라간다.
· 재기동: `:8200` 리스너 PID 를 직접 죽이고 `Start-ScheduledTask SauronV2Backend`.
  ⚠ `Stop-ScheduledTask` 로는 uvicorn 이 안 죽는다.
  ⚠ **세션에서 uvicorn 을 직접 띄웠으면 끝나기 전에 스케줄 태스크로 되돌릴 것** —
    안 그러면 세션이 끝날 때 프로덕션이 같이 죽는다(오늘 그 상태였고 되돌렸다).
· `.env.development.local` 이 `:8299` 를 가리키고 **개발 서버는 뜰 때 한 번만**
  읽는다. 백엔드는 `--reload` 가 아니다 — 코드를 고쳤으면 **재기동해야 화면이
  바뀐다**(오늘 오너가 「아직 적용 안된듯」이라 한 것이 이것이었다).
· `next build` 와 `next dev` 가 `.next` 를 공유한다. 빌드 뒤 `rm -rf .next` +
  개발 서버 재기동.
· 페이퍼 장부는 `backend/data/paper_book.json`, 보관은 `backend/data/paper_archive/`
  (둘 다 gitignore). 오늘 시험 다리는 `paper_book_20260922-163202.json` 에 있다.

【정본 문서】
  docs/MR_LANE_STATE.md    MR 레인 — **§5¾-I 가 오늘 것**(부호 규약)
  NEXT_PROMPT-macro.md     모멘텀 레인
  NEXT_PROMPT-momentum.md  KTB 모멘텀 레인(머리에 은퇴 표시)
  BACKEND.md               아침 데이터 갱신 — 스냅샷 백엔드의 하루
  CLAUDE.md                UI 캐논 · 얼라인 · 말줄임 금지 · 키보드
```

---

## 이 파일을 지운 뒤

인계가 끝나면 이 파일은 지운다 — 리포에 인계문이 쌓이면 어느 것이 지금인지
읽는 사람이 모른다. 서 있는 사실은 위 「정본 문서」에 이미 들어가 있고,
어제 것(`HANDOFF-2026-09-21.md`)은 이 파일이 대체한다.
